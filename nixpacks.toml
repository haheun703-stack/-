[phases.setup]
nixPkgs = ["python312", "python312Packages.pip"]

[phases.install]
cmds = ["pip install -r requirements-railway.txt"]

[start]
cmd = "PYTHONPATH=/app python scripts/start_dashboard.py --host 0.0.0.0 --port ${PORT:-8000}"
