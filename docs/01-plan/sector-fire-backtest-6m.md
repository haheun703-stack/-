# SECTOR FIRE 점수 시스템 — 6개월 백테스트 + 개선

**작성**: 2026-05-14
**근거**: 5/14 액침냉각 53점 1등 BUY 4종목 정확도 25% — 시스템 검증 필요
**기준**: 퐝가님 지시 "6개월치 백테스트가 필요하다, 그 이후에 개선을 해야 한다"
**전제**: 퀀트봇 자동매매 ON 전 필수 작업

## 1. 문제 정의

### 5/14 실측 데이터로 본 시스템 약점

| 종목 | 점수 | 시그널 | 외5d | 기5d | 등락 | 진단 |
|------|-----|------|------|------|------|------|
| SK텔레콤 | 47 | BUY | +805 | **-160** | -2.9% | 외인만 매수, 기관 매도 — False BUY |
| GS | 37 | BUY | +58 | -141 | +3.9% | True (운) |
| 유니셈 | 37 | BUY | +1 | +4 | -2.1% | 수급 미미 — False BUY |
| SK이노베이션 | 35 | BUY | -33 | -27 | -2.3% | 수급 음수 — 시그널 오류 |
| **LG전자** | **0** | - | **+1,054** | **+1,197** | **+13.4%** | **Missed Alpha!** |
| 삼성물산 | 5 | - | -854 | +305 | +3.2% | 점수 낮은데 상승 |

### 3가지 근본 약점

1. **외+기 동시매수 가중 미반영** — SK텔레콤(외+805/기-160) 과대평가
2. **시장 레짐 보정 부재** — 시장 약세일에도 점수 그대로 → False BUY 증가
3. **점수 산식 불완전** — LG전자 같은 +2,251억 압도적 수급 종목 캐치 못함

## 2. 목표

- **6개월(180일) 백테스트** → 점수 vs 실제 수익률 상관관계 정량화
- **개선 산식 도출** → BUY 적중률 25% → **목표 60%+**
- **실전 적용 가능한 점수 시스템** 확립 → 자동매매 진입 전 검증

## 3. 데이터 소스

### A. 점수 데이터 (X축)
- **Supabase `quant_sector_fire`**: 2026-04-25 ~ 현재 (20일 누적)
- **Supabase `quant_supply_surge`**: 2026-04-23 ~ 현재
- **Supabase `quant_supply_chain`**: 2026-04-26 ~ 현재
- **부족 시**: 과거 30일치만이라도 검증 (6개월은 점진 누적)

### B. 가격 데이터 (Y축)
- **정보봇 OHLCV CSV** (`data/external/jgis_ohlcv/`, 2,629종목 × 1~2년)
- **퀀트봇 stock_data_daily/** (보완)
- D+1, D+3, D+5, D+10, D+20 수익률 계산

### C. 교차 검증
- **정보봇 `stock_picks_verify`** (Supabase): 매일 17:25 적중률 검증
- **퀀트봇 FLOWX `signals`** 테이블: 과거 시그널 기록

## 4. 단계별 진행

### Phase 1: 데이터 수집 + 정합성 검증 (1세션)

- [ ] Supabase quant_sector_fire 전체 export (CSV/parquet)
- [ ] 정보봇 OHLCV에서 동일 종목 코드 조인 검증
- [ ] 결측 일자 + 종목 카운트 보고

### Phase 2: 점수 vs 수익률 상관관계 측정 (1세션)

```python
# 기본 백테스트 흐름
for date in [4/25 ~ 5/14]:
    for ticker in fire_picks(date):
        score = sector_fire.get(date, ticker)
        d1_return = (close[date+1] - close[date]) / close[date]
        d3_return = ...
        records.append({date, ticker, score, d1, d3, d5})

# 결과
correlation_d1 = corr(score, d1_return)
correlation_d3 = corr(score, d3_return)
```

**측정 지표**:
- 점수 vs D+N 수익률 Pearson correlation
- 점수 구간별(>=40, >=30, <30) 평균 수익률
- BUY 시그널 적중률 (D+N > 0 비율)

### Phase 3: 개선안 도출 (1세션)

**가설 검증**:
- H1: 외+기 동시매수 가중 +10점 → 적중률 증가
- H2: 시장 레짐(KOSPI -1%+) 디스카운트 -5점 → False BUY 감소
- H3: 외인 강한매수 + 기관 강한매수 동시 조건 → 상위 종목만 추출

**A/B 비교**:
- 현재 점수 vs 개선 점수
- 적중률 + 평균 수익률 + Sharpe ratio

### Phase 4: 시스템 패치 적용 (1-2세션)

- [ ] `scripts/scan_sector_fire.py` 점수 산식 수정
- [ ] 환경변수로 A/B 토글 가능 (`FIRE_SCORING=v1` or `v2`)
- [ ] 1주일 병행 운영 (v1 + v2 결과 모두 기록)

### Phase 5: 자동매매 진입 결정 (1세션)

- 6개월 누적 검증 + 1주일 실전 병행 결과
- 적중률 60%+ + 평균 수익률 +0.5%+ 시 자동매매 PDCA P0 진행
- 미달 시 추가 개선 또는 다른 시그널 채택

## 5. 검증 기준 (정량)

| 항목 | 현재 (추정) | 목표 |
|------|---------|------|
| BUY 시그널 D+1 적중률 | ~25% | **60%+** |
| BUY 시그널 D+3 평균 수익률 | -0.5%~+0.5% | **+1.0%+** |
| 시그널 vs 수익률 correlation | <0.1 | **0.3+** |
| False BUY 비율 | ~75% | **<40%** |
| Missed Alpha (LG전자 같은 호재) | 빈번 | **<10%** |

## 6. 데이터 한계 + 대응

| 한계 | 대응 |
|------|------|
| quant_sector_fire는 4/25부터 (3주만) | 6개월 누적은 시간 필요. 시작은 3주로 |
| 시장 레짐 데이터 (KOSPI 등락) | `data/macro/macro_history.csv` 활용 (ECOS 매크로) |
| 거래량 부족 종목 | 시총 상위 200종목으로 필터링 |
| 시차 (시그널 발생일 vs 실제 매매 가능일) | T+1 진입 가정 (16:30 시그널 → 다음날 09:00 매수) |

## 7. ETF + 대형주 전용 모드 검증 (퐝가님 전략)

별도 백테스트 트랙:
- 대상: ETF 100개 (정보봇 마스터) + KOSPI200 대형주
- 같은 점수 시스템이 ETF/대형주에서 더 잘 작동하는지 검증
- 가설: 유동성 큰 종목 → 슬리피지 낮음 → 점수 시그널 더 정확

## 8. 위험 관리

- 백테스트 결과 만족 못해도 **억지로 자동매매 진입하지 않음**
- 점수 산식 변경 시 기존 시그널 7일간 병행 운영
- 모든 변경은 환경변수 override로 즉시 롤백 가능

## 9. 다음 액션 (즉시)

**Phase 1 시작**: Supabase quant_sector_fire 전체 export → 정보봇 OHLCV 조인 검증

```python
from supabase import create_client
sb = create_client(URL, KEY)
res = sb.table('quant_sector_fire').select('*').execute()
df = pd.DataFrame(res.data)
print(f'기간: {df.date.min()} ~ {df.date.max()}, 총 {len(df)}건')
```

## 10. 의존 작업

- 정보봇 OHLCV 통합 Phase 1 완료 ✅ (5/14)
- BAT-D 시간 최적화 5건 완료 ✅ (5/14)
- 5/15 BAT-D 실측 결과 (백테스트와 별개로 자동 진행)

---

**현재 단계**: **Plan 완료**, **다음 → Phase 1 데이터 수집** (다음 세션)

**중요**: 본 백테스트가 *완료되고 적중률 60%+ 검증된 후*에만 자동매매 PDCA의 P0 가드레일 단계 진행. 그 전에는 **자동매매 ON 절대 금지** (퐝가님 지시).
