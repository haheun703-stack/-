# 퀀트봇 TODO — 2026-06-18 (목·거래일)

> 6/17(수) 이월. 상세 맥락: 메모리 `session_state_6_17_backfill_sectorfire.md`.
> ★freeze·실주문0·매매로직0·production 배선0 유지.

## 🔥 우선 (바로 진행)

1. **자동화 첫 가동 확인** — `QuantumLocalDailyUpdate`(평일 16:10·StartWhenAvailable)가 실제로 돌았는지 점검.
   - 노트북 16:10에 꺼져 있었으면 켠 직후 캐치업 실행됨 → `logs/daily_local_update.log` 확인.
   - 6/18 데이터(종가 parquet·지표·수급db·kospi csv·종목별csv·밸류밴드) 6단계 정상 적재됐는지 검증.
   - ⚠️ KIS 토큰 1분당1회 제한 — 3단계(collect_investor_kis)가 토큰에러로 실패했으면 단독 재시도.

2. **지수 시스템 예고 인계서 수신 처리** — 정보봇이 `docs/from-jgis/`로 보낼 예정(지수 레벨 공급 예고).
   - 도착 시: 스키마·지수정의·갱신주기·finality 필드 검토 → 매매활용 협의 회신.
   - ★freeze 제약 명시: 지수 레벨은 **관측·보조 레이어로만**, hard gate 금지(sector_momentum·factor_exposure 동일 원칙). 실연동은 unfreeze 후.
   - LIVE 데이터는 정보봇이 내일 DDL→적재→배포 후 별도 통보 예정.

## 🔒 사장님 결정 대기 (커플링 세트)

3. **서버 배포 / E-0 배선 / KRX_PW** — 3건 묶음(6/17 보류).
   - 서버 HEAD `12ca898`(6/10) → 전체 pull = 리스크엔진+실행게이트(C-ii) **production 첫 배포**(실주문은 KILL_SWITCH 차단이나 "production 배선0" 불변식 깨짐). 신중 결정.
   - E-0(페이퍼 게이트 드라이런 배선) 트리거 = 서버 배포 결정. 배포 시 E-0배선+배포+gate_log적재확인 3종 동시(체크리스트 `3facb63`).
   - KRX_PW 갱신(연기금/금융투자 bulk 복구) — 사장님 KRX 사이트 비번 확인 필요. 서버 KRX(nationality)는 유효하나 pykrx STAT 경로는 거부 지속.

## 🧹 슬러지 정리 결정 (6/17 검수 발견)

4. 삭제 여부 확인 후 처리:
   - `quant_bot (1).py`(33K)·`hybrid_design_6_1.bundle`(13K) — 6/14 다운로드(사장님 참조 코드?).
   - `D:\home\ubuntu\...`(3.8M) — 로컬 export 서버경로 하드코딩 부작용, 프로젝트 밖. 지워도 무방.
   - (P2) export 스크립트 서버경로 하드코딩 → 로컬 실행 시 다른 경로 쓰도록 보완 검토.

## ⏳ 대기/조건부

- **서버 sector_fire 재발방지**: 6/16은 로컬백필로 해결, 6/17+ 서버분은 배포 전까지 동일 NaN FAIL 가능(fix `93233d2` 배포 시 효력).
- **보험·금융 FCF**: 소스 null 처리 완료(`33e3f9f`), 웹봇 통보함. 웹 렌더 확인 회신 대기.
- **2층/알림 실데이터**: unfreeze(포트 실운용) 후.

## ✅ 완료 (6/17)

- 6/16+6/17 기초데이터 전량 backfill + **일일 자동화 구축**(BAT `6348ae9`+Win스케줄러).
- 밸류밴드 KR/US 적재·웹봇 회신 3건·sector_fire NaN fix(`93233d2`)·E-0 보류 박제(`3facb63`).
- 정보봇 회신서 후속(보험FCF null·drift 2건·가치함정 2경로) `33e3f9f`.
- 전체 검수: 코드 P1버그0·테스트 신규실패0(31 baseline=날짜만료)·슬러지 스윕.

---
*freeze 유지. unfreeze는 서버배포→E-0→20거래일 후.*
