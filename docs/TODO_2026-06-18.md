# 퀀트봇 TODO — 2026-06-18 (목·거래일)

> 오전 진행분 반영 + **사무실 이월**. ★freeze·실주문0·매매로직0 유지.
> 상세 맥락: 메모리 `feedback_runtime_source_of_truth.md`(런타임=서버/Supabase가 정본).

---

## ✅ 오전 완료

1. **자동화 첫 가동 점검 — 정상.** `QuantumLocalDailyUpdate` 평일16:10·StartWhenAvailable·Enabled·BAT 6단계 검증. LastRun=never는 정상(6/17 16:10 이후 등록됨). **데이터 6/17까지 적재(누락0).** 6/18은 16:10 첫 자동실행.
2. **E-0 페이퍼 게이트 드라이런 배선 — 완료·커밋·푸시 (`617fcf0`).**
   - `src/use_cases/paper_gate.py`(PaperBalancePort + run_paper_gate_dryrun·graceful·enforce=False·로그 분리 `gate_logs_paper`) + `paper_trading_unified.enter_new_positions` 1줄 배선 + 테스트 10케이스.
   - 검증: 005930 매수→verdict PASS(adv20 7.4조·VaR95 0.6%)·gate_log 1줄 실증. 회귀 신규실패 0(4건 baseline=날짜만료/sell-path).
   - ★효력은 **서버 배포 후**(서버 페이퍼 엔진이 옛 코드라 로컬 배선만으론 로그 0).
3. **슬러지(#4) 정정 종결.** D:\home는 삭제 대상 아님 — 매일 AM/MIDDAY/PM 도는 **인텔시그널(정보봇 계열) 활성 출력**이 `/home/ubuntu/...` 하드코딩으로 새는 것(우리 repo 무관·재생성). 참조파일 2개(quant_bot(1).py·bundle)는 보존(untracked).

---

## 🔥 사무실 이월 — 실제 개선 본론

### A. ★진입 "확신 모델"이 거꾸로 박혀 있다 (페이퍼 63건 해부, 6/18 오전)

서버 `paper_portfolio.json`(일반 페이퍼 SCAN 63 청산) 해부 결과 = **손절 룰 문제가 아니라 진입신호 문제**:

- **전체 edge 없음**: Profit Factor **1.14**, 승률 48%(승30 +221%/패33 −194%) → 비용 빼면 마이너스. = −14.3%의 정체.
- **★발견2 — AA등급이 A등급보다 나쁘다(확신 역전)**: A 18건 승61% **+4.41%** vs **AA 45건 승42% −1.17%**(최고확신이 손실).
- **★발견3 — "쌍끌이매집"(최강 매수신호)이 최악**: 외인단독 80%·+4.95% / 쌍매도 60%·+2.89% / **쌍끌이매집(외+기 동시매수) 33%·−1.4~2.0%**.
- **한 뿌리 가설**: AA = 쌍끌이매집 요구 → "모두 이미 산 종목을 최고확신 매수 = 천장 진입 → 반전". 발견2·3 동일 병.
- 부차: 손절 6/15건이 −7%룰 뚫고 −10~15%(갭 오버슈팅) = 2차 문제.
- ⚠️ 표본한계 정직히(AA n=45/A n=18/쌍끌이 n=15) — 작지만 방향+메커니즘 또렷 → **운보다 코드 로직 의심 단계**.

**→ 첫 삽 (사무실)**: ① 등급 부여 로직 코드 까서 *정말 AA가 쌍끌이매집을 요구하는지* 가설 검증 → ② 맞으면 **확신 반전 재설계**(합의매수 감점·외인단독/선행 가점, sector_momentum·factor_exposure·밸류밴드 연결) → ③ 페이퍼에서 A/B 검증. ★게이트 B(흑자)를 여는 열쇠. ★엔트리 개선은 E-0 게이트검증(리스크한도)과 직교 — 병행 가능.
- ⚠️ "매매로직0 변경" 불변식과 충돌 검토 필요(페이퍼 전략 개선 vs freeze 베이스라인) — 사장님 결정.

### B. ★서버 배포 (E-0 포함 60커밋) — 자동/타이밍 재논의

- 계획서: [server-deploy-e0-plan_2026-06-18.md](01-plan/server-deploy-e0-plan_2026-06-18.md). 서버 HEAD `12ca898`(6/10)→현재 `617fcf0` 전체 pull = 리스크엔진·E-0 production 첫 배포 → **20거래일 페이퍼 시계 시작**.
- ⚠️ **장중 금지**(scheduler 재시작). 마감 후 윈도우.
- ⚠️ **"자동 스케줄 배포" 재검토 필요**: 노트북 OFF(6/18 오전 사장님 끔)면 로컬 예약태스크가 안 돎(SSH+pem 로컬 필요). 사무실서 ①수동-마감후 vs ②노트북 켜둔 채 1회 예약태스크(검증+자동롤백 포함) 결정.
- 검증 3종(배포 후): gate_logs 적재 시작·`import risk.*` 성공·sector_fire 6/16 재업로드 효력.

### C. KRX_PW 갱신 (사장님)
- 로그인 ID `ha08080907`, 비번 2개 갈림(`KRX_DATA_PW=...0045^^` vs `KRX_PW=...0907^^`) = 불일치가 거부 원인 후보. 링크 https://data.krx.co.kr / 고객센터 **1577-0088**. 통하는 비번 확인 → 로컬+서버 `.env` 통일. 저녁 배포 선행(연기금 bulk 복구).

---

## ⏳ 대기/조건부
- 정보봇 **지수 시스템 예고 인계서** — `docs/from-jgis/` 미도착(대기). 관측·보조레이어만·hard gate 금지.
- 보험·금융 FCF null(`33e3f9f`) 웹 렌더 확인 회신 대기.
- 2층/알림 실데이터 — unfreeze 후.

---
*freeze 유지. unfreeze = 서버배포(E-0) → 20거래일 + 전략 흑자전환 둘 다 초록일 때.*
