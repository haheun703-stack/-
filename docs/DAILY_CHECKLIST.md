# 데일리 운영 체크리스트 — 퀀트봇

> **용법**: 매일 세션 시작 시 §1 아침 루틴을 수행하고 §4에 당일 엔트리를 기록한다.
> 발견한 개선점은 **각주로 근거를 남겨** §3 백로그에 등재하고, 매일 최소 1건을 소화한다.
> 마감 시 §2 자기성찰 3질문에 답한다. 이 문서가 "매일 개선해나가는 퀀트봇"의 정본 루프다.

---

## 1. 매일 아침 루틴 (확인 — 0번 규칙 확장판)

> ★★**2026-07-27 데이터계약 전환 — 퀀트봇 = 계산봇**. 개정 자본시장법(유사투자자문 규제) 대응으로 FLOWX가 시그널/픽/추천/성과 서비스를 폐지하고 데이터·교육 플랫폼으로 전환. 매매판단 16테이블의 Supabase 적재를 중단(`b7ae38b`)했고, **목적함수가 "수익/알파"에서 "계산 산출물의 정확성·건강성 + 데이터계약 준수"로 바뀌었다**. → 아래 A-0(스코어보드)와 §3 수익형 백로그(B-18·B-20·B-22·B-16)는 전략 판정 목적이 소멸해 보류. 근거: 운영자 지시서 260724 / 웹봇 회신 d727478. 상세 [[session_state_7_27_data_contract_pivot]].

| # | 체크 | 방법 |
|---|------|------|
| **A-★** | **봇 간 수신함 확인 (신설 — 세션 시작 즉시)** | 웹봇 `D:/kr.TradingView.site_웹봇/`·정보봇 `docs/from-jgis/`에 신규 `[→퀀트봇]`/`[운영자→]` 지시서·회신이 있는지. **7/24~27 3거래일 방치 재발방지** — 최근 7일 `find ... -newermt`로 훑는다 [^27b] |
| A-0 | 📊 스코어보드 (참고 — 판정 잣대 아님) | `pnl_scoreboard_latest.json`은 로컬 계속 생성되나 Supabase 적재는 중단. **내부 페이퍼 추적용일 뿐, 알파로 전략을 판정하지 않는다**(픽 자체가 폐지). 새 1번 지표 = 데이터계약 준수(A-★) + 유지 산출물 정확성 |
| A-1 | 날짜 동기화 + 장날 여부 | 로컬 `Get-Date` + VPS `date` 일치 확인, 전일이 장날인지 |
| A-2 | 전일 cron 완주 | `logs/cron_YYYYMMDD.log` 존재 + `grep '완료 (실패'`로 단계별 실패 건수 |
| A-3 | 헬스체크 등급 | `데이터 건강검진` 블록 — 통과 수(예 17/18)와 실패 항목이 **알려진 항목**인지 대조 |
| A-4 | 종가·수급 기초데이터 | 종가 커버리지 100% / 투자자수급 최신일=전일 / 수급커버리지(#18) 급감 없음. ★**BAT-D 진행 중(≈16:30~19:00)이나 장중에 확인하면 수급 종목수가 과소평가된다**(껍데기 종목이 후반에 뒤늦게 채워짐) — 이때는 **종목수를 무시하고 "실거래값(buy/sell>0) 있는 종목이 다 들어왔는지"만** 본다. 종목수 정본은 BAT-D 완주 후 [^27] |
| A-5 | Supabase 당일 산출물 | FLOWX·SIGNALS·RELAY 당일 데이터 (런타임 정본 = VPS+Supabase, 로컬 data/ 아님) |
| A-6 | **신규 배포분 첫 실동작** | 배포 다음 장날 로그에서 해당 코드 경로 실제 동작 확인 (import OK ≠ 동작 OK) |
| A-7 | 페이퍼 계좌 | 실주문 0 유지, 원장별 NAV/보유종목 급변 여부 |
| A-8 | 백로그 소화 | §3에서 우선순위 최상단 1건 착수 (장중이면 배포 없는 작업만) |

**알려진 실패 항목** (헬스체크에 떠도 신규 이슈 아님): 국적별수급 STALE(KRX 잠금·단타봇 재연결 대기), BAT-D 소요시간 `?분`(임계 재보정 대기).

## 2. 매일 마감 루틴 (자기성찰 — 0+3질문)

0. **오늘 한 일 중 데이터계약 이행·계산 산출물 정확성과 직결된 것은?** — (구: 수익/알파 직결. **2026-07-27 계약 전환으로 목적함수 변경** — 픽·시그널이 폐지돼 "수익"은 더 이상 목적함수가 아니다.) 이행 진척(적재차단·자가검사·생성로직 정리) 또는 유지 산출물의 정확성 개선을 답한다. "없음"이 3일 연속이면 위생만 하고 있다는 경보.
1. **오늘의 오탐/누락은?** — 자동 체크·에이전트·내 판단이 낸 거짓 경보와 놓친 것. 오경보를 낸 체크는 **그 체크 자체를** 백로그에 올린다.
2. **내 판단 중 틀렸거나 뒤집힌 것은?** — 근거와 함께 기록 (예: 7/13 "재시도 실익 낮음" 판단 오류).
3. **내일 개선할 1건은?** — §3 백로그 최상단으로 승격.

답은 §4 당일 엔트리에 쓰고, 세션 메모리(session_state)에도 반영한다.
**등재 원칙**: 에이전트/자동 체크의 발견은 **직접 검증 후에만** 백로그 등재 (7/14 `.env.SAFE_FOR_AI` 오탐 교훈).

## 3. 개선 백로그 (우선순위순 · 각주 = 발견 근거)

| # | 항목 | 상태 | 근거 |
|---|------|------|------|
| **B-53** | **★"값을 안 보는 감시" 계열 전수 점검 — 같은 실패 모드가 5회 반복됐다(8/10)**. 7/31 smart_money(적재 여부만 봄·`price=0` 6주) · 8/6 B-43(존재·커버리지만 봄) · 8/7 B-46(연기금 401일 0) · 8/7 B-47(공매도 4컬럼 5.5개월 0) · 8/10 #18(종목수만 보고 그 종목에 실값이 있는지 안 봄). **개별 픽스로 다섯 번 대응했다는 것은 계열로 다루지 않았다는 뜻**이고, 여섯 번째는 아직 안 드러났을 뿐이다. 조치안: HEALTH 18개 검사를 **"존재/개수만 보는가 vs 값을 보는가"로 전수 분류** → 값 미검사 항목 목록화 → 위험순 교정. 미점검 후보: DART(건수만) · 원자재/FRED(최신값 존재만) · COT · 레짐·BRAIN·SHIELD 산출물 · 업로드 2종. ※종가는 `5196b7c3`로 정체 판정이 이미 추가됨 | 🔴 즉시 | 8/10 |
| **B-27** | **🚨비중단 테이블 속 매매판단성 필드 — 실측 확정, 판단 요청(B-25 확장)**. 계약 이행이 "테이블 차단"에 집중된 사이 **비중단 테이블의 필드**로 매매의견이 매일 나가고 있었다. 7/30 Supabase 실측: ⑴`quant_sector_fire.etf_recommend` = **"ETF 적극 매수: TIGER 인터넷TOP10"·"ETF 관심: KODEX 바이오"** 등 7/29자 5건 실재 + `fire_grade`(B)·`structure_grade`(C)·`composite_grade`(B) ⑵`quant_supply_surge.signal`=**"BUY"**·`quant_supply_chain.signal`=**"BUY"** 리터럴 하드코딩(`flowx_uploader.py:2472·2501·2558`) ⑶**`quant_valuation_gap.grade`="A"** — 유지 5종 중 하나인데 SUSPENDED_FIELDS 미등재(scenario_dashboard의 grade는 제거했으면서 여기는 존치) ⑷`quant_us_macro`(회색지대) `strategy_mode`·`position_limit`·`hold_days_min/max`·`entry_conditions` — `upload_quant_us.py:50`이 자체 create_client로 **guard 원천 밖**, BAT-M_US 08:10 매일. **임의 제거 안 함** — B-25 선례(공개페이지 렌더 확인 전 제거 시 파손) + 7/27 내 분류 오류 2건 재발 방지. 회신 전달 완료 | ⏸️ 판단 | 7/30 검수 |
| **B-28** | **감시 사각 2건 — 자가검사가 못 보는 쓰기 통로**. ⑴**psycopg2+DATABASE_URL 직결 SQL 채널**(`run_morning_briefing.py:91` BAT-B 매일·`snapshot_session.py:437`)은 supabase 클라이언트를 안 써 flowx guard도, 자가검사(SELECT 기반)도 감지 못 함. 현재 쓰는 테이블(quant_bot_advisory·macro_forecast_actual)은 중단 대상이 아니라 위반은 아니지만 **누가 이 채널로 중단 테이블에 쓰면 어떤 방어선에도 안 걸림** ⑵`flowx_uploader` guard 미배선 4메서드(upload_foreign_flow·upload_morning_briefing·upload_sector_rotation·upload_quant_scenarios) — 전부 비중단 테이블이라 현재 위반 없으나, 중단 목록이 늘면 자동 보호 안 됨. → 조치안: 자가검사에 "쓰기 경로 인벤토리 대조"(코드 grep 기반) 추가 | 🟡 순번 | 7/30 검수 |
| **B-29** | **슬러지 정리 — 좀비 스케줄·고아 15개**. ⑴**로컬 윈도우 작업 16건**(QM_A/B/D/D2/E/F/G/H/I/J/K×2/N/O/P1/P2)이 Enabled로 매일 발화하나 등록 경로 인코딩 깨짐(`D:\sub-agent-project_??몃큸\`)으로 즉시 실패. ★위험: 누가 경로를 "고치면" `QM_D_AfterClose → coo_orchestrator → schedule_D_original.bat → scan_nationality.py(KRX 로그인=CD007 계정잠금)`가 부활 → **삭제가 안전**. ⚠️단 `QM_SNAP1~4`·`QM_L_NXTAfter`·`QM_M_NXTPre`는 **지금도 정상 실행 중**(schedule.log 7/30 11:00)이라 보존 + VPS cron과 이중수집 여부 확인 필요 ⑵**고아 스크립트 15개**(저장소 전체 참조 0 확증 — risk_status_notify·calibrate_thresholds·daily_winners·backtest_chart_hero·run_market_scan·chart_hero_morning_monitor·surge_pattern_collector·register_adaptive_queue·auto_backtest_weekly·auto_regression·weekly_chatgpt_review·reconcile_ledger·clean_holiday_rows·explore_kodex_leverage_hedge·explore_leading_cycle_signal) + `coo_orchestrator.py`(연쇄 고아 3종) + `collect_short_selling.py`·`scan_nationality.py`(죽은 bat에서만 호출) ⑶`scripts/schedule_*.bat` 19개(SNAP/L/M 3개 제외) | 🟢 저우선 | 7/30 검수 |
| **B-30** | **코드 이중정의 — 한쪽만 고쳐질 구조**. ⑴★`get_kospi_regime` **동명 4곳**이 서로 **다른 레짐 분류체계**(`kospi_regime_calc.py:34` BULL/CAUTION/BEAR/CRISIS+MA20/60 vs `market_regime_guard.py:58` STRONG_BULL/NEUTRAL/CAUTION/BEARISH+MA5 vs `scan_volume_spike.py:251` vs `scan_buy_candidates.py:837`) — import 한 줄로 의미가 바뀜 ⑵텔레그램 유틸 복붙(`send_telegram` 18파일·`_send_telegram` 5·`format_telegram_message` 6) — 7/20 TOKEN 오타 4일 무음 사고와 동류 ⑶`build_name_map` 10파일·`get_flow_from_csv` 4파일. ★SUSPENDED_TABLES는 단일 정본 확인(양호) | 🟢 상시 | 7/30 검수 |
| **B-31** | **눌림목 엔진 텔레그램 이중 사망 — 판단 필요**. `scan_surge_pullback.py:60·63`의 1차 `src.adapters.telegram_adapter`(**파일 없음**)·2차 `flowx_uploader._send_telegram`(**모듈레벨 함수 없음**) 둘 다 죽어 매일 `"텔레그램 어댑터 없음 — 알림 스킵"`(7/29 18:11:13 로그 실증). ★단순 픽스 아님: ⑴BAT-D는 `TELEGRAM_QUIET=1`이고 허용태그에 `[눌림목 엔진]`이 없어 import를 고쳐도 발송 안 됨 = **이중 차단** ⑵메시지 본문이 **"★ 매수 시그널 ★"+entry_price**라 계약 전환 방향과 충돌. → 되살릴지/제거할지 운영자 판단. 참고: `scan_consensus.py:509` `send_photo` 부재는 **호출처 0 = 죽은 코드**(에이전트가 "매일 실패"로 심각도 과대 판정 → 내가 정정) | ⏸️ 판단 | 7/30 검수 |
| **B-26** | **★유지 테이블 값 정합성 검사**. **①완료(7/31 `22797ca`·`7b9b427`)**: 자가검사에 §2.5 신설 — 유지 5종 당일 수치 컬럼이 **통째로 0/null**이면 `[HEALTH] 🟡` 알람. ★공동적재 테이블은 **퀀트봇 적재분만** 걸러서 본다(60행 중 퀀트봇 50행만 0이고 타봇 10행은 실값이라 전체로 보면 "전량 0"이 성립 안 해 미탐). 이미 cron 편입 도구라 추가 배선 없이 매일 자동. 7/30 실물 역시험: smart_money 42행 price·change_pct·exec_strength 전량 0 **탐지 성공**, 오탐 0. 배포 직후 "미검사를 ✅로 찍던" 표기 결함 자체 발견·교정(`7b9b427`, F4와 동류). **③오탐 억제 완료(8/1 `ce8f0a2`)**: 첫 실행 경고 3건이 **전부 오탐**이었다(`overheat_penalty`=감산 전용 지표·`market_kospi_stoch_k`=범위 지표 바닥·`exec_strength`=우리 페이로드에 없는 정보봇 컬럼). `const_sanity` 신설 — **이력(우리 페이로드)+사람 판정** 두 축. ★★억제 근거를 "과거에 실값 있었음"으로 잡으면 **잘 돌던 컬럼이 0으로 고장나는 결함이 영구 침묵**한다(검증에서 자체 발견) → **"이미 알려진 지속 상태인가"**로 교체: 급변은 언제나 경고, 연속 0은 5회마다 재알림. 억제분은 리포트에 사유와 함께 표기. 35/35 PASS | ✅ ①②③ 완료 | 7/30·[^31][^32] |
| **B-36** | **★엔진 품질 — `tomorrow_picks`가 기저선 미달(8/1 실측)**. B-35로 전방평가+동일규칙 기저선이 서자 처음 보인 것. 표본 100+ 엔진 중 **하회 7개**: `whale_detect` **-4.3%p**(1,134건)·`동반매수` -3.9%p(728)·**`tomorrow_picks` -2.7%p**(934)·`pullback_scan` -2.1%p(675)·`AI섹터` -0.8%p. ★`tomorrow_picks`는 **메인 픽 엔진**이고 표본도 충분해 우연으로 보기 어렵다. 전체가 +2.4%p인 건 표본 큰 `기술촉매`(15,085)·`accumulation_tracker`(9,457)가 끌어올린 결과. **계약 정합성**: 이건 "수익을 위한 종목선택 개선"(B-18, 보류)이 아니라 **산출물 품질 진단**이라 계약 방향과 맞다 — 우리가 내보내는 계산 결과가 시장 기저선보다 나쁘다는 사실 자체가 정확성 문제. **선행 조건(코덱스 8/1 확정 — 이 셋을 통과해야 엔진 품질 판단으로 넘어간다)**: ⑴**날짜 단위 클러스터 검정** — 같은 날 신호는 서로 상관돼 명목 표본(934건 등)이 유효표본이 아니다. 종목 단위 t검정 금지, **날짜를 블록으로** 묶어 재표본화 ⑵**국면별 안정성** — 46일이 폭락장 편중(7/28 -10.84·7/29 -5.98·7/31 **+17.91**). 구간을 쪼개 부호가 유지되는지 ⑶**거래비용 포함 `avg_ret_excess`** — 수수료·슬리피지 차감 후에도 초과분이 남는지. ★표본·커버리지는 3차로 확정됨(flat 0.89%·invalid 0.50%, 사유 `bad_entry_price` 124·`insufficient_bars` 100). ★실측 손익비 첫 확보: 22엔진 중 1 초과는 **인텔리전스 1.14·기술촉매 1.04**뿐, 나머지 0.76~0.97(AI두뇌 0.79·US모멘텀 0.82). 켈리 양수 조건 `승률 > 1/(1+b)` 기준 **전 엔진 미달**(기술촉매 필요 49.0% vs 실제 44.1%) = 기대값 음수. 구 하드코딩 `b=1.5`가 얼마나 낙관적이었는지 드러남. **결론 내기 전 금지**: 이 수치만으로 엔진을 끄거나 가중치를 내리지 말 것 — `source_weight_learner`가 `avg_ret<=0`으로 전 엔진을 끄려 한 것과 같은 종류의 성급함이 된다 | 🟡 순번 | [^35] |
| **B-35** | **★★성과 승률이 순환 논리로 만들어지고 있다 + 생산자 충돌(8/1 발견)**. 켈리 검수 중 전제를 파다가 나왔다. ⑴**생산자 충돌**: `data/market_learning/signal_accuracy.json` **한 파일의 같은 `signals` 키**를 둘이 쓴다 — `signal_performance_refresh.py`(cron 16:50, **D+3 종가·다음날 시가 진입·60일**, 엔진 **22개**) → `daily_market_learner.py`(BAT-D **18:53**, **당일 수익률·20일**, 엔진 **10개**)가 **통째로 덮어씀**. 파일 실측 `window_days:20`·mtime `18:53:30`. **양쪽 로그 다 성공이라 찍힌다**(7/31 price=0과 같은 구조, 이번엔 로컬 JSON). ★**올바른 전방평가가 후방평가에 매일 파괴된다** — `signal_performance_refresh`는 "가짜 진입 방지, 백테스트 동일 기준" 주석까지 달고 제대로 하는데 그 산출이 안 남는다 ⑵**살아남은 값이 순환 논리**: `tomorrow_picks.json`(**18:43** 생성 = "내일" 살 종목)을 **18:53**에 `ret_1d = 당일종가/전일종가`로 채점. **신호가 만들어지기 전에 확정된 수익률로 그 신호를 평가**한다 ⑶**그 값은 시장 베타**: 7/31 KOSPI **+17.91%** 폭등일이라 **전체 1,182종목 중 89.7%가 상승**(평균 +8.66%). 신호 167종목 93.4%·+9.88% = **기저선 대비 +3.7%p뿐**인데 저장은 **92.8% 절대값**. 켈리가 이 `p`를 그대로 쓰면 **시장이 좋았던 날을 신호 능력으로 오인**한다(7/24 αEW 노출도 편향과 동형 — 그때 내린 "기저선 대비로 봐야" 결론이 여기선 안 지켜짐). **→ B-34(켈리) 재제출 조건의 0번**: 이게 안 되면 코덱스 조건 1~4를 다 지켜도 켈리는 정의가 섞인 숫자 위에서 돈다. **①②조치 완료(8/1 `6444a45`·`4457186`)**: ⑴생산자 분리 — `daily_market_learner`를 `signal_accuracy_daily.json`으로 떼고 `signal_accuracy.json`을 **전방평가 정본**으로. 엔진명 체계가 `SOURCE_MAP`↔`signals_to_track`에서 이미 일치해 **소비자 6곳은 코드 변경 없이** 더 정확한 값을 받는다 ⑵**기저선 대비 승률** — 신호일마다 유니버스 기저선을 **완전히 같은 규칙**(next_open→D+3_close)으로 계산해 `hit_rate_excess`·`avg_ret_excess` 추가(기존 필드 유지). ★**★★분리 직후 실측 = 진짜 성적표**(46일·44,653건, 그동안 매일 덮어써져 아무도 못 본 값): **전체 승률 40.7%·전 엔진 평균수익 음수**(기술촉매 43.8%/-0.60%·accumulation 39.5%/-1.47%·dual_buying 40.3%/-1.42%·**tomorrow_picks 35.7%/-2.31%**·whale_detect 34.7%/-3.01%). **후방평가 92.8% → 전방평가 35.7%**(같은 엔진, 거의 3배 차). 섀도 재실행 시 **전 엔진 켈리 배수 0.000**(승률이 전부 50% 미만이라 켈리가 음수→floor) = **재료를 바꾸니 같은 공식이 정반대 답**. ⚠️단 이 46일이 폭락장(7/28 -10.84·7/29 -5.98)이라 **신호 탓인지 시장 탓인지는 ②의 기저선 결과를 봐야 확정**. **③완료(8/1 `925a490`)**: win/loss/**flat**(정확히 0)/**invalid**(조회실패·봉부족, 사유별 카운트) 4분. `p`·`b`는 엄격한 승패표본에서만(`hit_rate_strict`·`win_loss_n`·`odds_b` 신설), 손익비 없으면 **1:1 가정 대신 null**. ★검산에서 내 설명의 절반이 틀린 걸 확인: flat을 패배에서 빼면 b는 정확해지지만(4.00→1.00) **분모가 줄어 승률이 올라가**(0.600→0.857) 켈리가 오히려 커진다(0.250→0.357) — "b 과대=위험방향"은 한쪽만 본 것. **★★기저선 실측 = 이 국면 해석의 답**(37일·43,177건): 시장 기저선 **승률 38.3%·평균 -1.82%**. 우리 신호 40.7% = **+2.4%p 상회** → **"신호가 나쁘다"가 아니라 그 46일이 나빴다**. 단 엔진별로 갈린다(표본100+ 중 상회 13·하회 7) — **whale_detect -4.3%p·동반매수 -3.9%p·tomorrow_picks -2.7%p**(메인 픽 엔진이 기저선 미달). **★코덱스 8/1 후속 판정: `hit_rate_excess`는 롱온리 켈리의 `p`가 아니다**(현금기준 승률 vs 시장대비 승률, 변환식 없음 — "늘 -1%인 종목 vs 늘 -2%인 시장"이면 초과승률 100%지만 계좌는 손실). → 섀도에서 켈리 표기 제거·`selection_edge_proxy`로 개명(`30d531a`). 허용: 종목선택력·αADJ·진단 / 금지: 주문수량·성과감산. ⚠️`source_weight_learner`(cron 16:55)는 `avg_ret<=0`이면 엔진 비활성 → 전방평가 전환으로 전 엔진이 꺼질 수 있다는 코덱스 경고는 **이론상 타당하나 실피해 0**(산출물 `source_weights.json`을 읽는 곳이 전수조사 결과 **없음** = 고아). 기저선 필드로 대체하지 않고 존치(코덱스 권고). ★**이 파일을 7곳이 읽는다**(dashboard_data·build_killer_picks·source_weight_learner·market_journal·run_v3_brain·flowx_uploader·data_integrity) — 영향 범위 확인이 수정보다 먼저 | 🔴 즉시 | [^35] |
| **B-34** | **★켈리 사이징(코덱스 작업분) — 검수 완료, 회신 대기·미커밋**. 코덱스가 7파일에 Fractional Kelly 기반을 넣었다(b=1.5 하드코딩 → `avg_win/avg_loss` 실측). 방향(기존 게이트 상한 안에서 성과 좋은 신호만 덜 감산)은 타당하나 **켈리 타입에 3층 쟁점**: ⑴**공식** — `(pb−q)/b`는 승/패 이항 베팅용인데 주식은 연속분포(이론상 μ/σ²). 승·패를 평균 2개로 요약하면 **분산·좌측꼬리 소실**, CRISIS 국면에 불리 ⑵**★★의미론** — 켈리 `f*`는 "자본 중 몇 %"인데 코드는 **기존 ATR 수량에 곱하는 배수**로 쓴다(`multiplier *= kelly_mult`). 결과는 사장님 요구(상한 유지·덜 감산)와 부합하고 안전하나 **켈리 공식이 아니라 켈리 값의 전용**이라 이론적 대응이 없다 → 이름 변경 또는 진짜 켈리 구조(`f*×자본`과 ATR 수량의 min) 중 택일 필요 ⑶**손익비 `b` 편향 2건(실증)**: 무변동 0%를 패배로 세며 손실은 0으로 더해 **b 4배 과대**(kelly 0.200→0.500, 위험 방향) / 누적 avg_win 분모불일치로 **반대 방향 과소** → **상쇄인지 증폭인지 불명 = b 신뢰 불가**. ⑷단위를 값 크기로 추측(`hit_rate>1`) — `1.0`(1%)을 **100%로** 읽고 인접값 `1.5`는 1.5%로 읽어 판정 66배 차이(생산자 2곳 모두 백분율 확정인데 추측). ★**지금 켜면 켈리가 계산조차 안 된다** — `signal_accuracy.json`이 `signals:{}`·`cumulative` 키 부재라 전 신호가 `kelly_default 0.5` → 실측 **66주→33주 전 종목 반토막**(성과 무관 일괄 감산이라 이후 A/B에서 켈리 효과와 분리 불가). **사장님 결정: 페이퍼 한정**(실계좌는 B-16 관문 후). 질의서 Q1~Q8 발송 → **코덱스 회신 도착: 커밋 보류 판정 + 재제출 조건 5개**(지적 전부 수용, Q2는 `performance_haircut` 개명 권고). ★내 실증 정정: "signals:{} 라 데이터 없음"은 **로컬 사본 오판** — VPS엔 엔진 10개·tomorrow_picks 167건으로 표본은 이미 충분(코덱스 요구 `win+loss>=100`도 통과). 그러나 그 값이 못 쓸 값이라 **B-35를 조건 0번으로 추가 요청**. **🔄섀도 관측 시작(`0b103ec`·`39b619b`, 사장님 "켜서 돌려보면서 진행" 지시)**: 매매 경로 무접촉 관측 스크립트 `kelly_shadow.py` 신설 + BAT-D 종반 cron 편입(8/3부터 매일). 코덱스 코드는 계속 미커밋이라 import하지 않고 산식만 자체 구현(단위는 백분율 **계약 고정**, 추측 안 함). ★첫 실행 실측 — **절대 승률 배수 vs 시장 보정 배수**: tomorrow_picks 0.428→0.031(**13.7배**)·dart_event 0.411→0.014(**28.9배**)·dual_buying 0.385→**0.000**·accumulation_tracker 0.167→**0.000**. **B-35의 크기가 숫자로 나왔다.** 단 기저선이 7/31(KOSPI +17.91% 극단일) 하루치인데 승률은 20일 누적이라 시점 불일치 — 보정값은 과대 감산 가능, 매일 쌓이면 완화. 손익비는 전부 미실측(1:1 가정, 코덱스 코드 미배포). **커밋은 여전히 안 함**(코덱스 협업 규칙 = 검수 우선·단독 결정 금지). ★판정지표로 **αADJ 필수** 제안 — 켈리는 정의상 노출도를 줄이므로 수익률·MDD만 보면 **항상 이기는 것처럼 보인다**(7/24 노출도 편향 교훈) | 🔄 섀도관측 | [^34] |
| **B-39** | **★DB→parquet 수급 전달이 끊겼다 — `기타법인` 3.5개월 0(8/5 VPS 실측 → 8/6 원인 확정·수정 배포 `f5e61d5c`)**. 원인 후보가 사실로 확정: KIS 종목별 API(FHKST01010900)가 **외/기/개만 반환하고 기타법인이 없어** `if col in inv_df.columns`가 조용히 건너뜀 + **외/기/개가 채워진 행은 zero_mask(4컬럼 합=0) 재충전에서 영구 제외** = 이중 미충전. 타임라인이 커밋으로 잡힘 — **기타법인 컬럼은 4/15 추가(`c5113224`)되고 4/18 벌크 전환(`9cd5b7ed`)으로 이틀 만에 사망**. 수정: `investor_daily.db`(11세분) `net_val`(순매수 대금·원)로 충전 — 일일 증분 14일 창(수집기 17:42 종료 > 본 스크립트 16:57이라 당일분 D+1) + `--backfill-etc-corp` 전구간 복구(DB 커버리지 2025-04-21~). ★단위 검산 선행: 4/10~16 겹침 구간 DB=parquet **소수점 일치**(005930·000660, 음수 포함) — 4월 삼전 일 3~4천억은 자사주 매입기 실값. 0/NaN만 채워 pykrx 시절 실값 불변. 테스트 5케이스+로컬 백필 실동작(3파일 518칸). **✅8/7 완결**: ⑴**증분 첫 실동작 성공** — 8/6 커밋(17:06)이 그날 BAT-D(16:35)보다 늦어 8/7이 첫 실행. parquet 기타법인 실값 구간이 **7/24~8/6**에 정확히 생겼고 `8/7−14일=7/24`로 창 경계와 일치(8/6 실행이었다면 7/23이어야 함). 8/7 당일분 0은 설계된 D+1 충전 → 8/10 채워짐. ★내 1차 판정은 "8/7 전량 0 = 수정 실패"였는데 시계열을 보니 **정반대(작동 증거)**였다 — 단일 날짜 스냅샷으로 판정하면 안 된다는 것을 또 확인 ⑵**전구간 백필 실행**(8/7 19:29, 조용한 창) — `raw` **1,209/1,218파일 265,456칸**, 실패 0, 68초 ⑶★**백필만으로는 소비처에 안 닿는다** — 백필 대상은 `data/raw`(`extend_parquet_data.py:586`)인데 내가 검증한 곳과 실제 소비처는 `data/processed`였다. 두 디렉토리는 `rebuild_indicators.py`(BAT-D 17:43~17:56 **전량 재계산**)가 잇는다. 백필 직후 processed는 그대로여서 하마터면 "완료"로 찍을 뻔했다(0/NaN만 채우는 설계라 로그는 정상 성공). → 주말 백테스트(토 09:00·일 17:00)가 0인 상태로 돌지 않도록 **8/7 밤 지표 전량 재계산 수동 실행**해 전파 | ✅ 완료 | 8/5 검수·8/6·8/7 |
| **B-40** | **★계약 위반 신규 6종 실물 확정 + 자가검사가 40개 중 26개만 본다(8/5)**. Supabase **8/4 실적재분**으로 확정한 것(코드 경로가 아니라 실물): ⑴**`quant_supply_chain.signal="BUY"`** — B-27에 없던 **신규**, 생성 `flowx_uploader.py:2714` 하드코딩 ⑵`quant_supply_surge.signal="BUY"` ⑶**`quant_sector_fire.etf_recommend="ETF 관심: KODEX 철강"`** + `fire_grade`/`structure_grade`/`composite_grade` ⑷`quant_valuation_gap.grade="A"/"D"/"B"`(KEEP 5종인데 SUSPENDED_FIELDS 미등재) ⑸**`quant_bluechip_checkup`의 `target_price`·`grade`·`comment`** — `scan_bluechip_checkup.py:968`이 `uploader.client.table()` 직접 호출로 **guard 원천 밖**, `target_price`는 SUSPENDED_FIELDS에 명시된 금지 필드명인데 scenario_dashboard에서만 지워졌다 ⑹**`quant_market_brain.data.briefing`** — `"자본 배분: 개별스윙 0.0% / 금ETF 20.0% / 현금 57.0%"`, `"v3 캡: CAUTION → 신규진입 금지"` 등 **자연어 매매 지시문**이 통째로. **★감시 범위**: 자가검사가 보는 것은 SUSPENDED 18+KEEP 5+GRAY 3 = **26개**인데 실제 사용 테이블은 **40개** — 위 ⑴⑵⑶⑹이 전부 무감시 목록에 있다. 필드 단위 검사는 `quant_scenario_dashboard` 하나에만 존재. **임의 제거 금지**(B-25 선례·7/27 분류 오류 2건) — 웹봇 렌더 확인 후 판단 | ⏸️ 판단 | 8/5 검수 |
| **B-41** | **검수 미검증 발견 묶음 — 재검증 후 조치(8/5)**. 4팀+하위 3팀이 40건 이상 냈으나 시간상 상위만 재검증했다. **미검증 상태로 등재**하며 착수 전 반드시 직접 확인할 것(7/14·7/30·8/5 에이전트 오탐 선례 3회): ⑴**`run_bat.sh`에 휴장일 가드 전무** — Windows BAT엔 `should_run_bat('kr')`가 있는데 리눅스 정본에 미이식. 평일 공휴일(추석·10/3·12/25 등)에 전 파이프라인 실행 → 유령 행 적재·수익률 이중계상 우려 ⑵**`run_adaptive_cycle.py:118` `is_market_safe_for_buy`가 없는 함수** → 매수 안전게이트가 항상 통과(`market_regime_guard`에 그 이름 없음) ⑶**`price_change` 단위 혼재**(`extend_parquet_data.py:276` pykrx=%, `:298` FDR=소수) → `scan_whale_detect`의 방향 판정이 전부 "횡보폭발", 하한가 종목이 세력포착 1위 ⑷**`Foreign_Net`(억원)을 주식수로 오인**(`scan_pullback.py:142`) → 눌림목 수급 25점이 67.5% 무력화. ★⑶⑷는 **B-36의 `tomorrow_picks` 기저선 미달과 직결될 수 있다** ⑸`scan_supply_surge`의 `ELSE 0`이 "데이터 없음"을 "순매수 0"으로 둔갑 ⑹JSON 원자성 없는 쓰기 다수(`os.replace` 미사용) ⑺`MacroAggregator` 32소스 신선도 무가드 → LLM 프롬프트에 낡은 값 ⑻고아 스크립트 약 55건·`main.py:552`의 archive import. ★**정정 1건**: `STOP_LOSS_PCT` 5중 정의가 "실주문 경로에서 조용히 오작동(🔴)"이라는 보고는 **오탐** — 그 상수를 import하는 곳이 0건이라 교차 불가, 🟢 위생 항목 | 🟡 순번 | 8/5 검수 |
| **B-42** | **★미배포 감시 — 배포가 세션 유무에 의존하는데 그 실패를 아무도 못 본다(8/6 실물)**. 8/5 저녁 세션이 없어서 **8/5 낮에 푸시된 5커밋이 VPS에 안 들어간 채** 8/5 저녁 BAT-D 전체 + 8/6 BAT-D 초반(16:30~16:44)이 **8/1 코드로 실행** — /scenario 거짓 0이 하루 더 나갔고, 세션이 열리기 전까지 어떤 감시도 이를 몰랐다(17:45 `deploy_pull.sh`는 단타봇용, quantum-master 미포함 — 7/16 "자동배포 없음" 정정과 일치·이번에 실측 재확인). 파이프라인 전체에서 **유일하게 사람 세션에 의존하는 단계가 배포**인데 감시 사각. 조치안: `data_health_check`(이미 cron 편입)에 "코드 신선도" 항목 — `git fetch --quiet` 후 `rev-list --count HEAD..origin/main` ≥1이면 `[HEALTH] 🟡 미배포 N커밋(M일째)`. B-23①-b("세션 없는 날 감시 공백 제거")와 같은 원리. 자동 pull은 하지 않음(배포 타이밍 규칙은 사람이 지킴 — 감시만) | 🔴 즉시 | 8/6[^36] |
| **B-43** | **parquet 수급 값 검사 — B-39 재발 방지 일반화(8/6)**. HEALTH "수급(종목)" 항목이 **존재·커버리지만 보고 값을 안 봐서** 기타법인 전량 0이 3.5개월 무탐지였다(8/5 내 "수급 11주체 전부 실값" 판정도 DB만 보고 parquet을 안 본 사각). 조치안: 종가 정체 검사(5196b7c3)와 같은 구조로 **수급 4컬럼별 최근 5거래일 전종목 0-비율 검사** — 임계(예: 95%) 초과 시 `[HEALTH] 🟡 {컬럼} 전량 0 의심`. 정상적으로 0이 많은 컬럼(소형주 기타법인 등)이 있어 임계는 4/16 이전 실측 분포로 캘리브레이션 후 확정 | 🟡 순번 | 8/6[^36] |
| **B-45** | **FLOWX 마스터 지시서(260805) §8 데이터 기준 대조 — 퀀트봇 해당분 현황표(8/6 접수)**. 상위 기준 문서라 즉시 작업은 아니나 **구독 출시 전 필수 기준 5개가 퀀트봇 산출물에 걸린다**: ⑴수집 성공률 99.5%+ — 종가는 충족(99.5%), 수급·기타 배치는 측정 정의부터 필요 ⑵**데이터별 기준일·출처·단위 표시** — B-33(source 컬럼)과 직결, 페이로드에 asof/source/단위 명시 확장 ⑶결측·이상값 감시+운영자 알림 — B-26/B-42/B-43 계열로 진행 중 ⑷국내 3~5년 백필 — OHLCV parquet ~8년 보유, 수급 11세분은 **2025-04-21~**(1년 4개월, 3년 미달) ⑸지연·장애 상태 공개 — HEALTH는 내부용, 공개 채널 없음. §1 "결측값을 0으로 채우지 않는다"는 B-39(완료)·price=0(7/31 완료)이 실물 사례 | 🟡 순번 | 8/6[^36] |
| **B-46** | **★연기금이 parquet에 한 번도 실린 적 없다 — `pension_net_5d` 401일 전구간 0(8/7 발견)**. B-39를 검증하다 옆 컬럼에서 나온 것. 표본 101파일 전수에서 **2024-12-13~2026-08-07 401일 내내 0**, 실값 구간이 아예 없다 = 고장이 아니라 **처음부터 미배선**. 사슬: `extend_parquet_data.py:247` `supply_cols=["기관합계","외국인합계","개인","기타법인"]`에 **연기금이 없어** raw에 `pension_net` 컬럼이 안 생기고 → `indicators.py:768` `if "pension_net" in result.columns`가 **항상 False** → `pension_net_5d=0`, `pension_top_buyer=0`(:777). 원 생산자 `pykrx_supply_adapter.py:38`(연기금등→pension_net)은 pykrx 시절 경로라 지금 parquet과 무관. 소비: **`v8_scorers.py:540-544` 연기금 보너스(top_buyer 0.05 / net_5d>0 0.03)가 영구 미발동** — 사용처 `unified_scorer`·`regime_weighted_scorer`·`situation_reporter`·`v8_pipeline`. **★영향 범위 정정(과장 금지)**: 3봇 분업의 "퀀트봇=금투+연기금" **주 신호는 무사하다** — `future_value_engine.py:152-165`·`signal_engine.py:226`이 `investor_daily.db`를 **직접 조회**해 금투·연기금 5D를 쓴다(정상 작동 확인). 죽은 것은 **parquet 경유 v8 스코어러 가점 한정**. 복구 난도 낮음 — DB에 연기금 `net_val`이 2025-04-21~ 있어 B-39와 **동일 패턴**(supply_cols 확장 + DB 충전 + 백필)으로 처리 가능. 단 DB 시작일 이전은 복구 불가라 `0`이 아닌 **null**로 둬야 함(B-45 §1 원칙) | 🟡 순번 | 8/7 |
| **B-47** | **★공매도·대차 4컬럼 5.5개월 전량 0 — 소스 중단 후 대체 미배선(8/7 발견)**. `short_volume`·`short_ratio`·`short_balance`·`lending_balance`가 **2026-02-19부터 8/7까지 전량 0**(표본 148파일, 컬럼 보유는 176~671파일). 원인은 코드 주석에 이미 적혀 있었다 — `run_bat.sh` "collect_short_selling.py 제거 — KRX 공매도 데이터 제공 중단(2026-04)". **소스가 죽었는데 컬럼은 0으로 남아 계속 계산에 들어간 것**이 문제(B-45 §1 "결측을 0으로 채우지 않는다" 위반, B-39와 동형). ⚠️주석은 "2026-04"인데 실제 0 시작은 **2/19** — 중단 시점이 기록과 다르니 확인 필요. 소비: `v8_scorers` short_selling 점수(0.02) + parquet 파생 `short_spike`·`short_cover_signal`·`short_ratio_ma40`·`short_balance_chg_5d`가 전부 무효값 위에서 계산 중. **대체 소스 있음** — 3봇 분업상 공매도·신용·대차는 **정보봇 담당**이고 `data/short_selling/jgis_short_factor.json`이 **매일 정상 도착**(8/7 18:27, 486KB, `total_scanned`·`factor_summary` 포함). → B-39 방식으로 JSON→parquet 배선하면 복구 가능. **함께 확인된 0 컬럼 2종**: `fund_PER`·`fund_PBR`(30파일 보유·전량 0, 기존 미결 "fund_PER 백필"과 동일 건)·`earnings_surprise`(89파일·전량 0) | 🟡 순번 | 8/7 |
| **B-48** | **수급 수집 이력이 안 남아 "오늘 다 받았나"를 사후 판정할 수 없다(8/7)**. `investor_daily.db`의 `collect_log` 마지막 기록이 **2026-06-01**에 멈춰 있다 — 그 테이블에 쓰는 코드는 폐기된 `krx_nationality_collector.py:525`뿐이고, 현행 수집기(KIS 11세분 `FHPTJ04160001`)는 **아무것도 기록하지 않는다**. 실제로 막혔다: 8/7 수급이 **2,623종목**인데 직전 10영업일이 전부 **2,827**로 204 부족 → 알려진 D+1 채움 패턴(7/30 확정 "종목수 정본=D+1")인지 당일 수집 실패인지 **판정 근거가 없었다**. cron 로그엔 진행이 찍히지만 30일 후 삭제되고(`0 4 * * * find ... -mtime +30 -delete`) 종목수 시계열로 볼 수 없다. 조치안: KIS 수집기 종료 시 `collect_log`에 1행(date·collected_at·investors·total_rows·status·elapsed_sec) 기록 — **스키마가 이미 있어** INSERT 한 줄이면 되고, 이후 "전일 대비 종목수 급감"을 HEALTH가 자동 판정할 수 있다(B-43과 짝). ⚠️8/10(월)에 8/7분이 2,827로 채워지는지 확인하면 D+1 패턴 자체는 확정된다 | 🟡 순번 | 8/7 |
| **B-49** | **★★자동 검수의 절반이 두 달간 꺼져 있었다 — 277건 전부 EXIT 1(8/9 발견·✅수정)**. `ops/codex_inbox` 검수요청 **277건 전수**에서 `self_review_checklist`가 **EXIT 1**, `PASS`는 **0건**(6/10 09:35 ~ 8/9 00:46). 즉 "코드베이스 전체 영구 정합성 8룰"(5/27 사고 4회 패턴 학습분 — 보호종목↔universe 모순·주문 우회 테스트 존재 등)이 **한 번도 실행된 적이 없다**. 원인은 한 줄 — `tools/codex_auto_request.py:166-170`이 `[sys.executable, "tools/..."]`로만 호출해 **`-X utf8`이 빠졌고**, Windows 기본 cp949 stdout이 스크립트 첫 줄의 em-dash(`—`)에서 `UnicodeEncodeError`로 죽었다. **CLAUDE.md "한글 출력 시 `python -u -X utf8`" 규칙이 정확히 이 호출부에만 빠져 있었다.** 조치: 3개 체크 전부 `-X utf8` 부여(`codex_lite`·`self_review_checklist`·`quant_preflight`) → 실행 검증 **3/3 exit=0, "8개 영구 정합성 룰 모두 통과"**. ★불행 중 다행: 두 달 만에 처음 돌렸는데 **8룰 전부 통과**라 그 사이 실제 위반은 없었다. ★★교훈: **검수 도구의 실패를 아무도 검수하지 않았다** — 277번 같은 문자열이 로그에 남았는데 두 달간 아무도 안 읽었고, critical 변경(`c31db350`)조차 이 체크를 건너뛴 채 통과했다. 감시 도구는 자기 자신의 실패 모드로 검증해야 한다(7/30 교훈의 재현) | ✅ 수정완료 | 8/9 검수 |
| **B-50** | **★전방 페이퍼 원장이 지시서 필수 필드를 아직 못 채운다 — 성과로 쓰면 안 된다(8/9)**. 8/8 소유자 지시서(`docs/02-design/flowx-forward-paper-control-2026-08-08.md`) 대비 실측(드라이 실행 1,263이벤트 전수): ⑴**§4-3 비용 차감 미구현** — `fee` 비영 **7/1263**, `tax`·`slippage_cost`·`spread_cost` **전부 0** ⑵**§3 `net_pnl`·`net_return` 0/1263** ⑶**§4-1·2 `signal_at`·`eligible_at` 0/1263** → "신호는 진입 가능 시각 전 확정"·"같은 봉 유리가격 체결 금지"를 **검증할 근거 자체가 없다** ⑷**§3 이벤트 순서 중 `SIGNAL_FROZEN`·`ORDER_INTENT`·`REJECT` 0건**(FILL 196·MARK 904·EXIT 163만). ★**설계자가 한계를 인지하고 표시해 뒀다** — `metadata.cost_complete=false` + `"legacy pnl_pct is retained as source value, not certified net return"`. 즉 결함이 아니라 **legacy 미러 1단계**다. **그러나 위험은 실재한다**: 이 플래그가 `metadata` **안쪽**에 있어 소비자(웹봇·FLOWX 화면)가 **무시하고 집계하기 쉽고**, 그러면 비용 미차감 수익률이 §6("실현수익으로 오해할 표현 금지")을 위반한 채 나간다. 조치안: ⑴소비 계약에 "`cost_complete=false` 행은 순성과 집계 제외" 명문화 + 웹봇 통보 ⑵비용·`signal_at` 채우는 2단계 일정 확정 ⑶`real_order=true` 0건은 확인됨(1,263/1,263) | 🔴 즉시 | 8/9 검수 |
| **B-51** | **원장 append가 건마다 전체를 재검증한다 — 지금은 괜찮고 쌓이면 막힌다(8/9)**. `ForwardPaperLedger.append()`가 매 호출마다 `self.load(verify=True)`로 **원장 전체를 읽고 전 행 해시를 재계산**한다(`forward_paper_ledger.py:68`). 게다가 `mirror_profile()`이 프로파일 7개마다 또 `ledger.load(verify=True)`를 부른다 → **O(N²)**. 실측: 빈 원장에서 1,263건 첫 적재 **63초**(CPU 바운드), 2회차 멱등 실행은 **0.75초**(정상). 즉 **일 증분이 작으면 당장 문제없고, 원장이 커질수록 증분 1건의 비용이 선형 증가**한다 — 원장 1.5MB/1,263행 기준 건당 1.2KB라 5만 행이면 60MB를 매 append마다 파싱하게 된다(VPS RAM 1.9GB·여유 1.2GB). ⚠️**별건으로 stale lock 위험**: `_directory_lock`이 프로세스 사망 시 lock 디렉토리를 남기고 **자동 정리·PID 기록이 없어** 이후 매 실행이 `TimeoutError`(5초)로 실패한다. `run_bat.sh`가 BAT-D 초입에 `pkill -f`를 여러 개 돌리는 환경이라 가상의 위험이 아니다. 조치안: append 배치화(한 번 로드 → N건 append → 한 번 verify) + lock에 PID·mtime 기록 후 stale 자동 해제 | 🟡 순번 | 8/9 검수 |
| **B-52** | **★`use_kelly`를 켜면 매매가 멈춘다 — 코드 어디에도 경고가 없다(8/9)**. `c31db350`이 켈리를 `live_trading` 경로에 배선했다(`use_kelly` 또는 `use_correlation`이 true면 `PositionSizerV2`). 현재 `config/settings.yaml:2287-2288`이 **둘 다 false**라 실동작은 불변이고, `kelly_cap:1.00`이라 **증액은 구조적으로 불가**(지시서 §1 "Kelly를 수량 확대 근거로 쓰지 않는다" 부합) — 여기까지는 안전하다. **문제는 켰을 때다**: VPS `signal_accuracy.json` 실측으로 켈리를 계산하면 **22엔진 중 18개가 음수**(동반매수 -0.14·whale_detect -0.22·dual_buying -0.06·accumulation_tracker -0.10 …), `kelly_floor:0.00` + `multiplier<=0이면 shares=0`(`position_sizer_v2.py:95`)이라 **해당 신호는 전량 진입 차단**된다. 양수는 시나리오 0.015·기술촉매 0.038 둘뿐(사실상 1~4% 축소), 나머지 2개는 표본부족으로 `kelly_default 0.5`. 즉 **플래그 하나로 사실상 매매 정지**인데 설정에도 코드에도 이 경고가 없다. B-34에서 "켜면 전 종목 반토막"이라 했던 것이 전방평가 전환(B-35) 이후 **"전 종목 진입 불가"로 악화**된 것 — 값이 나빠서가 아니라 **정직한 값으로 바뀌어서**다. 조치안: `use_kelly` 주석에 실측 결과와 "켜기 전 섀도 재확인 필수" 명기 + 활성화 시 기동 로그에 켈리 0 엔진 수 경고. ※`hit_rate>1`로 단위를 추측하는 코드(B-34 ⑷지적)가 아직 남아 있으나 **현 데이터는 전부 백분율(41.4·39.5…)이라 미발현** | 🟡 순번 | 8/9 검수 |
| **B-37** | **score 상한 포화 — 상위 40행이 서로 구분 안 됨(8/5 실측)**. 8/1 배포한 상한 100 재배분은 목적(거짓 비교 방지) 달성 — 8/3·8/4 `score>100` 0건. 그러나 분포가 8/3 `100×31/99×9/98×10`, **8/4 `100×40/99×5/98×5`** = 상위 80%가 동점. 원인은 재배분식이 아니라 **원본 공식의 연속일수 보너스가 상한에 포화**(`STRONG90 + dual10 + 연속7~10`)돼 -10 시프트가 그대로 옮겨온 것. 8/1에 "보너스 절반 안"을 기각한 사유(연속일수가 상한에 포화)가 **상한 10에서도 성립**함을 실측으로 확인. ★부수 확인: 정보봇 max 70 vs 퀀트봇 min 98로 **여전히 완전 분리** = 8/1 내 반론("상한만으론 상위 편중 안 풀린다")이 재확인, 해소는 B-33 소관. 변별력 개선은 연속일수 외 축(금액 규모 등) 추가 여부 판단 필요 | 🟡 순번 | [^35b] |
| **B-38** | **`collect_log` 2개월 미갱신 — 수집 이력 감시가 죽어 있다**. `investor_daily.db`의 `collect_log` 마지막 행이 **20260601(6/2 06:43 기록)**. 6/27 KIS 종목별 11세분 전환 후 새 수집기가 이 테이블에 안 쓴다. 데이터 본체(`investor_daily` 4,896,879행)는 매일 정상 적재되므로 **실피해 0**이나, "수집이 언제·몇 행·성공했는가"를 남기던 원장이 조용히 끊긴 채 2개월 방치됐다. B-28(감시 사각)과 동류 — 조치안: 새 수집기에 collect_log 기록 복원 또는 테이블 공식 폐기 후 대체 원장 명시 | 🟢 저우선 | [^35b] |
| **B-33** | **★`source` 컬럼 전환 — 3봇 합의 완료, 컷오버 대기**. `dashboard_smart_money`에 `source` 추가 후 **`(date,ticker,source)` 유니크**로 전환(웹봇 8/1 제안·정보봇 동의·퀀트봇 동의 회신). 같은 `(date,ticker)`를 두 산출물이 공유하는 한 UPSERT 덮어쓰기는 구조적으로 계속되고 가드로는 못 막는다 — **우리가 정보봇 실값 price를 0으로 파괴한 것이 그 증거**. B-32를 `xmin`으로 임시 해소했으나 xmin은 내부 구현(32비트 순환·FREEZE 시 변경)이라 계약으로 삼기 약하고, **"누가 썼는지"는 여전히 시각대 추정에 의존**한다. `source`는 화면 편중과 감시 판정 소스를 동시에 해결한다. 일정은 웹봇·정보봇 주관 → **컷오버 통보 오면 ⑴자가검사 생산자 판별을 `source` 기준으로 교체 ⑵`source` 필드 적재 ⑶라벨 한글화 플래그(`SMART_MONEY_KOREAN_LABELS`) 켜기를 같은 배포에** 낸다. 부수: 웹봇의 계보별 균형 노출도 이때 열림. **★8/1 순서 리스크 수용(`4ff71df`)**: 라벨 한글화가 B-33보다 먼저 나가면 **웹이 두 계보를 구분할 유일한 수단을 잃는다**(웹봇 지적). `created_at`은 B-32로 못 쓰고 `source`는 아직 없어, 남는 실마리가 `signal_type` 영문/한글 차이뿐. **가정이 아니다 — 7/31 price=0을 웹봇이 잡은 근거가 정확히 "DUAL_FLOW 행과 price=0 행이 1:1"이었다.** → 계약은 코드에 두고 적용만 플래그로 잠가 **끊기는 구간 0**. ★**웹봇도 자기 처방을 철회**(`e4246c3`): 재배분 시뮬레이션 결과 상위50이 퀀트49/정보1 → **통일 후 50/0으로 더 나빠짐**, 현재 1칸도 척도 덕이 아니라 `price=null` 1행이 후순위 정렬에 밀린 자리였음. score 상한은 "거짓 비교 방지" 목적으로 유지 요청 | ⏸️ 대기 | [^32][^33] |
| ~~B-32~~ | **완료(8/1 `2e9be51`)**: 판정 소스를 `created_at` → **`xmin`**(마지막으로 쓴 트랜잭션 ID, UPDATE 때 갱신)으로 교체. 단서는 정보봇이 냈고 웹봇 8/1 회신으로 받았다. 실측 — 업로드 로그 50행인데 `created_at` 기준 18:40은 33행뿐(**34% 미탐**), xmin으로는 **50/50 정확**. 7/30 역시험도 42→**49행**으로 정확해져 정탐 유지. 배치 라벨에 `17:05+18:40#356140×50`으로 시각을 병기해 **덮어쓰기 사실이 리포트에 드러나게** 함. 못 받는 환경은 시각 기준 폴백. 항구적 해법은 B-33 | ✅ 완료 | [^32] |
| ~~B-32(원문)~~ | **★★생산자 판별법의 근본 결함 — `created_at`은 "쓴 시각"이 아니다**. 7/31 실측: 퀀트봇이 `quant_sector_flow`를 **18:40:33·18:50:55에 POST 200**으로 올렸는데 Supabase `created_at`은 **4일 내내 16:56 고정**. `on_conflict` upsert는 기존 행을 UPDATE하므로 **created_at이 최초 생성 시각 그대로 남는다**. 즉 **"퀀트봇 시각대 행 0" = "퀀트봇이 안 썼다"가 아니다**. ⚠️**영향 범위가 값 검사에 그치지 않는다** — 중단 18종 위반 판정(§1)도 같은 시각대 논리라, 타 봇이 16:30에 만든 행을 퀀트봇이 18:40에 덮어쓰면 **위반이 "타봇 적재"로 미탐된다**. 7/28 "위반 5건 = 전부 타봇" 결론도 이 케이스를 배제하지 못한다(당시 18:40 레코드 소멸·대조군 0건이라는 별도 증거는 유효하나 upsert 갱신은 못 걸러냄). 지문은 이미 있었다: 7/30 한국가스공사 `created 08:05`(정보봇)인데 내용은 `DUAL_FLOW`(퀀트봇). **조치안: DB 시각 추정 대신 우리 업로드 로그(`퀀트 20테이블 업로드: N/N 성공 {…}`)를 판정 소스로 삼는다** — B-19① "판정 소스는 대상 파이프라인과 독립이어야" 원칙의 재적용. 7/31 1차로 적재 직전 페이로드 자기검사는 넣었다(`ad7e800`) | 🔴 즉시 | [^31b] |
| ~~B-26②~~ | **완료(7/31 `ad7e800`)**: JSON `data`형 3종을 `_record_groups()`로 펼쳐(list[dict]·dict[str,dict]) 같은 상수 판정 적용 → 3일치 검증에서 **유지 5종 전부 검사·미검사 0**. 부수로 **배치 단위 판정 전환**(7/30에 잡힌 건 우연히 18시대에 퀀트봇만 있었기 때문 — 7/29는 정보봇 19:39 193행이 퀀트봇 38행의 전량 0을 덮어 미탐) + **적재 직전 페이로드 자기검사** 신설. 잔여 미탐 원인은 B-32로 분리 | ✅ 완료 | [^31b] |
| **B-23** | **★★FLOWX 데이터계약 이행 (2026-07-27~) — 새 최우선**. 적재차단 `b7ae38b`+`409ee34`. **①완료(7/28 `6c961f7`)**: 자가검사 도구 신설 → **퀀트봇 적재분 중단 18종 전부 0건 확인**(근거: 퀀트봇 업로드 시각대 KST 18:40/18:47 레코드가 7/24·7/27 대비 7/28 전량 소멸). 운영자·웹봇 회신 2건 전달. **①-b 상시화 완료(7/30 `89b0fd7`)**: `--alert` 모드 + BAT-D 종반 cron 편입 — 위반·유지누락 시만 [HEALTH] 텔레그램, 평시 무음, 휴장일 인지. 세션 없는 날 감시 공백 제거. **②완료(7/28 `06ed005`)**: quant_scenario_dashboard 금지필드 5종(entry_price·stop_loss·target_price·scenario_risk_reward·**grade**) 재귀 제거 + SCRUB 자가검사 로그, 45개→0 검증, **7/29 첫 실동작 성공(런타임 SCRUB 로그+Supabase 실물 0건)**. 남은 것: ③생성로직 정리(cron 픽 단계 — 로직 보존 원칙상 마지막) ④경계 4종·유지 3종 내부 연구가치 재평가. **판정 = 계약 준수(수익 아님)** | 🔴 진행 | [^27b][^28] |
| **B-25** | **🚨quant_leader_cycle `signal` 매매지시 — 웹봇 판단 대기**. `data.leaders[].signal` 실측 **"매수적기"7·"청산"28·"보유"2·"경계"10**(90종목) + `summary.by_signal` 집계. `note`에 "관측 전용(shadow)"이라 적혀 있으나 **필드값 자체가 매매 의견**이라 지시서 §1 정면 해당. **임의 제거 안 함** — /leader-cycle 유일 소스라 렌더 여부 확인 전 제거 시 공개페이지 파손. 코드에 후보 주석 등재(`flowx_uploader.SUSPENDED_FIELDS`), 웹봇 회신 즉시 한 줄 주석 해제로 활성화. 사실 필드(delta_value·survival_pct·mdd_from_high·age_months)는 생존하므로 대체 표기 가능 | ⏸️ 판단 | [^28] |
| **B-24** | **검수 후속 미조치분(2026-07-27 서브에이전트 3팀 검수)** — 시급 2건 `409ee34` 조치완료. **②완료(7/28 `35f033b`)**: `_upload_rows` PGRST204 무한재귀 **실재 확인**(bad_col='date'면 재귀 직후 date 재주입 → 구코드 재현 결과 RecursionError) → 3중 가드(필수컬럼 제외·제거0건 중단·depth 5) + 가짜 client 6/6 PASS. **③완료**: `upload_nxt_picks` 제거(호출처 0·본문 return True뿐). **③ 나머지(signals API)는 B-23 ③으로 병합** — 7/28 정밀 집계상 `insert_signal`만 활성(`signal_logger.py:221`, guard로 적재는 차단 중)이고 `update_signal_performance`·`fetch_open_signals`·`fetch_signals_by_period`·`close_signal` 4종은 archive 전용 死. signals 테이블 CRUD라 **생성로직 정리와 함께 처리해야 중복이 없다**(계약상 로직 보존 원칙이라 지금 떼지 않음). **⑤기각(7/28 실측)**: dashboard_sniper는 정보봇이 16:40에 계속 적재 → 차단해도 빈값 안 됨. 잔여: ①**회색지대 3종 운영자 판단**(quant_bluechip_checkup·us_macro·market_ranking — 7/28도 적재 중 실측) ④build_* None/KeyError 엣지·except:pass 다수(상시). **각 항목 착수 전 직접 재검증**(7/14 오탐 원칙) | 🟡 검증후 | 7/27 검수 |
| **B-22** | **★SHIELD 4개월 고착 — 구조적 결함 3건 규명, 의도적 미해제**. ①MDD 고점 2026-03-11 고정(절대 안 내려감) → 매일 -27.66% 재계산 → LEVEL_3 → RED → 3종목 → 회복 불가 → 무한루프 ②해제 조건 `KOSPI 3일≥+3% AND VIX≤-10%`가 3년 중 10일(1.29%)만 충족하고 **VIX 조건은 성과를 개선하지 않음**(D+20 ①단독 +2.48%p p=.0495 vs ①AND② +0.21%p p=.96) ③해제가 **하루짜리**(고점 리셋 없어 다음날 재발동, shield.py:288). 7/23 `KOSPI +8.9% OK / VIX -2.4% 미달`로 전환 감지하고도 차단. **⏸️미해제 결정 근거: αADJ -7.3%p라 지금 풀면 손실 증폭.** 종목 선택이 양수 전환 후 착수. ORANGE 키 누락만 선조치(`6766273`) | ⏸️ 판단 | [^24] |
| **B-18** | **★★7/24 재재정의 — "자산배분 문제"도 틀렸다. 다시 종목 선택이다**. 7/23 "αEW +5.7%p라 종목선택은 문제없음"은 **노출도 편향**(30% 투자 계좌를 100% 벤치마크와 비교)이었고, 노출도를 맞춘 **αADJ는 -7.3%p**(B안 -8.2, 파도VF만 +1.3). 현금 70%는 유휴가 아니라 **손실 방어**였다. → 지수배분·SHIELD 해제 전부 기각하고 **원위치: 종목 선택 개선**. 앞으로 판정은 αADJ로. ~~7/23 구 진단(벤치마크 착시·자산배분)~~ / ~~7/20 구 진단(전 전략 알파 음수)~~ 모두 잣대 문제였음. **⛔2026-07-27 계약전환으로 보류 — 픽 폐지로 "종목 선택 개선"의 목적 자체가 소멸**(계산봇엔 픽이 없다). B-16·B-20·B-22도 동일 사유 보류 | ⛔ 보류(계약) | [^18][^23][^24][^27b] |
| ~~B-18(구)~~ | ~~7/23 재정의 — 전제였던 "전 전략 알파 음수"는 벤치마크 착시였다~~. 메인A 3/24~7/22: 계좌 -11.7% / KOSPI +22.4% / **유니버스 동일가중 -17.4%** → KOSPI 알파 -34.1%p지만 **동일가중 알파 +5.7%p(이기고 있음)**, **구조격차 +39.8%p = 현행 알파의 117%**. 즉 손실 원인은 종목 선택이 아니라 **"개별주를 들고 있었다는 사실"**. 정합 증거: 진입거래 기저선 대비 전부 무의(D+5 -1.53~D+20 -3.65%p, p=.13~.21)·SCAN vs 비SCAN 무의(p=.34~.39)·5계좌 알파 1위가 현금방어NAV. **→ 남은 진짜 과제 = 사이즈/자산배분(초대형주·지수 비중)**이지 진입 로직 개선이 아니다. ~~구 진단(참고): KOSPI 대비 SCAN -1.77 / AI_BRAIN -3.37 / ALPHA -3.59%p, "상승장 미참여"~~ — 전부 시총가중 잣대의 산물. **7/21 진전 = 기계적 범인을 손절→진입으로 특정**: STOP_LOSS 35건(전부 SCAN)은 손절 후 D+10 -4.77%·알파 -6.48%로 **손절이 정당**(무죄), 범인은 SCAN의 종목 선택. **★7/23 진전 = 판정 기준 교정 + 프레임 정정**: KOSPI 차감 알파는 이 국면에서 무용(유니버스 동일가중조차 D+20 -9.9%p) → **기저선(동일가중 평균) 대비**로 교체·시총 5분위 매칭 후 **SCAN D+5 -4.53%p(p=.0015)/D+10 -4.80(p=.044)/D+20 -8.14(p=.012) 전부 유의**, 시그널 TOP5는 **중립(무의)** = 처방은 "갈아타기"가 아니라 **"SCAN 진입 축소"**. 손실은 **하락구간 집중**(D+10 -10.19%p, p=.000)이고 상승구간은 무의 → **"상승장 미참여"는 중소형주 전체 현상이지 SCAN 고유 결함 아님**(단 상승 n=14~25로 검출력 부족=확인 못 함). **다음 = ①상승구간 표본 축적 후 재측정 ②`FOREIGN_SURGE_PB`(6/2~, n=4~11) 축적 대기 ③SCAN 진입 축소안 설계(퐝가님 결정 필요)** | 🔴 즉시 | [^18][^21][^23] |
| ~~B-17~~ | ~~SCAN A등급 차단~~ — **내 권장 철회(7/20 밤, 퐝가님 반론이 촉발)**: 알파로 재측정하니 A등급은 **-0.12%p(중립)**·상승구간 **+0.25%p**라 차단 이득 없음. 진짜 범인은 AA(**-2.61%p**·상승구간 **-4.50%p**)이고 **이미 6/27에 차단 완료**. 절대손익·승률(36%)로 판단한 1차 권장이 **베타를 알파로 오독**한 것 | ❌ 철회 | [^17] |
| B-16 | 실주문 경로 준비 — E 게이트(페이퍼 20일 누적) 완주 후 소액 실계좌. B-15가 후보를 제시해도 **집행 경로가 freeze라 "번 돈"은 여전히 0.** 스코어보드가 실계좌 숫자를 찍으려면 이 관문이 필수 | 🟡 순번 | [^16] |
| B-14 | 원자재 cost_gap(원가 갭) 부활 — 시나리오 대시보드 원자재 섹션은 원가 기준값(production_cost) 출처 확보 전까지 빈 상태(정직). 원가 기준 재정의 시 collect_commodity_prices.py에 추가 | 🟢 저우선 | [^12] |
| **B-20** | **★★"6/27 SCAN/AA 전면 차단"은 상위 15개에만 걸린 반쪽 차단이었다 — 실측 확정(7/23)**. 섹션2 차단은 `picks_sorted[:15]`만 검사하는데 섹션4(ALPHA)는 `picks` 전체(634건)를 훑어 STRONG이면 AA 등재 → **16위 이하 STRONG은 차단 검사 자체를 안 거치고 AA·score=100(진입 우선순위 1등)으로 직행**. 오늘 실측 9건(SKC·HJ중공업·큐로셀·인벤티지랩·가온전선·켄코아·메카로·코오롱·코오롱생명과학, 전부 PULLBACK15_DUAL/VOL3x). 제2경로 = 섹션3 SWING 라벨교체(차단된 성호전자·LG이노텍이 `SWING/A/score100`으로 생환, 실측 2건). **→ 7/20 부검이 "AA는 이미 차단됨"을 전제한 것 성립 안 함**. ⏸️**봉쇄 여부는 판단 대기** — 오늘 백테스트상 차단 대상 AA는 **무의(p=.22~.49)**인데 통과되는 A가 **유의하게 더 나쁨**(D+10 -4.89 p=.020) = 차단 정책 방향 자체가 뒤집혔을 수 있어 근거 없이 손대면 손해. 잔여 미규명: SCAN/AA 2건(6/29·7/03)이 코드상 불가능한데 존재하는 이유 | ⏸️ 판단 | [^20] |
| B-19 | 헬스체크 조용한 실패 — **③ 완료(7/23 `b21bc8b`)**: `_check_price_data` 50개 샘플 추정("1,180/1,180 100.0%")을 **전수 검사로 전환**(실측 9.7초, BAT-D 143분 대비 무시 가능) + **미달 종목 코드를 detail에 기록**해 상폐/거래정지 판별 가능. **①② 는 의도적 미조치(7/23 판단)** — ①#18 당일검증 추가 시 **수동 실행에서 오탐**(장중엔 전일이 최신인 게 정상. 시각조건 하드코딩은 더 나쁨) ②`collect_investor_kis` insert=0 → exit1로 바꾸면 **휴장일 가짜 실패**. 둘 다 **백스톱 #17(투자자수급 신선도)이 당일 여부를 이미 판정** 중. 필요 시 휴장·시각 인지형으로 재설계해야 하므로 근거 확보 전 보류 | 🟢 저우선 | 7/21 검수·[^23] |
| B-8b | 데드 스크립트 정리 — **collect_investor_bulk.py 7/21 삭제 완료**(CD007 위험 제거). 잔여: collect_short_selling.py(`schedule_D_original.bat` 실호출 잔존 — bat 死확인 후 삭제)·scan_nationality·coo_orchestrator | 🟢 저우선 | [^8] |
| B-21 | 코드위생 묶음(7/21 검수) — `paper_mirror.py:24` 아키위반(use_cases→adapters 최상위 import) lazy화 / `scan_tomorrow:1409` 死코드 부분가점(임계 -1e6 단위불일치) / `check_exits` 종가 신선도 가드 부재 / `kill_switch_manager` except:pass 로깅 | 🟢 상시 | [^9] |
| B-9 | except-pass 로깅 보강 — **7/21 검수 실측 263건/131파일**(무로깅). 접점 파일 수정할 때마다 1건씩 | 🟢 상시 | [^9] |

**완료 (7/20)**: B-13(휴장일 가드 — KIS 공식 API 기반, 가짜 D등급 경보 제거) + 스코어보드 텔레그램 배달 픽스 + **B-15(BULL 전환 감시 게이트 — 반등 시 후보 즉시 제시)**[^15] — §4 당일 기록 참조.
**완료 (7/17)**: B-11(EWY→KIS 구성종목 전환)·B-12(원자재 수집 부활)·B-6(레버리지 ETF 수급 KIS 재배선)·B-7(훅 픽스+파일 삭제)·B-8(수급 복구 경로 라이브 리스크 제거) — §4 당일 기록 참조.
**완료 (7/16)**: B-1(오진방지)·B-2(순서 오탐)·B-3(원장 라벨)·B-4(원자재 신선도)·B-5(소요시간 복원+임계 175)·B-10(SKIP 표기) — §4 당일 기록 참조.

상태: 🔴 즉시(다음 작업일) · 🟡 순번 대기 · 🟢 저위험/상시 · ⏸️ 판단 대기. 완료 시 §4 엔트리에 기록 후 행 삭제.

## 4. 일일 기록 (최신순)

### 2026-08-10 (월·오전) — ★"채워진다"가 무엇이 채워지는지를 안 봤다 · mirror 배선 위치 오인 정정

**09:34 세션 시작(장중).** 주말 이틀 공백 후 첫 거래일. 오전 루틴 + 8/9이 남긴 3건 처리.

**기초데이터: 정상** — 8/7 cron **15단계 전부 실패 0건** 완주. 8/7 헬스체크 **17/18 B등급**, 실패는 알려진 국적별 STALE(57일)뿐. 오늘 BAT-A~I 실패 0, intraday_eye 5분 주기 정상 순환. VPS HEAD `9b62227` = 로컬 최신(동기화 확인). A-★ 수신함 **양 경로 모두 확인** — 로컬 웹봇 폴더 최신은 8/7 우리 발신분, git `docs/` 인바운드는 8/7 웹봇 요청 2건(`863ab191`·`722b38fb`)이 마지막이고 같은 날 회신 완료. 신규 없음.

**★★A-4 — "8/7 수급 2,827 확인"이라는 질문 자체가 틀렸다**: 8/7은 **2,623**이고 평시는 2,827이다. 주말이라 D+1 채움이 안 일어난 상태 그대로. 그런데 **실거래값(buy/sell>0) 있는 종목수를 같이 뽑으니 8/7이 2,615로, 다른 날(2,608~2,641)과 같은 범위 안**이었다. 차이 204종목의 정체를 판별하니 **197종목(96.6%)이 8/6에도 실값 0**인 무거래 종목(우선주·거래정지주 — 000880·001570 등은 8/7 헬스체크 정지 목록과 일치). 나머지 7종목 중 종가 parquet이 있는 3종(067390·082640·091810)은 **8/7 거래량이 실제로 0**이었고, 4종은 유니버스 밖이라 parquet 자체가 없다. → **D+1에 추가되는 것은 실값이 아니라 무거래 0행이다. 총 종목수는 데이터 완전성 지표가 아니며, 실질 데이터는 당일 수집에서 이미 완비된다.** 7/30 "껍데기 가설 확정(종목수 정본=D+1)"은 **채워진다는 사실만 맞고 무엇이 채워지는지를 안 봤다** — §1 A-4의 "장중엔 종목수 말고 실거래값 종목만 보라"는 지침이 오히려 정본에 가까웠다. 헬스체크 #18은 총 종목수 D-1 대비 95%로 판정하므로 **무거래 0행 추가를 완전성으로 오인**하고 있다(오탐은 아니나 잘못된 것을 보고 있음). **★배포 전 예측검증 예약**: 오늘 BAT-D 후 8/7은 총 2,623→2,827(+204)·실값 2,615는 거의 불변·추가 204행 전부 0이어야 한다. 어긋나면 이 판정을 뒤집는다(7/30 "단일 스냅샷으로 판정하면 부호가 뒤집힌다" 적용).

**★A-6 mirror — 8/9의 배선 위치 판단이 틀렸다**: 8/9 기록에 "BAT-J(평일 17:00)에 배선"이라 썼으나 실물은 `run_bat.sh:333`으로 **BAT-D(16:30 시작) 블록**, 페이퍼 5트랙 직후다. J 블록은 `run_portfolio_outlook.py` 한 줄뿐. 따라서 첫 실운영은 17:00이 아니라 **BAT-D 중반**이며, 8/7 기준 BAT-D가 148분(16:30~18:58)이었으므로 그 사이 실행된다. 오늘이 첫 평일이라 첫 실행인 것은 맞다. ※VPS `data/forward_paper/events.jsonl`이 **8/9 00:47자로 이미 존재**(1,263건) — 코덱스 커밋 시각(00:42~00:46)과 인접. 오늘 실행은 첫 생성이 아니라 **멱등 증분**이 된다.

**★B-50 웹봇 통보 완료 (`5a7af781`, 10:00 — 데드라인 7시간 전)**: "cost_complete=false 행은 순성과 집계에서 제외" 요청을 보내려고 원장을 실측하다 **더 중요한 사실을 발견 — 현재 1,263건 전량(100%)이 false**다. 즉 웹봇이 이 필터를 그대로 걸면 **순성과 표시 대상이 0건**이 된다. 요청만 보냈으면 웹봇은 빈 화면을 장애로 오인했을 것이라, "0건이 정상이며 계약을 지키고 있다는 뜻"을 함께 통보했다. 실측: FILL 196·MARK 904·EXIT 163 / KR 790·US 473 / 전략 36 / **real_order=true 0건** / `net_pnl`·`net_return`·`signal_at`·`eligible_at` 각 **0/1263**(부정확한 값이 아니라 아예 null) / `fee` 7·`tax`·`slippage_cost`·`spread_cost` 0 / `benchmark_return` 646·`exposure` 703·`regime` 48. 소비 주의 3건 명시(플래그가 `payload.metadata` 안쪽이라 최상위로 읽으면 조용히 None → 필터가 통과됨 / false를 결측으로 다루지 말 것 / 필드명은 `slippage`가 아니라 **`slippage_cost`**). 배달은 **로컬 폴더 + git `docs/` 양 경로**(8/7 교훈).

**A-5·A-7 정상** — 8/7 업로드 2/2(brain·journal). 실주문 **삼중잠금** 확인: `data/KILL_SWITCH` 파일 활성(5/27~) + `AUTO_TRADING_ENABLED=0` + `OWNER_LIVE_TRADING_APPROVED=False`(8/8 신설 코드 상수). 페이퍼 6종 원장 8/7 18:51 갱신(주말 미갱신 정상).

**★내 오류 2건(자진 기록)**: ⑴**원장 조회에서 `payload` 아래로 안 내려가 11개 필드가 전부 None/0으로 나왔다** — 그대로 믿었으면 "전 필드 결측"이라는 통보서를 보냈을 것이다. 최상위 키가 해시체인 래퍼 4개뿐임을 확인하고 즉시 재조회(8/1 "0건 주장 전 출력 확인" 적용). 통보서에 이 실수를 그대로 적어 웹봇도 같은 함정을 피하게 했다 ⑵**KILL_SWITCH를 `.env`에서 찾아 "없음"으로 판정할 뻔** — 실제로는 파일 기반(`data/KILL_SWITCH`)이고 코드 주석에 "env로는 noop"이라 명시돼 있었다.

**★오후 — 오늘 발견이 감시 2건의 교정으로 이어졌다 (커밋 3건 `36a0879`·`7c6b73e` + 규칙 `11973b9`)**

**★퐝가님 지시로 장중 배포 규칙 변경(`11973b9`)** — "페이퍼 중이니 상관없다". 실주문이 삼중잠금이라 장중 코드 교체로 체결이 틀어질 경로가 없다. CLAUDE.md에 예외를 반영하되 **소멸 조건을 함께 못박았다**(실주문 재개 시 자동 복원). BAT-D 실행 중(16:30~19:00) 회피만 남겼다 — 그건 bash가 시작 시 스크립트를 메모리 로드하는 별개 사유로 실주문 여부와 무관하다.

**★★#18이 잘못된 것을 보고 있었다 — 200종목 누락 미탐(`36a0879`)**: 오전 A-4의 후속. `drop=(prev_n-today_n)/prev_n`에서 **today는 실거래 종목만(~2,62x), prev는 0행 포함 완전판(~2,827)** — 성격이 다른 둘을 비교하고 있었다. **상시 7.2%의 가짜 감소가 깔려 임계 15% 중 실질 여유가 7.8%뿐**이었고, 행은 있고 값만 0인 형태에서는 총계가 유지돼 **실값 절반이 날아가도 통과**했다. 급감 판정 소스를 실거래값 종목수로 교체(동종 비교)하고 임계 15%→5%(직전 12일 실측 변동폭 1.3%의 4배 여유). **역시험**: 정상 12일 전부 통과(-0.46~+0.57%, 8/7은 구 7.22%→신 **-0.27%**로 drift 제거) / 인공 장애에서 구판정은 **1,300종목 누락도 0.00% PASS**, 신판정은 130종목부터 탐지. VPS 함수 직접 호출로 실동작 확인(`20260807 실값 2615종목/총 2623 … 정상`). **★한계 명시** — 특정 투자자 유형 컬럼만 0인 경우(B-39·46·47 계열)는 여전히 탐지 대상이 아니다(B-43 소관). 전제가 틀려도(추가분이 실값이면) drop이 음수라 오탐은 나지 않는 구조.

**★B-44 완료 — 17/18 B등급 영구 고착 해소(`7c6b73e`)**: 국적별수급이 매일 ❌로 세어져 등급이 굳어 있었고, 그러면 진짜 이상도 "늘 그렇지"에 묻힌다. `RETIRED_CHECKS` 명시 화이트리스트로 등급 분모에서 분리. **★설계 함정 2개를 피했다**: ⑴**"실패하면 자동 retired"로 만들지 않았다** — 그러면 일시적 장애까지 등급에서 빠져 진짜 고장을 숨긴다(8/1 "억제 설계가 뚫림"과 같은 계열) ⑵**감시를 끈 것이 아니다** — 데이터가 돌아오면 `passed=True`가 되어 자동으로 판정 대상 복귀, 리포트엔 경과일수를 남겨 방치도 막는다. 5케이스 검증: ①현행 **17/17 A** ②부활 18/18 A ③**다른 1건 실패 시 16/17 B(은폐 없음)** ④3건 실패 14/17 C ⑤부활+실패 17/18 B.

**★교훈 — 같은 실패 모드가 다섯 번째다**: 7/31 smart_money(적재됐는가만 봄) → 8/6 B-43(존재·커버리지만 봄) → 8/7 B-46 연기금 401일 0 → 8/7 B-47 공매도 5.5개월 0 → **오늘 #18(종목수만 보고 그 종목에 실값이 있는지 안 봄)**. 개별 픽스로 다섯 번 대응했다는 것은 **계열로 다루지 않았다는 뜻**이다. 남은 동형 후보를 선제 점검할 것(→B-53 등재).

**★★B-53 착수 — 4건 교정(`36a0879` 외 `dc8f1a2`급 4커밋). 진단이 "값을 안 본다"보다 정확해졌다**

18개 검사를 전수 분류하니 **값을 보는 것은 3개뿐**(①종가 정체판정 ⑮파이프라인 ⑱수급커버리지)이었다. 순차 교정하면서 드러난 공통 형태는 "값을 안 본다"가 아니라 **"검사가 참조하는 키·컬럼이 실제 산출물과 어긋나 있는데, 느슨한 임계가 그 불일치를 가렸다"**였다. 넷 다 매일 `?`나 `⚠️`를 찍고 있었고 아무도 읽지 않았다.

| | 검사가 찾던 것 | 실제 산출물 | 왜 안 드러났나 |
|---|---|---|---|
| **①수급(종목)** | `체결강도` 컬럼 | **존재하지 않는 컬럼** | `not pd.isna()`로 NaN만 봐서 0이 통과 |
| **②수급(섹터)** | 최상위 `date` | `sectors[].date` | `found>=2`가 상시 ⚠️를 통과시킴 |
| **③학습기록** | `signal_count`·`total_signals` | `summary.total`·`signal_accuracy` | 파일 존재만으로 통과 |
| **④원자재** | 1차=`liquidity_signal` | **정본은 2차** `commodity_prices` | 매번 1차 헛돌고 폴백으로 통과 |

⑴**①(`36a0879` 후속)** — B-39·B-46·B-47이 **전부 이 검사를 통과**했다(같은 검사가 세 번 뚫림). 값 생존 검사 추가: 판정 단위는 **컬럼 × 표본 전체**이고 관측창 20거래일 내내 0인 컬럼만 결손으로 본다(개별 종목 특정일 0은 거래가 없었을 뿐 — 8/1 `overheat_penalty` 오탐 교훈). 검증: 기타법인 전량0 주입 시 **당일 탐지**(과거엔 3.5개월). `pension_net_5d`는 `KNOWN_ZERO_COLS`로 신규 결손과 분리 — 매일 ❌면 B-44에서 막 걷어낸 등급 고착이 재현된다. **★신규 발견: `pension_top_buyer`도 0/401** — B-46은 연기금 컬럼 1개만 봤으나 실은 2개다.

⑵**②** — 실측하니 investor_flow는 매일 17:57 정상 생성에 19개 항목 전부 당일자였다. **데이터가 아니라 검사가 틀렸다.** `_json_asof()`로 항목 레벨 폴백 + 임계를 전량 요구로. **감시 구멍 두 개가 서로를 숨기고 있던 형태.**

⑶**③** — 없는 키 2개를 찾아 매일 `(시그널: ?)`. 실제는 `summary.total`(1182)·`signal_accuracy`(10엔진). 내용이 비어도 ✅였고 **`except`에서 `True`를 반환해 파손 파일도 통과**하던 것까지 교정.

⑷**④ — 주석이 사실과 정반대였다**: `commodity_prices.json`을 "Alpha Vantage 3/31 아카이브 유물"로 적어 폴백에 뒀는데, 실측하니 **오늘 06:10 갱신**에 `source="yfinance futures (v2, 7/17 재구축)"` — **7/17 B-12로 부활한 정본**이었다. 반대로 1차 `liquidity_signal.json`엔 원자재 키가 아예 없다. **틀린 주석을 방치하면 언젠가 "유물이니 지우자"로 진짜 소스가 사라진다.** 순서 교체 + 가격 값 판정 추가(구 로직은 표시만 하고 판정엔 안 씀) → `live 5/5`.

⑸**장중 가드 4곳 통일** — ①②③에 "당일 산출물이 없고 종가도 당일치가 없으면 판정 보류"를 넣었다. 구 로직들은 장중 실행 시 **늘 가짜 실패**였는데 HEALTH가 18:45 cron으로만 돌아 안 드러났을 뿐이다. 오늘부터 장중 배포·점검이 늘어나므로 선제 차단. 판단 소스는 시각이 아니라 **종가 존재**(#18·B-19① 원칙) — 종가가 당일치인데 산출물만 비면 시각과 무관하게 진짜 이상이다.

**남은 B-53**: 참조 키는 맞으나 값 판정이 없는 부류 — ⑥FRED(키 존재만) ⑦COT(개수만) ⑧DART(건수만) ⑨⑩⑪레짐·BRAIN·SHIELD(날짜만) ⑬업로드(mtime만) ⑯⑰(날짜+행수). ※⑨⑩⑪ 값 고착 검사는 **오탐 위험이 커 보류** — 레짐/SHIELD는 원래 오래 유지되는 게 정상이고(8/1 `market_kospi_stoch_k` 범위지표 오탐과 동형), B-22 SHIELD 4개월 고착의 진짜 원인은 값이 같은 것이 아니라 **해제 조건이 구조적으로 충족 불가**였다 — 헬스체크 층위가 아니다.

**저녁 대기 3건**: BAT-D 중 mirror 첫 실운영 / B-48 예측검증(위 A-4) / **18:45 HEALTH에서 오늘 배포분 전체 첫 실동작**(A-6 — #18·B-44·B-53 ①~④).

### 2026-08-09 (일·새벽) — 전방 페이퍼 커밋 전체검수 · 검수 도구가 두 달간 꺼져 있었다 [^38]

**퐝가님 지시 = "전체 커밋+푸시 하고 전체 검수".** 커밋·푸시할 것은 **없었다** — 로컬·원격·VPS 모두 `0d97d506`, working tree clean. 대신 8/9 00:42~00:46에 **내가 만들지 않은 커밋 2건**(1,306줄)이 들어와 있었고 `risk: critical` 자동검수가 걸려 있어 그것을 검수했다. ★8/7에 내가 미커밋으로 남겨둔 코덱스 켈리 5파일이 이 커밋에 포함돼 정리됐다.

**【양호 — 지시서 이행 확인】** 8/8 소유자 지시서(`flowx-forward-paper-control-2026-08-08.md`) 대비: ⑴**`OWNER_LIVE_TRADING_APPROVED = False` 상수 신설**(`trade_runtime_safety.py`) — §1 "`.env` 단일 플래그 변경만으로 실주문을 열 수 없다"를 **정확히 이행**. 이 게이트는 `assert_runtime_orders_allowed`로 주문 어댑터·adaptive 4종·텔레그램 3곳·sell_monitor까지 **광범위 배선**돼 있어 실효적이다 ⑵`live_trading.enabled: true→false`, `mode: "real"→"paper"` — 3/2 이후 `real`이던 설정이 명시적으로 닫혔다 ⑶켈리는 `kelly_cap:1.00`이라 **증액 구조적 불가**(§1 부합), `use_kelly`·`use_correlation` 둘 다 **false**라 실동작 불변 ⑷원장 **SHA-256 해시체인·sequence 검증·원자적 디렉토리 락·fsync**(§3 부합) ⑸**테스트 37 passed**(첫 실행 24 errors는 Windows 임시폴더 권한 문제였고 코드 결함이 아니었다) ⑹**`real_order=true` 0건**(1,263/1,263).

**★미검증 배포를 드라이 실행으로 선검증** — `mirror_forward_paper_ledger.py`가 BAT-J(평일 17:00)에 배선됐는데 8/8·8/9가 주말이라 **8/10이 첫 실행**이었다. 임시 원장(`--ledger /tmp/fp_dry`)으로 실제 페이퍼 데이터를 돌려 **1,263이벤트 생성·63초·2회차 멱등(0.75초, appended 0)** 확인. 첫 실행은 정상 동작한다.

**【결함 4건】** ⑴**★★B-49 — 자동 검수의 절반이 두 달간 꺼져 있었다**: `codex_inbox` **277건 전수**에서 `self_review_checklist`가 EXIT 1, PASS **0건**(6/10~8/9). 원인은 호출부에 **`-X utf8`이 빠져** cp949 stdout이 em-dash에서 죽은 것. **수정·검증 완료**(3/3 exit=0, "8룰 모두 통과"). 두 달 만에 처음 돌렸는데 **8룰 전부 통과**라 실제 위반은 없었다 ⑵**B-50 — 원장이 지시서 필수 필드를 못 채운다**: 비용 4종 사실상 전부 0(`fee` 7/1263, `tax`·`slippage`·`spread` 0), `net_pnl`·`net_return` **0/1263**, `signal_at`·`eligible_at` **0/1263**(→ §4-1·2 "같은 봉 유리가격 체결 금지"를 **검증할 근거가 없다**), `SIGNAL_FROZEN`·`ORDER_INTENT` 0건. **설계자가 `cost_complete=false`로 한계를 표시해 둔 legacy 1단계**라 결함은 아니나, 그 플래그가 `metadata` 안쪽이라 **소비자가 무시하고 집계하기 쉽다** ⑶**B-51 — append가 건마다 전체 재검증**(O(N²)) + **stale lock 자동정리 없음** ⑷**B-52 — `use_kelly`를 켜면 18/22 엔진이 진입 0주**가 되는데 경고가 없다.

**★교훈: 검수 도구의 실패를 아무도 검수하지 않았다.** 277번 같은 문자열이 로그에 남았는데 두 달간 아무도 읽지 않았고, critical 변경조차 그 체크를 건너뛴 채 통과했다. 7/30 "감시 도구는 자기 자신의 실패 모드로 검증해야 한다"가 **도구 바깥(호출부)에서 재현**됐다.

### 2026-08-07 (금) — 백필이 "성공"을 찍고 소비처엔 닿지 않았다 · 옆 컬럼에서 결함 3건 [^37]

**세션 19:11 시작(장 마감 후).** 퐝가님 지시 = 8/6·8/7 종가·CSV·수급 갱신 확인.

**기초데이터: 정상** — cron 8/6·8/7 모두 완주(오늘 BAT-D 143.3분, 실패 0·KIS 에러 1). 종가 parquet **1,176/1,182** 8/7까지(미달 6종은 상폐·정지 낡은 파일, 기확인). CSV 8/6·8/7 전량 생성(picks_v2·sector_fire·supply_chain·supply_surge·type2_bottom + kospi/kosdaq_index). 수급 DB 8/5~8/7 **11개 투자자 전부 실값**. 정보봇 `jgis_short_factor.json` 8/7 18:27 도착.

**★B-39 첫 실동작 — 내 1차 판정이 정반대였다**: "8/7 기타법인 1,176개 전량 0"을 보고 **수정 실패로 판단**했다. 시계열을 펴보니 **7/24~8/6만 채워져 있고** `8/7−14일=7/24`로 코드의 14일 창 경계와 정확히 일치 — 즉 **작동한 증거**였고 당일 0은 설계된 D+1 충전이었다. 8/6 커밋(17:06)이 그날 BAT-D(16:35)보다 늦어 오늘이 첫 실행이라는 것도 맞물린다. **단일 날짜 스냅샷으로 판정하면 부호가 뒤집힌다.**

**★★백필이 성공 로그를 찍고 소비처엔 닿지 않았다 (오늘의 핵심)**: 전구간 백필 실행 → `1,209/1,218파일 265,456칸, 실패 0, 68초`. 그런데 검증하니 **4/27~7/23이 그대로 0**. DB엔 실값이 있는데(005930 5/4 -121.7억 등) parquet은 0 — 모순이라 파보니 **백필 대상은 `data/raw`**(`extend_parquet_data.py:586`)이고 **소비처는 `data/processed`**였다. 둘은 `rebuild_indicators.py`(BAT-D 17:43~17:56 **전량 재계산**)가 잇는다. **0/NaN만 채우는 설계라 로그는 정상 성공으로 찍힌다** — 검증을 raw에서만 했으면 "완료"로 보고했을 것이다. 주말 백테스트(토 09:00·일 17:00)가 0인 상태로 돌지 않도록 **지표 전량 재계산 수동 실행**(1,218/1,218, 약 25분) → 전파 확인: 4/27 **0→102**, 5~6월 92~108, **D+1 대기였던 8/7까지 62개**(백필이 전 구간이라 당일분 포함). 값 대조 **329/330 일치**, 불일치 1건은 8/6 **차 100만원(0.003%)** — 어제 증분이 채운 뒤 DB가 미세 정정된 것으로 "실값 불변" 원칙대로의 거동.

**★검증하다 옆 컬럼에서 결함 3건(B-46·47·48 신규)**: ⑴**연기금이 parquet에 한 번도 실린 적 없다** — `pension_net_5d` **401일 전구간 0**, `supply_cols`에 연기금이 없어 `indicators.py:768`이 항상 else. `v8_scorers` 연기금 보너스(0.03~0.05) 영구 미발동. **★단 영향 범위를 과장하지 않는다** — 3봇 분업의 "퀀트봇=금투+연기금" **주 신호는 무사**(`future_value_engine`·`signal_engine`이 DB 직접 조회). 죽은 건 parquet 경유 v8 가점 한정 ⑵**공매도·대차 4컬럼 2/19부터 5.5개월 전량 0** — 원인은 코드 주석에 이미 있었다(KRX 공매도 제공 중단으로 수집기 제거). **소스가 죽었는데 컬럼은 0으로 남아 계속 계산에 들어갔다**. 정보봇 JSON이 매일 오니 B-39 방식으로 복구 가능 ⑶**수집 이력이 안 남아 "오늘 다 받았나"를 판정할 수 없다** — 8/7 수급 2,623종목 vs 직전 10일 2,827인데 `collect_log`가 6/1에 멈춰 D+1 패턴인지 수집 실패인지 **근거가 없었다**.

**웹봇 회신 발송** — 무감시 6종 주기 확정 요청. 5종 우리 소관(쓰기 경로 명시)·`supply_scoring` 아님. **★"고정 시각 라벨 쓰지 마시라"** 권고 + 15거래일 실측 범위(flowx 18:45:51~18:52:27, two_layer 18:51:57~19:00:01) — **시장이 크게 움직인 날일수록 늦어져 하필 가장 중요한 날 가짜 STALE**이 뜬다(7/31 KOSPI +17.91%일 최대 지연). 거래일 가드는 **없음(코드 사실)**이나 공휴일 거동은 **양쪽 미관측**이라 단정 보류 — 다음 평일 공휴일 공동 관측 제안.

**★A-★ 또 부분 이행** — 로컬 웹봇 폴더만 보고 "수신함 신규 없음"으로 넘어갈 뻔했다. 웹봇은 **우리 git `docs/`에 커밋**으로 보냈고, VPS pull 결과에서 우연히 발견했다. 어제 반성한 바로 그 항목을 **경로 하나만 확인해서 또 놓칠 뻔했다**. 수신 경로가 두 갈래(로컬 폴더 + git docs/)임을 확인. ※`deploy_pull.sh`(17:45)는 단타봇 저장소용이라 퀀트봇 미배포는 고장이 아님 — 어제 docs 2커밋 후 pull을 안 한 것(오늘 배포 완료).

### 2026-08-06 (목) — 미배포 이틀을 아무도 몰랐다 · B-39 원인확정·수정 [^36]

**⚠️ 세션이 16:42에 열렸고, 첫 발견이 미배포였다** — VPS가 **8/1 코드(`6d95daf`)에 멈춰** 있었다. 8/5 낮에 푸시된 5커밋이 미반영 = 8/5 저녁 BAT-D 전체 + 8/6 BAT-D 초반(16:30~16:44)이 옛 코드로 실행, /scenario 거짓 0이 하루 더 나감. 16:44 긴급 배포(`ed5f977`) — 시나리오 업로드(18:40)·HEALTH(18:45) 전이라 **오늘 저녁분부터 새 코드**. 미배포 커밋이 D·J 페이퍼 경로를 안 건드리는 걸 diff로 확인 후 진행. 17:45 `deploy_pull.sh`는 단타봇용(quantum-master 미포함) 실측 재확인 → **B-42 등재**

**기초데이터 (8/5 수): 🟡 17/18 B등급** — 실패는 국적별 STALE(55일, 알려진 외부대기)뿐
- **종가**: 1,176/1,182 (99.5%), parquet **내용** 확인 — 삼전 246,000 · 하이닉스 1,668,000 · 현대차 404,500 (last=8/5). 미달 6종 동일(기확인)
- **수급**: `investor_daily` 8/5 **28,930행**(총 493만행) · 커버리지 2,630종목(직전 2,827, 채움OK) · 금투/연기금 실값
- **CSV**: kospi_investor_flow 17:23 · supply_chain/surge 18:43 · 시그널원장 16:50 ✅
- **cron 에러 실질 1건**: 13:58 KIS 현재가 순간 끊김(305540) 단발. "2건" 중 1건은 `ERROR=0` 문자열 오매칭 = **내 grep 오탐**

**★B-39 원인확정 → 수정 배포(`f5e61d5c`) — 소화 1건**: KIS 종목별 API(FHKST01010900)에 기타법인이 없어 `if col in inv_df.columns`가 조용히 건너뜀 + 외/기/개 채워진 행은 zero_mask 재충전 영구 제외 = **이중 미충전**. 타임라인 커밋 확정 — **컬럼이 4/15 추가(`c5113224`)되고 4/18 벌크 전환(`9cd5b7ed`)으로 이틀 만에 사망**. 수정 = `investor_daily.db` `net_val`(순매수 대금·원) 충전(일일 14일 창 + `--backfill-etc-corp` 전구간). **단위·의미 검산을 수정 전에 수행** — 4/10~16 겹침 구간 DB=parquet 소수점 일치(005930·000660), 4월 삼전 일 3~4천억은 자사주 매입기 실값. 테스트 5케이스 + 로컬 백필 실동작 518칸. 잔여: VPS 전구간 백필(19:30 오토)

**★A-★ 지연 1건 — FLOWX 마스터 지시서(8/5 11:41 도착)를 마감에야 발견**: 세션 시작 수신함 확인(§1 1번)을 안 지켰다. 7/27 3거래일 방치의 축소 재발(1일). 퀀트봇 직결 조항 = **§8 구독 전 데이터 기준 5개**(수집 성공률 99.5%·기준일/출처/단위 표시·결측 감시·3~5년 백필·장애 공개) + §1·§2 **"결측값을 0으로 채우지 않는다·null과 0 구분"** — 오늘 B-39가 정확히 이 원칙 위반의 복구다. 현황 대조는 **B-45 등재**

**자기성찰 (0+3)**
- **0 (계약·정확성)**: B-39 복구 — 스마트머니 4요소 중 1개(기타법인)가 parquet 경로에서 3.5개월 거짓 0이던 것. 지시서 "결측을 0으로 채우지 않는다"의 실물 이행. + 미배포 해소로 /scenario 거짓 0 종료
- **1 (오탐/누락)**: ⑴**A-★ 수신함 확인 누락**(지시서 1일 지연) ⑵"에러 2건" grep 오탐 1건(`ERROR=0` 매칭) ⑶어제 "저녁에 배포"를 남겼지만 **저녁 세션이 안 열리는 경우의 백업이 없었다** — 선언이 세션 유무에 의존(→B-42)
- **2 (틀린/뒤집힌 판단)**: ⑴커밋 메시지에 "4/17 KIS 전환"이라 추정을 썼다가 git 이력으로 4/15 추가·4/18 전환으로 정정 ⑵4월 삼전 기타법인 3~4천억을 이상값으로 의심 → DB 겹침 대조로 실값 판명. 어제 mtime 오탐 2건과 같은 계열이나 **이번엔 보고 전에 검증이 먼저 왔다**
- **3 (내일 1건)**: B-42 미배포 감시 착수 — 단 오늘 밤 A-6(어제 배포분 첫 실동작) 통과 확인 후. 같은 파일에 검증 전 코드를 얹지 않는다(7/30 교훈)

**오토 진행분(기록 시점 대기)**: 19:00 A-6 4건(종가 5분류·/scenario 실값·낡음 경고·KRX 무음) / 19:30 기타법인 백필 — 결과는 본 엔트리에 후속 append

### 2026-08-05 (수) — 8/3·8/4 기초데이터 이상 0건 · ★내 의심 2건이 둘 다 오탐

**기초데이터 (8/3 월·8/4 화): 양일 🟡 17/18 B등급** — BAT 전 단계 실패 0건, BAT-D 완주(8/3 18:57 / 8/4 18:59, 144.9분). 실패는 국적별수급 STALE(53·54일, 알려진 외부대기)뿐

- **종가**: 양일 **1,176/1,182 (99.5%)**. parquet 1,182개 전수 재검사로 헬스체크 수치 독립 확인, `read_err 0`. 미달 6종목(003410·010620·012510·057050·138490·230360)은 양일 동일 = 기확인 거래정지/상폐. 실값 검증: 삼전 8/3 239,500 → 8/4 240,000 / SK하이닉스 1,567,000 → 1,577,000 (날짜별로 달라 복사·정체 아님)
- **수급 11주체**: 8/3 **2,827종목 31,097행**(껍데기 204) · 8/4 **2,631종목 28,941행**(껍데기 3). 8/4분이 오늘 채워질 자리 — **익일채움 규칙 5거래일째 유효**
- **3봇 분업 전부 실값**:
  - 퀀트봇(금투·연기금) — 8/3 금투 **-15,462억**·연기금 -65억 / 8/4 금투 **+1,733억**·연기금 **+926억**. 종목 실측 8/4 삼전 연기금 +3,718억, SK하이닉스 연기금 +3,929억
  - 단타봇(외인·기관합계·개인·기타법인) — 8/4 외인 -6,468억·개인 +5,590억·기관합계 +42억·기타법인 +836억
  - 정보봇(공매도·신용·대차) — `jgis_short_factor.json` 8/4 18:28 갱신 ✅
- **CSV**: picks_v2·sector_fire·supply_chain/surge 양일 생성 ✅ · kospi_investor_flow 8/4 17:22 ✅ · universe.csv 8/3 11:31(후술)

**A-6 지난주 배포분 첫 실동작 — 예고한 4건 전부 성공**
- ✅ **켈리 섀도 무인 실행** — `kelly_shadow.jsonl` 8/3 18:56·8/4 18:59 자동 기록(BAT-D 종반 cron)
- ✅ **xmin 배치 판별 무인 동작** — `dashboard_smart_money 290행/배치 2개(17:05#361516×240, 17:05+18:40#361964×50)`. 우리 50행이 정보봇 17:05 배치에 묻히지 않고 정확히 분리
- ✅ **페이로드 자기검사·§2.5 무음** — 유지 5종 전부 검사, 거짓 상수 0. `[alert] 위반·누락 없음 — 발송 생략(무음)`, 퀀트봇 중단 18종 적재 0건 유지
- ✅ **score 상한 100 + 라벨 영문 유지** — 8/3·8/4 `score>100` **0건**(7/31까지 107~110). `price==0` **0건**, null 1행은 코오롱ENP(138490) = 종가 미달 6종목 중 하나라 **정확한 동작**. `signal_type='DUAL_FLOW'` 영문 유지(B-33 순서 리스크 대응대로 한글화 잠금 지속)

**★자기성찰 — 오늘의 오탐은 전부 내 것**

| 내 의심 | 실제 | 왜 틀렸나 |
|---|---|---|
| #18 "직전 2827"이 7/30·7/31·8/3 **3거래일 연속 동일** → 하드코딩·캐시 의심 | **정상**. 2,827 × 11주체 = **31,097행**으로 정확히 떨어짐(8/4도 2,631×11=28,941). 수집 대상 집합이 같으면 같은 수가 나오는 구조 | 산출 **구조를 안 보고 숫자 반복만** 보고 의심했다 |
| `universe.csv`가 8/3 11:31이 최신 = 8/4 미갱신 → 누락 의심 | **정상**. `유니버스 전체 재구성 시작 (DOW=1)` — **월요일에만 전체 재구성**, 화요일은 증분 동기화(raw +0, processed +0) | 갱신 **주기를 확인하기 전에** mtime 결손으로 읽었다 |

- 공통 원인: **파일 mtime·로그 숫자라는 2차 지표만 보고 산출 코드를 안 봤다.** 둘 다 코드 한 줄(11주체 곱셈 / DOW=1)이면 즉시 풀렸다
- 다만 **보고 전에 검증**해서 퐝가님께 오보는 가지 않았다 — 7/14 "에이전트 발견은 직접검증" 원칙이 내 자신의 의심에도 적용됐다
- 반대로 **놓칠 뻔한 것**: 이 2건에 시간을 쓰느라 아래 3건(실제 개선점)을 늦게 봤다

**개선점 3건 — 백로그 등재**
- **B-37(신설·🟡)**: score 상한 포화 — 8/4 퀀트봇 50행 중 **100점이 40행(80%)**. 8/3은 31행. 상한 100 목적(거짓 비교 방지)은 달성했으나 상위 40행이 서로 구분 안 됨. 원인은 재배분이 아니라 **원본 공식의 연속일수 보너스가 상한에 포화**(`STRONG90+dual10+연속7~10`)라 -10 시프트가 그대로 옮겨온 것. ★정보봇 max 70 vs 퀀트봇 min 98로 **여전히 완전 분리** = 8/1에 낸 "상한만으론 상위 편중 안 풀린다"는 실측 반론이 재확인. 해소는 B-33(source) 소관
- **B-36 관련 실측(🔴)**: **기저선 승률이 3일 연속 상승** — 8/1 38.3% → 8/3 39.2% → 8/4 **41.2%**. 켈리 양수 조건 격차도 절반으로 좁혀짐(기술촉매: 8/1 필요 48.9% vs 실제 44.1% = **-4.8%p** → 8/4 필요 47.4% vs 실제 **45.3%** = **-2.1%p**, b 1.045→1.111). **B-36 선행조건 ⑵ '국면별 안정성'이 실시간으로 증명되는 중** — 46일 표본이 폭락장 편중이라던 우려대로, 시장이 회복하자 같은 엔진의 수치가 움직인다. 전 엔진 `mult_abs 0.0`은 유지
- **B-38(신설·🟢)**: `collect_log` 테이블 **2개월 미갱신**(마지막 20260601, 6/2 06:43). 6/27 KIS 11세분 전환 후 새 수집기가 이 테이블을 안 쓴다. 데이터 자체는 정상 적재되므로 실피해는 없으나 **수집 이력 감시가 죽은 채로 방치**돼 있었다(B-28 "감시 사각"과 동류)

**[오전] A-8 백로그 소화 — 껍데기 204건을 파다 ★종가 검사의 사각을 찾았다** (배포는 15:30 이후)

껍데기 규명에서 시작해 헬스체크 결함으로 이어졌다.

- **껍데기 204건 정체 = 저유동 종목의 무거래일**(우선주·스팩 다수: 삼양홀딩스우·한화3우B·JW중외제약2우B·교보18호스팩…). **유니버스(1,182) 교집합은 12종목뿐** — 나머지 192는 유니버스 밖. 결손이 아니라 실제로 거래가 없어서 0
- **★그런데 그 12종목 중 한화(000880)가 걸렸다** — 대형주인데 7/30부터 수급 0. 파보니 **종가 83,800 5일 고정 + 거래량 0 + 수급 DB 부재** = 거래정지 확실
- **★★전수 조사: 1,182종목 = 정상변동 1,161 + 정지 15 + 당일행없음 6.** 그런데 헬스체크는 **`1,176/1,182 (99.5%)`로 보고**한다 — 정지 15종목이 "정상"에 섞여 있다

**★결함의 본질**: `_check_price_data`가 `target_ts in df.index`, 즉 **"당일 행이 있는가"만** 본다. 거래정지로 값이 멈춘 것과 **수집기가 고장나 전일 값을 재사용하는 것이 화면상 완전히 같다**. 7/31 `price=0`이 6주간 ✅로 보였던 것과 정확히 같은 구조 — *적재됐는가만 보는 감시는 값 오류를 영원히 못 잡는다*가 종가에도 그대로 있었다

**★★검증이 또 내 설계를 잡았다**(8/1에 이어 2번째)
- 1차 설계 = `종가 고정 AND 거래량 0` → 거래정지는 잡는다
- **그런데 수집기가 전일 행을 통째로 복사하면 거래량도 함께 고정되면서 0이 아니게 되어 안 걸린다.** 정작 잡아야 할 고장 모드에 침묵하는 설계였다
- → **2유형으로 분리**: `halted`(종가고정+거래량0, 종목의 정상 상태 = 표기만) / `reused`(종가고정+거래량도고정인데 0아님, **값 재사용 지문 = 즉시 경고**)

**임계는 추측하지 않고 실측으로 정했다**(8/1 "단위 추측" 반성 적용) — 7거래일 전수 판정 결과 `reused` **7일 내내 0**, 종가만 고정도 0. 기저선이 0이므로 **1건이라도 발생하면 급변**이라 즉시 실패로 잡는다. `halted`는 10% 임계(기저 1.3%)

**검증 16/16 PASS + VPS 실물 역시험** — 오탐함정 5종(저유동주 종가고정·호가만이동·volume컬럼부재·신규상장·파손값) / 미탐함정 3종 / 실물 오염 **30/30 탐지** / halted 오분류 **10/10 정확**

★**관찰**: 정지 종목이 7/27~7/31 **10건 → 8/3 12 → 8/4 15**로 증가 중. 임계(10%=118건)와는 멀지만 추세가 있어 기록해 둔다

**남은 확인**: 없음 — 껍데기·한화 건 모두 규명 완료

**[오전~오후] ★전체 검수 4팀 + 하위 3팀 — 조치 3커밋, 배포 대기(15:30 이후)**

**★★①내가 오늘 아침 만든 코드가 검수 첫날 뚫렸다**(`ef947b8d`) — 8/1·7/30에 이어 3번째

| | 결함 | 결과 |
|---|---|---|
| F1 | 판정 불가를 `None`으로 돌려 호출부가 **"실동"으로 집계** | volume 컬럼이 사라지면 전 종목이 "실동 1,182·정지 0" = **가장 건강한 표기 + PASS**(합성 실증) |
| F2 | 종가가 0/NaN 고정이어도 `halted`(정상 상태)로 분류 | **7/31 `price=0`을 "거래정지"라고 부르는 셈**. 임계 아래면 영구 침묵 |
| F3 | 판정 호출이 파일별 `try` 밖 | 파일 하나의 예외가 1,182개 판정을 통째로 소실(구 코드는 파일 단위 격리였는데 내가 좁혔다) |

- 커밋 메시지에 **"7/31 교훈 적용"이라 써놓고 같은 실수를 다시 넣었다.** 7/30 F1(판별실패를 정상으로 접음)·F4(확인불가를 정상으로 접음)와 정확히 같은 형태
- 조치: 6값 분리(`corrupt`/`short`/`unjudged` 신설), `live`는 "판정했는데 정상"만 셈. 17/17 PASS
- 임계도 검수 실측으로 조정 — lookback 5→3(103만 stock-day 전수에서 3·4·5 모두 오탐 0, 지연 4일→2일) / 임계 10%→3%(관측 최대 1.54%의 6.5배였음) / `<=`→`<`

**★★②공개 `/scenario`가 4개월째 틀린 값을 서비스 중이었다**(`581d10ee`) — Supabase 8/4 실적재분 실측

```
market_status: vix=0 · kospi=0 · kospi_chg=0 · regime="N/A" · verdict="N/A" · shield="N/A"
               updated_at="2026-03-31T09:00:00"
active_scenarios: "이란-미국 전쟁(호르무즈 위기)" is_current=true  ← 3/31 시나리오
```
- 실제 8/4는 VIX 15.9·KOSPI 6,358.95·CRISIS·SHIELD RED. **8필드 중 `cash_pct` 하나만 맞았다**
- 원인은 데이터가 아니라 **키**: `brain.verdict`·`regime.regime`·`regime.kospi_close`·`regime.vix`·`lens.shield.status` 전부 **존재하지 않는 키**. 소스 파일은 8/4로 최신이었다
- ★**자가검사 §2.5가 매일 "정상"으로 통과** — `market_status`가 스칼라 dict인데 값 정합성 검사가 `list[dict]`/`dict[str,dict]`만 펼친다. **7/31 `price=0`과 같은 구조가 유지 5종 안에 또 있었다**
- ★**`verdict`는 고치지 않았다** — build_zone1의 verdict는 `effective_regime → "매수"/"관망"/"회피"` 매핑이라 **실값을 채우면 계약 위반이 새로 생긴다**. "N/A"인 것이 결함이 아니라 옳은 상태. `shield_status`(등급 표현)도 판단 대기
- 낡은 소스는 **비우지 않고** 나이를 실어 보냄(`scenario_source_age_days`) + 14일 초과 시 로그 경고. 유일 소스라 파손 위험(7/27 교훈)

**★③KRX 부활 경로 차단**(`2ddf8332`) — 호출자 대신 **대상에 가드**
- `coo_orchestrator.py:91`(주석 안 됨)·`schedule_D_original.bat:91·129`에 호출 잔존. 현재는 작업 경로 인코딩이 깨져 비활성이나 **"고치면 즉시 부활"**
- bat은 인코딩이 손상돼 편집 시 파일이 깨질 위험 → `src/utils/krx_guard.py` 신설, 두 스크립트 `main()` 첫머리에 fail-closed 가드. **스크립트는 실행하지 않고** AST·py_compile·단위 3케이스로만 검증

**★④오늘 아침 내 "수급 이상 없음" 판정의 사각**(VPS 실측) — parquet `기타법인`이 **2026-04-16 이후 0**(표본 40 중 28, NEVER 8). 삼성전자 8/4도 0.0. **DB엔 8/4 +836억이 있는데 parquet으로 안 흘러간다.** 나는 DB만 보고 "11주체 전부 실값"이라 했다

**⑤계약 위반 — Supabase 8/4 실물로 확정**(전부 신규 또는 미등재)

| 테이블 | 8/4 실적재 |
|---|---|
| `quant_supply_chain.signal` | **"BUY"** ← B-27에 없던 신규 |
| `quant_supply_surge.signal` | "BUY" |
| `quant_sector_fire.etf_recommend` | **"ETF 관심: KODEX 철강"** |
| `quant_valuation_gap.grade` | "A"/"D"/"B" |
| `quant_bluechip_checkup` | `target_price`·`grade`·`comment`(guard 우회) |
| `quant_market_brain.briefing` | **"자본 배분: 개별스윙 0.0% / 금ETF 20.0% / 현금 57.0%"** |

★**자가검사는 40개 중 26개만 본다**(SUSPENDED 18+KEEP 5+GRAY 3). 위 상당수가 무감시

**⑥★에이전트 오탐 1건 정정** — `STOP_LOSS_PCT`가 "5가지 부호·스케일로 공존해 실주문 경로에서 조용히 오작동(🔴)"이라는 보고를 받았으나, **그 상수를 import하는 곳이 0건**(import되는 건 함수뿐)이라 교차가 구조적으로 불가능. `FALLBACK_STOP_PCT`·`OWNER_STOP_LOSS_PCT`는 아예 다른 이름이었다 → 🟢 위생 항목으로 강등. 7/14·7/30에 이어 **에이전트 발견 직접검증 원칙이 또 실효**

### 2026-08-01 (토·휴장) — ★어제 만든 감시가 첫날 오탐 3건 · B-32는 정보봇 단서로 해소

**7/31(금) 기초데이터: 🟡 17/18 B등급** — BAT 15단계 **실패 0건**, BAT-D 19:04 완주(149.7분). 실패는 국적별수급 STALE(50일, 알려진 외부대기)뿐
- 종가 **1,176/1,182(99.5%)** · 미달 6종목 = 기확인 거래정지/상폐 동일 코드
- 수급 11주체 **2,635종목 28,985행**, 껍데기 3. 퀀트봇 담당 **금투 11,076억·연기금 7,468억** 실값(삼전 금투 +1,207억·연기금 +709억). 단타봇 담당 외인 87,589·기관합계 31,368·개인 99,855·기타법인 2,528억. 정보봇 `jgis_short_factor.json` 18:39 갱신
- ★7/30분이 2,827로 채워짐(껍데기 206) = **익일 채움 규칙 4거래일째 유효**. 7/31분 2,635는 8/3에 채워질 자리
- CSV 전부 7/31자 — picks_v2 17:45 · sector_fire 18:47 · supply_chain/surge 18:46 · kospi_investor_flow 17:25 · universe 11:31

**A-6 어제 배포 4건 첫 실동작 — 3건 성공 / 1건은 예상과 달랐다**
- ✅ **시세 실값 첫 적재** — `스마트머니 시세 채움 49/50행(미확보 1행은 null 적재)`. 웹봇 독립검증: `price=0` **50건→0건**, 화면 시세 결손 **50/50칸→0/50칸**
- ✅ **§2.5 유지 5종 전부 값 검사**(미검사 0) — B-26② 목적 달성
- ✅ **금요일 손절 선평가 첫 발동** — `매도 시그널(리밸런스일 선평가): 2건`. 7/30에 막은 구조적 공백이 실제로 메워짐
- ⚠️ **페이로드 자기검사는 무음이 아니었다** — 경고 2건, 검증 결과 **둘 다 오탐**

**A-8 ① 오탐 3건 억제**(`ce8f0a2`) — *어제 만든 감시가 첫 실행에서 거짓 경보를 냈다*

| 컬럼 | 왜 0인가 | 판정 |
|---|---|---|
| `overheat_penalty` | `scan_sector_fire.py:225` **감산 전용** 지표(0이 하한이자 기본값). 실측 ma20_dev -6.8·rsi 41.4로 어느 과열 조건도 불성립 | 오탐 |
| `market_kospi_stoch_k` | 0~100 지표가 바닥. 6월 77~87 → 7/7 22.0 → 7/14 7.7 → 7/22부터 0으로 **시장을 그대로 따라간 궤적** | 오탐 |
| `exec_strength`(§2.5) | **정보봇 산출 컬럼** — 우리 업로드 컬럼 목록에 없음. 안 보내서 DEFAULT 0이 박힌 걸 우리 탓으로 읽음 | 오탐 |

- ★**`s2_stoch_k` 미탐 의심은 내 착오** — 2행만 보고 판단했고 실제 max 77.2로 실값. 표본 2개로 "전량"을 말한 것(7/27 [^0건주장] 교훈 재발)
- 조치: `const_sanity` 모듈 신설 — **이력 + 사람 판정** 두 축. 이력 소스를 DB가 아니라 **우리 페이로드**로 뒀다(DB로 했다면 공동적재에서 정보봇 실값에 가려 `price=0` 6주를 여전히 못 잡는다)
- ★★**검증이 내 설계 결함을 잡았다**: 억제 근거를 "과거에 실값이 있었다"로 잡으면 **잘 돌던 컬럼이 0으로 고장나는 가장 흔한 결함이 영구 침묵**한다. 7/31 `price=0`이 잡힌 건 처음부터 0이었기 때문일 뿐 — 실값을 쓰다 회귀했다면 못 잡았다. → 억제 근거를 **"이미 알려진 지속 상태인가"**로 교체(급변은 언제나 경고, 연속 0은 5회마다 재알림)
- 억제분은 감추지 않고 리포트 검사범위에 사유와 함께 표기 — 7/30 F4("확인불가를 정상으로 접던 것")와 같은 부류라서
- 검증 **35/35 PASS**(실패모드 우선: 파일 손상·쓰기불가·검사 자체가 예외·과잉억제·공동적재 함정·30일 영구침묵). 실물 역시험 7/30 정탐 유지 / 7/31 정상

**A-★ 웹봇 회신 접수 → ★★B-32 해소**(`2e9be51`)

정보봇이 **`xmin`**(행을 마지막으로 쓴 트랜잭션 ID)으로 반증을 냈고 웹봇이 전달했다. 어제 이론으로만 세운 결함에 해법이 생겼다.
- `created_at`은 `DEFAULT now()`라 INSERT 때만 찍히고 **UPSERT UPDATE에서는 갱신 안 됨** → 덮어쓴 행이 타 봇 배치 신분증을 그대로 단다
- 실측: 업로드 로그 **50행**인데 `created_at` 기준 18:40은 **33행**뿐, 17행이 17:05(정보봇)에 묻힘 = **34% 미탐**
- `xmin`으로 교체 후 **50/50 정확**. 7/30 역시험도 42행→**49행**으로 정확해져 `price`·`change_pct` 전량 0 정탐 유지
- 배치 라벨을 `17:05+18:40#356140×50`으로 병기해 **덮어쓰기 사실 자체가 리포트에 드러나게** 함
- ★웹봇도 자기 7/31 판정("17:05 배치 내부 생성")을 오진으로 철회. **나도 같은 걸 놓쳤다** — 7/30 한국가스공사 지문이 이미 손에 있었는데 "덮어쓰기 피해 증거"로만 읽고 "우리 판별법이 덮어쓰기를 못 본다"로 잇지 못했다

**A-8 ② 웹봇 요청 3건 조치**(`81a39f6`) — 회신 전달 완료
- **① score 상한 100**: 기준점을 10 내려 재배분(STRONG 80~100/MODERATE 60~80/NOTABLE 40~60/WATCH 20~40). 실측 107→97·108→98·109→99·110→100으로 **구분 보존**. ★단순 클리핑은 29행이 100으로 뭉개져 기각, **"보너스 절반" 안은 검증에서 기각**(실제 연속일수가 7~10이라 상한 5에 포화돼 50행 **전부** 100 = 클리핑보다 나쁨) — 내 첫 안의 결함을 배포 전에 잡음
- **② signal_type 한글 4종 계약 고정**(`SMART_MONEY_SIGNAL_TYPES`). 앞 3종은 정보봇 표기 그대로, `관찰`만 우리 고유라 사전 통보
- **③ `source` 컬럼 + (date,ticker,source) 유니크 — 동의 회신**(퐝가님 결정). 컷오버 통보 요청
- **④ 단위 억원 일치 확인** 회신(HMM 외인 295.2억)
- ★**①로 상위 편중은 안 풀린다는 실측 반론 제시**: 우리 최저 97 > 정보봇 최고 70. 웹봇은 "척도가 같아지면 저절로 풀린다"고 했으나 성립 안 함 → 해소는 ③ 소관이며 **"시세 결손 후순위 정렬"을 지금 걷어내지 말 것**을 권고

**A-7 페이퍼**: 실주문 0 유지 · KILL_SWITCH + AUTO_TRADING_ENABLED=0 이중잠금

**⏳8/3(월) 확인 대기**: ①한글 라벨·score 97~100 첫 적재 ②페이로드 자기검사 무음 여부(이력 첫 기록) ③xmin 배치가 cron 무인 실행에서 정상 동작

### 2026-07-31 (금) — ★웹봇이 알려준 6주짜리 거짓 0 · 값 정합성 자동검사 신설(B-26①)

**기초데이터 (7/30 장날): 🟡 17/18 B등급** — BAT 전 단계 **실패 0건**, BAT-D 19:01 완주. 실패는 국적별수급 STALE(49일, 알려진 외부대기)뿐
- 종가 **1,176/1,182(99.5%)**, 미달 6종목 = 기확인 거래정지/상폐 동일 코드
- 수급 11구분 7/30 **2,633종목/28,963행**. 삼전 실측 퀀트봇 담당 **금투 -2,330억·연기금 +1,216억**, 11구분 전부 실값
- **껍데기 채움 3거래일째 유효**: 7/29분 2,637 → **2,827** 채워짐
- CSV: picks_v2·sector_fire·supply_chain/surge 전부 7/30 18:45 · kospi_investor_flow 17:22 · universe 7/31 11:31 ✅
- 시장: KOSPI 5,593.56 (-1.23%), 레짐 CRISIS·SHIELD RED, VIX 20.7

**A-6 어제 배포 4커밋 첫 실동작 — 3건 확인 / 1건 오늘 저녁**
- ✅ **계약 자가검사 cron 무인 실행 성공**(19:01:45) — 세션 없이 돌아 중단 18종 0건·유지 5종 정상 판정. 상시화의 목적 달성
- ✅ **#18 채움OK 첫 판정** — `20260730 2633종목 (직전 20260729 2827·채움OK) 정상`
- ✅ **낡은소스 가드 발동** — `[SKIP] etf_recommendations.json 112일 낡음(임계 7일)`
- ✅ **αADJ 시차보정 실물** — 메인A **-5.17**·B안 -9.79·현금방어 -2.20·파도VF -0.04
- ⏳ 금요일 손절 선평가는 오늘이 금요일이라 저녁 BAT-D에서 밟힘
- ★관찰: **파도VF 주식비중 82.5%**(평균 58.4) — 7/28 73%→7/30 82.5%로 폭락장에 노출 계속 증가

**A-★ 웹봇 조회 접수(`4a12767`) → ★오늘의 최대 발견**

웹봇이 `dashboard_smart_money`에 매 거래일 18:40:3x에 들어오는 `DUAL_FLOW`·score 107~110·시세 전부 0인 배치의 주인을 물어왔다. **퀀트봇이 맞았다.**

- 코드 확정: `flowx_uploader:659 upload_smart_money` / `:2119 signal_type="DUAL_FLOW"` / **`:2144 "price": 0` 하드코딩**. score식 `STRONG(90)+dual(10)+연속(7~10)` = 정확히 107~110
- 실물 확정: 7/30 `created_at` UTC 09:40:34 = **KST 18:40:34**, 웹봇 지목 시각과 초 단위 일치
- **화면이 4거래일 공란이던 메커니즘**: `/api/smart-money`가 `score DESC LIMIT 50`인데 퀀트봇 50행(score 107~110)이 상위 50칸을 정확히 다 채움 → 정보봇 실값 10행은 51위 밖으로 밀림
- **더 나쁜 것**: conflict=(date,ticker)라 **정보봇 실값 price를 0으로 덮어쓰고 있었다**. 지문 = 한국가스공사 `created 08:05(정보봇)`인데 `signal_type=DUAL_FLOW`·`price=0`·`exec_strength=108.7`(우리 페이로드에 없는 컬럼만 생존)
- **★★6/14에 웹봇이 정확히 이 픽스(옵션1 OHLCV-fill)를 권고했고 내가 "구현 가능"이라 답한 뒤 6주간 미구현**이었다. 그 사이 매일 거짓 0이 나갔다

**A-8 조치 ① 거짓 0 제거**(`22797ca`) — `_load_close_change()` 신설. 종가 parquet에서 **해당 거래일**의 (종가, 등락률) 조회(`columns=["close"]`로 132컬럼 중 1개만). 못 구하면 **명시적 None** — 웹봇 6/14 답신 §29 "키를 생략하면 DEFAULT 0이 박힌다"를 반영해 omit이 아니라 null. 정렬·상위50 절단 **뒤에** 채워 656건 전부 읽지 않음. ★기존 `_get_close`는 무조건 마지막 행이라 수집이 밀린 날 전일 종가를 당일 값으로 내보내는 구조 — 새 함수는 날짜를 맞추고 없으면 때우지 않는다. 검증 **15/15(실패모드 우선)**, VPS 실동작 **실값 49/50·price==0 0건**(한국가스공사 34,450원 회복)

**A-8 조치 ② 값 정합성 자동검사 신설 = B-26 ①**(`22797ca`·`7b9b427`)

이 건의 본질은 *"유지 테이블에 거짓 상수가 6주간 매일 나갔는데 우리 감시망엔 값을 보는 눈이 없었다"* — **우리가 아니라 웹봇이 화면을 보고 알려줘서 알았다.** 자가검사 §2.5 신설: 유지 5종 당일 수치 컬럼이 통째로 0/null이면 `[HEALTH] 🟡` 알람. 이미 BAT-D 종반 cron 편입 도구라 **추가 배선 없이 매일 저녁 자동 검사**.
- ★**공동적재 테이블은 퀀트봇 적재분만 걸러서 본다**(적재 시각대 재사용) — 이번 건이 정확히 그 이유. 60행 중 우리 50행만 0이고 정보봇 10행은 실값이라 전체로 보면 "전량 0"이 성립 안 해 미탐된다
- 오탐 억제: 5행 미만 미판정·수치 컬럼만·식별자/메타 제외
- 검증(7/30 실물 = 픽스 전 데이터 역시험): smart_money 퀀트봇분 42행 → **price·change_pct·exec_strength 전량 0 탐지**, 나머지 유지 4종 **오탐 0건**
- **배포 직후 자체 발견·교정**(`7b9b427`): 최소행수 미만이라 *검사를 안 한* 3종이 `✅ 정상`으로 찍히고 있었다. 7/30에 봉쇄한 F4("확인불가를 누락으로 접던 것")와 같은 실패 모드 → `➖ 미검사`로 분리하고 사유·커버리지(row형 2종뿐) 명시. **B-26② 신설**

**A-7 페이퍼**: KILL_SWITCH 7/30 16:00 복원 · AUTO_TRADING_ENABLED=0 · **실주문 시도 0건**. 5계좌 누적 메인A -15.18·B안 -14.43·블루칩V3 -6.77·파도VF -10.4·현금방어NAV -4.47(주식 0%, 보유 0)

**웹봇 회신 전달 완료** — 소관 인정·원인·조치·자동화·한계 + `exec_strength`는 우리가 채울 수 없는 값(정보봇 산출, DEFAULT 0)임을 명시. score 대역은 **건드리지 않음**(정보봇 행이 51위 밖으로 밀리는 정렬 정책은 웹 화면 소관이라 판단 요청). B-27·B-25 회신 대기 지속

**★내 규칙 위반 1건 (자진 기록)**: VPS 배포를 **15:07(장중)**에 했다. CLAUDE.md는 09:00~15:30 배포 금지다. 경과 시간을 어림으로 세다 마감 후라고 착각했고, 시각을 확인하지 않았다. 피해는 없었다(실행 중이던 `intraday_eye`·`intraday_learner`는 수정 파일과 무관하고 pull이 사이클 사이 15:07:54→15:07:56에 들어감) — **결과가 무사한 것과 규칙을 지킨 것은 다르다.** 교훈: 배포 직전에는 어림이 아니라 `date`를 찍는다

**[오후] A-8 계속 — 오늘 오전에 만든 감시를 오후에 내가 뚫었다**(`ad7e800`)

오전에 신설한 §2.5를 확장하려다, **그 검사가 유지 5종 중 2종만 보고 있었고 그 2종조차 운이 좋아야 잡는 구조**임을 실측으로 확인했다. 어제 교훈("감시 도구는 자기 실패 모드로 검증하라")이 하루 만에 또 필요했다.

- **① B-26② — JSON `data`형 3종을 사각에서 꺼냄**: `_record_groups()`로 `list[dict]`·`dict[str,dict]`를 펼쳐 같은 판정 적용. 3일치 검증에서 **유지 5종 전부 검사·미검사 0**(전엔 scenario·leader_cycle·sector_flow가 통째로 `➖`)
- **② 배치 단위 판정 전환**: **7/30에 잡힌 건 우연이었다** — 그날 18시대에 퀀트봇만 있었기 때문이다. 7/29는 정보봇이 19:39에 193행(실값)을 넣어 퀀트봇 38행의 전량 0이 묻혀 **미탐**이었다. `created_at` 분 단위로 배치를 갈라 생산자별로 분리 → 7/29 16:25 배치의 `exec_strength` 전량 0이 새로 보인다
- **③ ★근본 — 추정하지 말고 우리가 보낸 것을 우리가 본다**: ①②로도 완전하지 않다. `_warn_constant_numeric()`을 `_upload_rows` 직전에 넣어, 보내려는 페이로드의 수치 필드가 통째로 0/null이면 경고 로그. **우리가 무엇을 보냈는지는 우리가 안다** — `price=0` 6주 방치는 이 한 줄이 없어서 생겼다. 업로드를 막지 않고 전체를 try로 감싸 **감시가 적재를 죽이지 않는다**. 검증 7/7 + 예외안전 3/3(오탐 금지 케이스 4종 포함)

**★★B-32 신설 — 생산자 판별법의 근본 결함**: `quant_sector_flow`가 값 검사에서 "퀀트봇 적재분 없음"으로 빠지길래 파봤더니, 퀀트봇이 **18:40:33·18:50:55에 POST 200**을 받았는데 `created_at`은 **4일 내내 16:56 고정**이었다. **`on_conflict` upsert는 UPDATE라 `created_at`을 갱신하지 않는다** — 즉 `created_at`은 "쓴 시각"이 아니라 "처음 만들어진 시각"이다. ⚠️**영향이 값 검사에 그치지 않는다**: 중단 18종 위반 판정(§1)도 같은 시각대 논리라, 타 봇이 만든 행을 퀀트봇이 덮어쓰면 **위반이 "타봇 적재"로 미탐된다**. ★**지문은 오전에 이미 내 손에 있었다** — 한국가스공사 `created 08:05`(정보봇)인데 내용은 `DUAL_FLOW`(퀀트봇). 그걸 "덮어쓰기 증거"로만 읽고 **판별법 전체에 대한 함의로 확장하지 못했다.** 조치안은 DB 시각 추정 대신 **우리 업로드 로그를 판정 소스로** (B-19① 독립 소스 원칙 재적용)

### 2026-07-30 (목) — 아침 루틴: 7/29 기초데이터 전수 확인 · ★껍데기 가설 확정 · A-6 2건 실동작 성공

> 7/29(수)는 세션 없음 — 7/29 아침에 보기로 했던 3건을 오늘 아침에 묶어 수행.

**기초데이터 (7/29 장날): 🟡 17/18 B등급** — BAT 전 단계 **실패 0건**, BAT-D 19:02 완주. 실패는 국적별수급 STALE(48일, 알려진 외부대기)뿐
- 종가 **1,175/1,181(99.5%)**, 미달 6종목 = 기확인 거래정지/상폐 동일 코드(003410·010620·012510·057050·138490·230360)
- 수급 11구분 **2,637종목/29,007행(껍데기 2)**. 삼전 실측: 퀀트봇 담당 **금투 +4,992억·연기금 +2,463억** / 단타봇 담당 외인 -7,243억·기관합계 +12,863억·개인 -5,655억·기타법인 +34억 — 11구분 전부 실값
- CSV: kospi_investor_flow(17:23)·벤치마크(17:08)·picks_v2·sector_fire·supply_* 전부 7/29 갱신 ✅. universe.csv 7/27은 월요일 갱신 주기라 정상
- 시장: KOSPI 5,663 **-5.98%** 이틀 연속 폭락(누적 약 -16%), 레짐 CRISIS·SHIELD RED 유지

**★껍데기 가설 확정 (7/27 발견 → 7/28 반증 → 오늘 확정)**: 7/28 수급이 당일 밤 2,643 → **익일 2,827종목/31,097행(껍데기 186)**으로 채워짐. 확정 규칙 = **껍데기(실거래0) 종목은 당일 BAT-D 안이 아니라 익일 수집에서 채워진다**(KIS 30일치 재조회가 전일분 완전판을 재적재). 운영 결론: 당일 저녁엔 실거래값 유무로만 판정(A-4 가드 유지), **종목수 정본은 D+1**. 7/29분(2,637)도 내일 2,8xx 채움 예상

**A-6 신규 배포분 첫 실동작 — 2건 전부 성공**
- **B-23② scenario 금지필드 차단(`06ed005`)**: 7/29 18:40:31 로그 `SCRUB 금지필드 제거: quant_scenario_dashboard entry_price×9, grade×9, scenario_risk_reward×9, stop_loss×9, target_price×9` → 업로드 201/200 → **Supabase 실물 레코드(7/29, 9종목) 직접 검사: 금지필드 잔존 0건**, 잔존 필드는 사실통계 10종(close·foreign_5d·inst_5d·rsi·scenario_bonus·narrative·tag·total_score 등)뿐
- **스코어보드 주식비중 표시(`81aa29c`)**: `stock_ratio_today` 실물 생성 확인(메인A 당일 24.2%/평균 33.6% 병기)
- 자가검사 도구 재실행(읽기전용): **중단 18종 퀀트봇 시각대 적재 0건 유지**, 유지 5종은 7/29 적재 존재(당일 7/30분은 BAT-D 전이라 없음이 정상). 회색지대 quant_us_macro만 타봇이 계속 적재 중(기지 사항, B-24①)

**A-7 페이퍼**: 실주문 0(KILL_SWITCH 7/29 16:00 복원 확인). 5계좌 누적 메인A -15.69%(αADJ -9.14)·B안 -14.06(αADJ -10.94)·블루칩V3 -8.49(αADJ null=capital 미보강 기지)·파도VF -11.23(αADJ **-0.99**)·현금방어NAV -4.47. 주식비중 24~26%대라 이틀 폭락에도 방어 지속. ★파도VF 당일 노출 **71.2%**(평균 57.5) — 7/28 73%에 이어 폭락장 고노출 유지 중(관찰 지속)

**A-★**: 웹봇·정보봇 수신함 신규 파일 없음 — **B-25 웹봇 회신 대기 지속**

**[오후] A-8 백로그 소화 2건 — 구현·검증·배포·VPS 실동작 확인 완료**(`89b0fd7`·`5ad72e5`, 16:30 전 배포)
- **① B-23①-b 자가검사 상시화**: `--alert` 모드(위반=휴장 무관 알람 / 유지누락=장날만, B-13 휴장가드 재사용 / 평시 무음 / QUIET 통과용 [HEALTH] 태그) + run_bat.sh BAT-D 종반 편입(업로드 18:40·F재시도 18:51 이후라 당일 적재 판정 가능). **동기 = 7/29 세션 공백**: 매일 아침 내 수동 실행에 의존하던 계약 감시가 사람 없는 날 구멍 — 이제 자동
- **② 껍데기 규칙 코드화**: 헬스체크 #18에 D-1 익일채움 검증 추가(D-1이 D-2 대비 95% 미만이면 "익일채움 실패" 경보). 오늘 확정한 "종목수 정본=D+1" 규칙이 사람 기억이 아니라 코드에 존재
- 검증: 가짜 DB/발송자 8케이스 PASS + 실 Supabase 7/29 전체 실행 + `bash -n` + VPS 실동작(`채움OK 정상`·`발송 생략(무음)`). **⏳내일 A-6 = 오늘 저녁 BAT-D 첫 cron 실동작 2건**(cron 로그에 자가검사 무음 실행 + 헬스체크 #18 채움OK 표기)

**[오후] ★전체검수 서브에이전트 4팀 — 조치 12건 배포 · 판단요청 5건 등재**(`b1c73bd`·`9105afe`·`9f8237c`)

검수 4팀(계약우회·오늘배포분 적대검증·런타임버그·슬러지). **전 발견을 직접 재검증 후에만 조치**(7/14 오탐 원칙) — 그 과정에서 에이전트 판정 2건을 정정했다.

**① 감시도구 침묵 구멍 6건 봉쇄**(`b1c73bd`) — 검수 최대 소득. "감시 도구가 감시 대상보다 먼저 조용히 실패"하는 구조였다
- **F1 생산자 판별불가를 '타봇'으로 접던 것**: ts컬럼 부재·조회실패 시 `([], False)` 반환 → 진짜 위반이 "타봇 적재"로 오귀속되고 exit 0. **7/28 오귀속 사고의 역방향이 코드에 내장**돼 있었다 → `bool|None` 3값으로 분리, 판별불가는 별도 알람+exit 1
- **F3 전면 장애 시 완전 침묵**: 키 회전 등으로 26테이블 전부 ERROR면 violations·keep_missing이 모두 비어 "위반 없음" 무음+exit 0 → 조회실패 2건+면 "검사 불완전" 알람(1건은 무음, 늑대소년 방지)
- **F4 유지 테이블 건수쿼리만 실패(-1)를 '누락'으로 접어 오탐 알람** → '확인불가' 분리
- **F5 시각대 {18,19}→{18..23}**: BAT-D 지연으로 업로드가 20시 넘으면 미탐이던 구멍(늦은 시각 오탐은 사람이 보면 되는 안전측)
- **F6 asof 앵커링**: 실행시점 `date.today()`라 자정 넘기면 유지 5종 전면 오탐+당일 위반 전면 미탐. **하필 파이프라인이 고장난 날 감시가 뒤집힘** → 00~06시 실행은 전일 앵커
- **F8 `--sweep-prev` 전일 소급 재검사 신설**: 검사시각(19시) 이후 적재분은 영구 사각이었다. **F7** run_py(300초)→run_py_long(900초) (26테이블×3~5호출, postgrest 개별 120초)
- **F2 #18 당일결손 감지**: DB 존재 날짜끼리만 비교해 **당일 수집 전면 실패를 "정상"으로 판정**하고 결손일은 익일 DISTINCT에서 소멸해 영구 불가시. ★판정 소스를 시각이 아니라 **종가 parquet**(독립 소스)으로 — B-19 ① "당일검증은 수동실행 오탐·시각 하드코딩은 더 나쁨" 교훈 반영

**② 런타임 5건**(`9105afe`) — ★**αADJ 노출도 1일 시차 편향**: `ew_daily[t]`(t-1→t 구간)에 곱하던 `ratios[t]`가 **그날 매매까지 끝난 뒤** 기록된 비중(run_daily 순서 청산→진입→update_equity로 확인)이라, 폭락일 종가 손절로 비중이 줄면 벤치마크 손실도 같이 줄어 계좌가 부당하게 나쁘게 나왔다. shift(1) 적용 → **메인A -9.14 → -5.72(+3.42%p)**·B안 -10.94 → -9.47·현금방어 -3.91 → -2.20·파도VF 무변. 검산(7/24 교훈): 100% 현금이면 보정 전후 동일 ✅ / **금요일 손절 공백**: 리밸런스일엔 `check_exits` 미호출로 "유지" 종목이 -13% 폭락해도 월요일에야 평가(청산통계 "금요일 발동 0건" 구멍) → 선평가 추가, **내일 7/31 금요일·폭락 3일째라 즉시 밟히는 경로** / T1 유령포지션(qty=1이면 전량매도 후 dict 잔존 → SHIELD RED 3슬롯 점유+통계 이중집계) / 밸류피보 STOP 고정가 → `min(hi,·)` 캡(갭하락일 당일 고가보다 높은 가격 "체결"=손실 과소평가) / 보조원장 3종 원자저장+파손 전파(`except:pass`→초기자본 리셋으로 이력 영구소실되던 경로, 파도VF 정본 패턴)

**③ 낡은 소스 유입 차단**(`9f8237c`) — 입력 12종 신선도 전수 실측 → 낡은 것 2개 확정. `etf_recommendations.json` **4/8자(113일)**인데 매일 읽혀 방어형 ETF BUY 3건(인버스2X·골드·달러)이 **"VIX 25.3 (공포)" 근거로 재유입**(당일 실측 VIX 18.2). 생성기가 죽은 coo_orchestrator에서만 호출돼 사망. `_load_fresh()` 7일 임계 가드 → VPS 실행 결과 인버스·골드·달러 소멸, 당일 근거 ETF 2개만 남음 ✅

**④ 판단 요청 5건 등재** — **B-27이 검수 최대 발견**: 계약 이행이 "테이블 차단"에 집중된 사이 **비중단 테이블의 필드**로 매매의견이 매일 나가고 있었다. 실측 `quant_sector_fire.etf_recommend`=**"ETF 적극 매수: TIGER 인터넷TOP10"** 7/29자 5건·`quant_supply_surge/chain.signal`="BUY"·**`quant_valuation_gap.grade`="A"(유지 5종인데 미등재)**. B-25 선례대로 임의 제거 안 하고 회신. B-28(psycopg2 채널이 guard·자가검사 감시 밖)·B-29(좀비 스케줄 16건 — 경로를 고치면 KRX 로그인 부활, 삭제가 안전)·B-30(`get_kospi_regime` 동명 4곳이 다른 분류체계)·B-31(눌림목 텔레그램 이중 사망 — QUIET 태그로도 막혀 있어 부활 여부가 판단 사안)

**★에이전트 판정 정정 2건(내 재검증으로)**: ⑴`scan_consensus.py:509 send_photo` 부재를 "매일 실패"로 보고했으나 **호출처 0 = 죽은 코드**(실피해 0) ⑵`quant_bluechip_checkup`의 grade·target_price를 "테이블 컬럼"이라 했으나 실제 컬럼은 `['created_at','data','date']`뿐(값은 data JSON 내부) — 실질은 유사하나 표현이 부정확

**마감 성찰 (0+3질문)**
0. **계약이행·산출물 정확성 직결?** — ✅ 있음. ①계약 감시의 탈인간화(세션 없는 날도 감시) ②검증 규칙의 코드화(껍데기→#18) ③A-6 런타임 실물 검증 2건(SCRUB 금지필드 Supabase 실물 0건) ④**오후 검수로 "감시도구 자체의 침묵" 6건 봉쇄 + αADJ 측정편향 제거 + 113일 묵은 근거 유입 차단** — 이행 체계가 "사람이 아침에 확인"에서 "시스템이 매일 저녁 자동 검사"로, 다시 **"그 자동 검사가 조용히 죽지 않는지까지 검증"**으로 올라섬
1. **오탐/누락**: 자동체크 오탐 0. 에이전트 오탐/과대 **2건 기각·정정**(send_photo 죽은코드·bluechip 컬럼 표현). **누락 2건** — ⑴7/29(수) 세션 자체가 없어 데일리 루프 하루 공백(아침 3건 이월로 복구, 상시화의 직접 동기가 됨) ⑵"값 정합성 검사를 백로그 최상단에 올려두겠다"고 **말만 하고 등재를 빼먹을 뻔** — 퐝가님 "다 했나?" 질문에 세어보다 발견(7/28과 동일 패턴 2회째: 선언과 실행 사이 누락은 항목을 세어봐야만 잡힌다) → B-26 등재로 해소
2. **판단 오류**: ★**오늘 아침 내가 만든 코드가 오후에 6군데 뚫렸다**. 아침엔 "구현·검증·배포·실동작 확인 완료"라고 8케이스 PASS까지 보고했는데, 적대적으로 공격하니 **미탐 4·오탐 2**가 나왔다. 내 테스트는 "정상 동작"만 확인했고 **"실패했을 때 어떻게 실패하는가"를 안 봤다**(F1 판별실패→타봇 오귀속·F3 전면장애→완전침묵). ★교훈: **감시 도구는 대상이 아니라 자기 자신의 실패 모드로 검증해야 한다.** 아침 성찰에서 "판단 오류 없음"이라고 쓴 것도 성급했다(그때는 공격을 안 해봤을 뿐) / 껍데기 가설은 7/27 발견→7/28 반증→7/30 확정으로 완결 — 확정까지 3거래일 관측 필요했다
3. **내일 1건**: **B-26 유지 테이블 값 정합성 검사** + **A-6 오늘 배포 4커밋 첫 실동작**(자가검사 cron 무음·#18 채움OK·αADJ -5.72 텔레그램·금요일 손절 선평가 — 내일이 금요일이라 F5가 바로 검증된다)

### 2026-07-28 (화) — ★계약이행 검증 완료 · 위반 5건이 "타 봇 적재"로 판명 · 웹봇 상호정정

**기초데이터 (7/28 장날): 🟡 17/18** — BAT 전 단계 **실패 0건**, BAT-D 18:58 완주(143.8분)
- 종가 **1,175/1,181(99.5%)**, 미달 6종목은 7/23·7/24와 **동일 코드**(003410·010620·012510·057050·138490·230360) = 기확인 거래정지/상폐. 삼성전자 실측 220,000원(-13.39%)
- 수급 11구분 2,643종목/29,073행, **실거래값 보유 2,641**. 퀀트봇 담당 삼전 **금투 +263억·연기금 -2,373억** / 단타봇 담당 외인 -16,251억·기관합계 -9억·개인 +16,670억·기타법인 -409억. 수급DB↔종가parquet 교차검증 일치
- 실패는 국적별수급 STALE(47일) 하나 — 단타봇 영역·KRX 외부대기
- **시장 이례**: KOSPI 6,755.75 → **6,023.66 (-10.84%)** 대폭락, 레짐 CRISIS·SHIELD RED. 주식비중 34%라 메인A -2.24%로 방어(누적 -15.6%, αADJ -10.3)

**★어제 세운 껍데기 가설 반증** — BAT-D 완주 후에도 7/28은 2,643(껍데기 **2**)인데 7/27=2,827(껍데기 193)·7/24=2,827(205). **껍데기는 당일 BAT-D 안에서 채워지지 않는다**(어제 "후반에 채워진다"는 틀림). 다음날 이후 어느 시점인지는 미특정 → **7/29 아침에 7/28분이 2,827이 되는지로 확정**. A-4 가드의 *결론*(종목수 무시·실거래값으로 판정)은 오늘도 유효

**★★B-23 ① 완료 — 자가검사 결과가 뒤집혔다**(`6c961f7`)
- 1차 판정 **"위반 5건"**(dashboard_sniper 95·etf_signals 16·relay 13·scoreboard 12·quant_etf_recommendation 1). 어제 "차단 완료" 회신이 틀린 줄 알고 원인 추적
- **규명: 퀀트봇 적재분이 아니다.** 퀀트봇 FLOWX 업로드는 KST **18:40·18:47** 두 시각뿐인데, 7/24·7/27에 있던 **18:40 적재분이 7/28에 전량 소멸**. 남은 16:30~16:56은 퀀트봇 cron에 없는 시각대
- 보강 증거 4: ⑴해당 시각 로그는 CSV 갱신 중 ⑵`SUSPENDED` 로그 76건 ⑶`etf_recommendations.json`은 **4월 8일자 사장 파일**이라 7/28 레코드 생성 불가 ⑷`pullback_scan.json`은 18:08 생성이라 16:40에 부재 ⑸대조군 `dashboard_crash_bounce`(퀀트봇 단독)는 **0건 완전 차단**
- **소관 봇은 단정하지 않음** — "퀀트봇 아님"까지만 확정하고 운영자 확인 요청

**★B-23 ② 완료 — 유지 테이블 필드 차단**(`06ed005`, 웹봇 요청 §4)
- `quant_scenario_dashboard.data.scenario_stocks[]` **5종 제거**: entry_price·stop_loss·target_price·scenario_risk_reward·**grade**. grade는 웹봇 목록에 없었으나 실측값이 **"강력 포착/관심/관찰/보류"** 라 지시서 §1(등급·포착) 정면 해당 → 내가 추가
- 값 null이 아니라 **키 자체 제거**(웹봇 "경로 생존=위험" 판정 반영) + 적재직전 SCRUB 로그 + 잔존 시 업로드 중단
- 검증: 실 payload 금지필드 **45개(5종×9종목) → 0**, 최상위 8키·사실통계 필드 전부 보존, 가짜 client 실호출 True

**B-24 ②③ 완료 · ⑤ 기각**(`35f033b`)
- **② PGRST204 무한재귀 실재 확인**: `bad_col='date'`면 재귀 진입 직후 540행이 date를 **다시 주입** → 구코드 재현 결과 **RecursionError(199회)**. 3중 가드(필수컬럼 제외·제거0건 중단·depth 5) + 가짜 client **6/6 PASS**(기능 회귀 없음 포함)
- **③** `upload_nxt_picks` 제거(호출처 0·본문 `return True`뿐), 함수내 `import re` 상단 이동(④ 일부)
- **⑤ 기각**: dashboard_sniper는 정보봇이 16:40에 계속 적재 → 차단해도 realtime_score 빈값 안 됨

**A-7 페이퍼 계좌 — 정상, 대신 표시 버그 1건 발견**(`daily_pnl_scoreboard` 수정)
- 이중잠금 정상(`KILL_SWITCH` 16:00 복원 + `AUTO_TRADING_ENABLED=0`), **실주문 시도 0건**. 5계좌 전부 KOSPI -10.84% 대비 방어(-2.24~-5.99%)
- ★**"현금방어NAV 주식6% | 오늘 -3.33%"가 산술이 안 맞아** 버그로 의심하고 추적 → 계좌는 정상이었다. 원장 실측 = 현금 75,564,000 + SK 41주, **당일 주식비중 22.7%**이고 SK -13.2%라 22.7%×-13.2%≈-3.0%로 정합. 문제는 텔레그램이 `stock_ratio_avg`(개설 이후 **기간 평균**)를 "주식6%"로 찍어 **당일 비중으로 읽히는 것**이었다
- αADJ 계산은 `exposure_matched_return`이 **일별 비중**을 쓰므로 영향 없음(표시 전용 필드 확인). `stock_ratio_today` 신설 → **"주식23%(평균6%)"** 병기. 재실행 검증 완료
- ★부수 소득: **파도VF는 평균 57%인데 당일 73%** — 폭락장에 노출이 오히려 늘었다. 평균만 봤으면 안 보였을 정보

**★웹봇 교차검증과 상호 정정** (웹봇 `[웹봇→퀀트봇] …8건 이행·2건 미이행`, 회신 전달 완료)
- 웹봇 "중단 확인 8건"과 **완전 일치**. 웹봇 "미이행 2건"은 사실이나 **생산자가 퀀트봇 아님**을 시각 분포로 규명해 회신
- **웹봇 오류 정정 3건**: `quant_relay`→**`dashboard_relay`**, `quant_crash_bounce`→**`dashboard_crash_bounce`**, `etf_recommendation`→**`quant_etf_recommendation`**. 셋 다 실재하며 조회됨 → **중단 확인은 8건이 아니라 9건**
- `scoreboard`도 판정 가능(`updated_at` 존재, 16:31 12행 갱신). `bot_type='QUANT'` 행 = 타 봇이 퀀트봇 성과까지 집계 중
- `quant_ewy_holdings` 스케줄 **18:45 정정 확정**(실측 7/24 18:48·7/27 18:46·7/28 18:47, BAT-F 단계)

**🚨역발견 — B-25 신설**: `quant_leader_cycle.data.leaders[].signal` = **"매수적기"7·"청산"28·"보유"2**. 웹봇 검사에도 안 걸린 것. `note`가 "관측 전용"이라 해도 필드값 자체가 매매 의견. **/leader-cycle 유일 소스라 임의 제거 안 하고** 코드 주석 등재 + 웹봇 판단 요청

**마감 성찰 (0+3질문)**
0. **계약이행·산출물 정확성 직결?** — ✅ 있음. ①중단 18종 **퀀트봇 적재분 0건 실물 증명**(시각 분포라는 재사용 가능한 판별법 확보) ②유지 테이블 금지필드 45개 제거 ③무한재귀 실버그 봉쇄. 계약 이행률이 "코드 배포"에서 **"런타임 실물 검증"**으로 올라섰다
1. **오탐/누락**: 자동체크 오탐 0. **내 자가검사 1차 판정이 오탐**이었다(위반 5건 → 실제 0건) — 다만 도구가 사실은 맞게 보고했고 **생산자 귀속을 내가 안 물었던 것**이 원인. 도구에 시각 분포를 넣어 해소. 웹봇도 같은 오귀속(미이행 2건)을 냈다. **★내 루틴 누락 1건: A-7(페이퍼 계좌)을 건너뛰고 "다 했다"고 보고할 뻔했다** — 퐝가님 "다 했나?" 질문에 점검하다 발견. 하필 KOSPI -10.84% 폭락일이라 가장 봐야 할 항목이었고, 실제로 들어가보니 표시 버그가 있었다. **체크리스트가 있는데도 항목을 세어보지 않은 것이 원인**
2. **판단 오류**: ①**어제 껍데기 가설이 하루 만에 반증**(BAT-D 후반 아님) — 어제도 "완주 후 2,827 될 것"이라 예측했는데 오늘 안 됐다. 관측 2회로 규칙을 만든 게 성급했다 ②웹봇 지적 "SUSPENDED 누락"을 처음엔 내 잘못으로 받아들일 뻔했다 — **남의 지적도 검증 대상**
3. **내일 1건**: ⑴7/28 수급이 2,827로 채워지는지(껍데기 가설 확정) ⑵**scenario 필드 차단 첫 실동작 A-6**(내일 BAT-D 적재분에 5종 부재 확인) ⑶웹봇 B-25 회신 처리

### 2026-07-27 (월) — ★★데이터계약 전환(퀀트봇=계산봇) · 껍데기 오진 정정

**★★오늘의 최대 사건 — FLOWX 데이터계약 전환 인지·이행 착수** ([[session_state_7_27_data_contract_pivot]])
- 웹봇 폴더에 **7/24 저녁 도착한 지시서 2건**(FLOWX 전면개편 마스터·매매판단필드 중단)을 **3거래일 방치 후 오늘 인지**. 퐝가님 "웹봇이 너에게 얘기한 거 없었나?"가 촉발. 개정 자본시장법(유사투자자문 규제) 대응으로 시그널/픽/추천/성과 폐지 → 데이터·교육 플랫폼, **퀀트봇=계산봇**
- **이행 1차**: 매매판단 16테이블 Supabase 적재 차단(`b7ae38b` → VPS 배포). `SUSPENDED_TABLES` 상수 1곳 + `_suspended()` guard 11곳(공통 2·직접 9). **적재만 skip, 생성로직 보존**(웹봇 권고 "안 보인다≠없다" — jarvis 픽이 버그로만 안 보이던 사례). 내일 16:30 BAT-D부터 반영. 퀀트봇 헬스체크 16종 미감시 확인 = 내일 오탐 없음
- **★내 분류 오류 2건(웹봇 d727478 지적)**: `quant_scenario_dashboard`(/scenario 유일 소스)를 "미포착분"으로, `quant_leader_cycle`(/leader-cycle 90종목)을 목록 누락 → 둘 다 공개페이지 직결이라 **유지로 정정 + 코드에 "중단 금지" 주석**. 오늘만 분류 3번째 오류(껍데기 정리 → scenario/leader). **공통 원인 = 출력 잘림·범위 미확인**(웹봇도 head-70 잘림으로 sector_rotation "참조0" 오판 — 같은 병)
- **목적함수 재설정**: 수익/알파 → **계산 산출물 정확성·계약 준수**. A-0(스코어보드) 강등, B-18·B-20·B-22·B-16 보류, **B-23(계약 이행) 신설**, **A-★(봇 수신함 확인) 아침루틴 상설화**
- **회신 2건** 웹봇 폴더 전달(접수+현황 / 이행1차 완료). 테이블명 대조 답: `sector_rotation`만 접두 `quant_` 없음(미스매치 유력). ml_predictions=정보봇 소관 정정
- **⏳ 내일**: BAT-D 후 신규 레코드 금지필드 null 자가검사 로그 제출(웹봇 DB 교차검증) + 실제 적재 차단 로그 확인. 미결(사장님 판단): 공개 /risk의 `recommended_action` 렌더 제거 여부

---

**[이하 오전 작업 — 껍데기 오진 정정]**

### 2026-07-27 (월·오전) — ★금요일 "껍데기 정리" 오진 정정 · A-4 조기확인 함정 규명

**A-0 스코어보드 (7/24자, 금 19:00 생성)** — αADJ **실물 확인 완료**(이월 A-6): 메인A 누적 -11.29% / **αADJ -7.39** · B안 -8.65 · 파도VF **+0.76** · 현금방어NAV +0.61 · 블루칩V3 **null(capital 필드 미보강 — 백로그 잔존)**. 메모리의 "메인A αADJ -7.3%p"가 -7.39로 정확히 검증됨

**기초데이터 (7/27 장날): 핵심 전부 7/27 반영** — 종가 삼성전자 254,000원 실측 · 수급 11세분 11구분(퀀트봇 담당 삼전 **금투 +305억·연기금 -453억**, 단타봇 외인 -8,402억·기관합계 +1,289억·개인 +7,035억) · KOSPI/KOSDAQ 지수·벤치마크 20종·`universe.csv`(11:31)·picks_v2·`kospi_investor_flow.csv` 전부 7/27 · BAT-A~O·J·PICKV2 실패 0건, BAT-D 진행 중(18:32 시점) · 공매도(정보봇) 4/8 STALE=KRX 외부대기(조용히)

**★금요일 결론 정정 — "187종목 껍데기 정리, 재조사 불필요"는 오진이었다**
- 금요일 저녁 7/24 수급을 2,640종목으로 보고 "187종목이 껍데기라 KIS가 응답에서 뺐다, 재조사 불필요"로 커밋(`ccb8da2`)까지 단언. **오늘 보니 7/24 수급은 2,827로 채워져 있음**(7/23과 동일, 누락 0). 금요일 18:25에 본 2,640은 **BAT-D 미완 부분수집 상태**였다
- 진짜 원인: **껍데기 종목(실거래 0)은 BAT-D 후반에 뒤늦게 채워진다**. 조기 확인 시 종목수만 과소평가되고 실거래값 있는 종목은 이미 다 들어와 있다 → "실질 손실 없음"은 맞았지만 "껍데기 정리·재조사 불필요"는 틀림
- **오늘도 동일 재현 중**: 7/27 현재 2,641종목(진행 중), 실거래 12,132행 이미 확보. 완주하면 2,827로 채워질 것
- **절차 개선(성찰 1번 원칙)**: A-4에 가드 추가 — **BAT-D 진행 중/장중 확인 시 종목수 무시, 실거래값 유무만 판정**. 종목수 정본은 완주 후. 지난주 금요일 187종목 조사는 이 함정 탓의 헛수고였다

**⏳ 7/27 저녁 이월**: ①오늘 BAT-D 완주 후 스코어보드 7/27자 재확인(αADJ 갱신) ②헬스체크 GREEN 회복 ③수급 종목수 2,827 채워지는지(정정 가설 확증)

### 2026-07-24 (금) — ★αEW 자기정정(하루 만에) · SHIELD 4개월 고착 규명

**A-0 스코어보드 (7/23 실측)** — 메인A 누적 -10.4% / **αADJ -7.3** / αK -38.2 / αEW +3.3
- 계좌 순위가 잣대마다 뒤집힘: αK 1위 현금방어NAV → αEW 1위 파도VF → **αADJ 1위 파도VF(+1.3%p, 유일한 유의미 양수)**. 메인A -7.3 · B안 -8.2로 명확한 열위

**기초데이터 (7/23 장날): 🟢 GREEN, 17/18** — 종가 raw 1,211/1,217·processed 1,174/1,180(미달 6종목 FDR 재검증 전부 거래정지/상폐) · 수급 11세분 2,642종목(삼전 금투 -2,936억·연기금 -180억) · 일별 CSV·벤치마크 18종 정상 · `universe.csv` "3일전 갱신 — 정상" · **BAT-D 실패 0건**(어제 1건→해소) · 국적별수급만 42일 STALE(외부대기)

**★7/24 당일분 선확인 (18:25, BAT-D 진행 중 헬스체크 조기 실행)** — 내일 아침 A-4 재확인 불필요
- **종가 ✅** 1,175/1,181 (99.5%) 7/24 반영 · 삼성전자 실측 249,500원 · 미달 6종목이 **7/23과 완전히 동일한 코드**(003410·010620·012510·057050·138490·230360) = 기확인 거래정지/상폐
- **수급 11세분 ✅** 7/24 · 2,640종목 · 11구분 동일 커버리지. 퀀트봇 담당 실값 **금투 -4,809억 / 연기금 -554억**(삼전), 단타봇 담당 외인 -8,680억·기관합계 -8,588억·개인 +16,928억·기타법인 +341억
- **★수급 종목수 2,827→2,640 (-187) 규명 완료 = 실질 손실 없음**: 7/8부터 3주간 2,827 고정이라 조사. ⑴**거래량>0인데 수급 누락 = 0건**(종가 parquet 교차검증) ⑵누락 187종목의 7/23 데이터 2,057행 중 **2,049행(99.6%)이 빈껍데기**(매수·매도·순매수 전부 0) ⑶실거래값은 3종목 8행뿐(KS인더스트리·베셀·파라텍, 1~5억 소액·유니버스 밖). **KIS가 껍데기 종목을 응답에서 뺀 것이지 수집 실패 아님** → 헬스체크 #18의 "정상" 판정이 결과적으로 옳았음. **내일 재조사 불필요**
- 벤치마크 22종·KOSPI/KOSDAQ 지수 CSV·`universe.csv`(11:31 갱신)·`kospi_investor_flow.csv` 전부 오늘 갱신 ✅
- BAT-A~O·J·PICKV2 **실패 0건** 완료, BAT-D는 18:25 시점 `scan_tomorrow_picks` 실행 중
- **⏳ 7/25로 이월**: ①**스코어보드 αADJ 실물 확인(A-6 핵심)** — 18:25 시점 `pnl_scoreboard_latest.json`이 아직 7/23분 ②BAT-D 완주 후 헬스체크 GREEN 회복 여부(조기 실행분은 🟠C 15/18, 실패 3건 중 학습기록·업로드 2건은 **미완료 탓**이고 국적별수급 43일 STALE만 알려진 외부대기)

**A-6 어제 배포 4건 전부 첫 실동작 성공**
- **EWY**: `빈 응답(시도 1/8) → 3초 후 재시도 → 2회차 성공 30종목`. 구 설정이면 30초 대기했을 상황
- **종가 전수검사**: `1,174/1,180 (99.5%) | 미달: 003410,...` 추정치 표기 소멸
- **휴장가드**: 장날 정상 통과(회귀 없음) / **αEW 병기**: 텔레그램 발송 OK 실물 확인

**★★αEW 자기정정 — 어제 배포한 지표가 하루 만에 오독을 만들었다**(`306348f`)
- **문제**: αEW = 계좌누적 − 동일가중누적인데 계좌는 주식 30%, 벤치마크는 **100% 투자**. 노출도가 다른 둘을 그냥 뺐다 → 하락장에서 현금이 많을수록 좋게 나옴 = **"덜 투자했다"가 능력으로 둔갑**
- **검산 하나면 잡혔다**: 계좌가 100% 현금이면 αEW +9.8%p로 "시장을 이긴다"고 나온다. 어제 이 검산을 안 했다
- **αADJ 신설** = 계좌누적 − Σ(그날 주식비중 × 그날 동일가중 수익률). 주식비중은 `stock_ratio`가 7/10부터 10일치뿐이라 **equity·capital로 역산**(두 값 공존 10일 전부 소수점까지 일치 검증) → 메인A 81일 전구간 + 파도VF·현금방어NAV 커버
- **★어제 결론이 뒤집힘**: 메인A αEW +3.3 → **αADJ -7.3%p**. 편향 +10.6%p가 전부 "덜 투자했다"에서 나왔다. 어제 "종목 선택은 문제 없고 자산배분이 문제"는 **틀렸다**. **현금 70%는 놀고 있던 게 아니라 손실을 막고 있었다**

**★SHIELD 4개월 고착 규명 — 퐝가님 "하락장 쉬고 상승 전환에 진입" 요청의 답**
- 그 기능은 **이미 구현돼 있고 고장나 있었다**. 5단계 톱니: ①MDD 고점 2026-03-11 고정(절대 안 내려감) ②매일 낡은 고점 대비 -27.66% 재계산 → LEVEL_3 재발동 ③RED → **최대 3종목**(자본 70% 유휴) ④해제하려면 +38% 회복 필요한데 3종목으론 불가능 ⑤→①
- **해제 조건이 이중으로 막힘**: ⑴`KOSPI 3일≥+3% AND VIX 전일비≤-10%` — 3년 778일 중 **10일(1.29%)**만 충족. 그런데 **VIX 조건은 성과를 개선하지 않는다**(D+20 기저선대비 ①단독 +2.48%p p=.0495★ vs ①AND② +0.21%p p=.96) = 걸러내는 게 아니라 그냥 막음 ⑵해제가 **하루짜리**(shield.py:288 그날 실행분에만 `killswitch_level="NONE"`, 고점 리셋 없어 다음날 재발동). 6/11·6/18 발동했는데도 6/23부터 계속 RED인 이유
- **7/23 실측이 결정적**: `KOSPI 3일 +8.9% (OK), VIX -2.4% (미달)` — **전환을 감지했는데 VIX가 막았다**
- **⏸️그런데 풀지 않기로 결정**: αADJ -7.3%p라 **지금 자본을 더 투입하면 손실이 증폭**된다. 역설적으로 고장난 SHIELD가 계좌를 지키고 있었다(6월 매칭EW -4.17% vs 100%EW -14.02%). **종목 선택이 양수로 돌아선 뒤에 푸는 것이 순서**
- **ORANGE 키 누락 수정**(`6766273`): SHIELD는 4단계 반환인데 `SHIELD_MAX_POSITIONS`에 ORANGE가 없어 폴백 8 = GREEN과 동일 → 위험 상태가 정상보다 느슨한 역전. ORANGE=4 추가로 단조성 복구. **킬스위치 회복 시 ORANGE 고정 경로(shield.py:307)가 있어 실제로 밟히는 값**

**★7번째 타이밍 축도 기각** — "전환 예측이 아니라 반응 속도면 되지 않나" 가설 검증(KOSPI 30년 7,289행): MA20 돌파 기저선 대비 **-0.87~-0.95%p(역방향)**, MA60 **엣지 ≈0**(p=.45~.97). 20일 신고가만 일부 p<0.05인데 ①"전환"이 아니라 "이미 오르는 중" 신호 ②45회 검정 중 4건이라 Bonferroni 보정 시 전부 탈락 ③**지연 D+0~D+10에 엣지가 소멸하지 않음 = "빨리 붙는 것"이 관건이 아님 → 반응 속도 가설 자체가 기각**

**구조격차 역사검증**(`0f89970`) — 60일 롤링 중앙값 +0.06%p·양수비율 50.4% = **평시엔 동전 던지기**. 현재 +32.6%p는 **역사적 97.4 백분위**(2026년 연간 +62.8%p로 7년 중 1위, 2위의 3배). ★평균회귀 예상이 빗나감: r=+0.290 **양의 자기상관**, 상위20% 이후 +5.78%p 추가 확대 → "곧 되돌아오니 버티면 된다"는 데이터가 지지 안 함. 지수배분 시뮬은 수익 개선(w=50% 시 -10.4%→+1.4%)에도 **하락일 누적 -22.4%→-61.9%로 3배 악화**·변동성 23%→60%(사실상 레버리지) → **기각**

**마감 성찰 (0+3질문)**
0. **수익 직결?** — ✅ 있음. **잘못된 지표를 하루 만에 잡은 것**과 **SHIELD를 풀지 않기로 한 결정**. 후자가 특히 중요 — 풀었으면 -7.3%p 선택에 자본을 3배로 태울 뻔했다
1. 오탐/누락: 자동체크 오탐 0. 대신 **내가 어제 만든 지표가 오탐원이었다**
2. **★판단 오류 — 오늘만 4번 뒤집혔다**: ①αEW 노출도 편향(어제 내가 발견·검증·배포한 것을 내 손으로 뒤집음) ②"종목 선택은 문제 없다"→실제 -7.3%p ③"현금 70%가 놀고 있다"→실제로는 손실 방어 중 ④"SHIELD를 풀자"→풀면 손해. **공통 패턴: 한 단계 더 검증하면 무너질 주장을 계속 내놓는다.** ★대책: **신설 지표는 극단 케이스로 검산할 것**(100% 현금이면? 100% 투자면?) — 이 한 줄이면 어제 잡혔다
3. 내일 1건: 오늘 배포 2건 A-6 검증 + **B-18 원위치 = 종목 선택 개선**(αADJ가 진짜 지표이므로 이걸로 재측정)

### 2026-07-23 (목) — B-18 판정기준 교정 · 휴장일 페이퍼 진입 발견 · EWY 원인 재규명

**A-0 수익 스코어보드** — 메인A 누적 **-11.7% (α-34.1%p)** · B안 -9.0%(α+8.2) · 블루칩V3 -5.3%(α+11.9) · 파도VF -3.5%(α+16.2) · 현금방어NAV +1.1%(α+25.0) / 동일구간 KOSPI +22.4%. 7/22은 전 계좌 양전(메인A +0.55%)이나 알파 격차는 -33.7 → **-34.1%p로 계속 벌어짐**.

**기초데이터 (7/22 장날): 🟢 GREEN, 17/18 B등급**
- 종가 `data/raw` 1,211/1,217 · `data/processed` 1,174/1,180이 7/22. **미달 6종목은 FDR 교차검증 결과 전부 거래정지/상폐**(003410·010620·138490·230360 데이터 0행, 012510·057050 거래량 0) — 수집 실패 아님
- 수급 11세분 전부 2,642종목. **퀀트봇 담당 금투·연기금 실값 확인**(삼성전자 금융투자 -3,766억·연기금 +16억) / 단타봇 담당 종목별 정상(외국인 +5,834억), 국적별수급만 41일 STALE(알려진 외부대기)
- 일별 산출 CSV 전부 7/22 생성. `universe.csv`는 7/20 이후 미갱신이나 **설계상 7일 임계**(health_check.py:649)라 정상
- A-5 FLOWX 퀀트 20/20 테이블 업로드 성공 · A-7 이중잠금 정상(AUTO_TRADING_ENABLED=0 + `data/KILL_SWITCH` 파일, 16:00 복원 cron 활성·06:00 삭제 cron 주석) · 실주문 0
- A-6 7/21 배포분 첫 실동작 확인 — 텔레그램 응답검증(`[PNL] 발송 OK`·`[telegram] 발송 OK`)

**★B-18 본선 백테스트 — 판정 기준 자체를 고쳐야 했다**(`44a5ff4`)
- **KOSPI 차감 알파가 이 국면에선 무용**: 유니버스 1,180종목 동일가중 평균조차 KOSPI 대비 D+20 **-9.9%p**(상승구간 -25.5%p). 시총 최상위 주도장이라 개별종목은 구조적으로 지수 미달 → 이 잣대로는 "모든 개별주 전략이 음수"라는 무의미한 결론만 나옴. **판정 기준을 '같은 날 유니버스 동일가중 평균 대비 초과수익'으로 교체**하고 시총 5분위 매칭으로 사이즈 효과까지 제거
- **결과**: A(SCAN 실진입) D+5 **-4.53%p(p=.0015)** / D+10 -4.80(p=.044) / D+20 -8.14(p=.012) — 3개 지평 전부 유의, 시간 갈수록 악화. B(시그널 TOP5)는 **전부 무의(p=.14~.67) = 중립**. 날짜 페어드 B−A는 D+5 **+4.25%p 승률 80%(p=.003)**
- **★처방이 뒤집힘**: "시그널 대형주로 갈아타면 번다"가 아니라 **"SCAN을 멈추면 손실이 준다"**. B도 알파를 내지 못하므로 갈아탈 곳이 아니다
- **★B-18 프레임 정정**: A의 손실은 **하락구간 집중**(D+10 -10.19%p, p=.000, 양전 5.3%) vs 상승구간 -1.48%p(p=.614) 무의. 상승장 KOSPI 미달은 **기저선도 -15.84%p**라 중소형주 전체 현상이지 SCAN 고유 결함이 아님. 단 상승구간 n=14~25로 검출력 부족 = **"효과 없음"이 아니라 "확인 못 함"**
- **원래 질문은 답 못 함**: `FOREIGN_SURGE_PB` 태그가 6/2부터라 n=4~11(상승구간 n=0). "외국인 유입 대형주로 좁히면?"은 표본 축적 대기
- **정정 2건**: ①7/21 "중소형주 반복 진입" 부정확 — 실제 Q5(중앙 6.79조) 19건·Q4 15건으로 **중대형주 다수** ②7/20 "SCAN A등급 중립"(KOSPI 기준)이 기저선 기준으론 재현 안 됨 — A등급 D+10 -4.89(p=.020)·D+20 -11.66(p=.018)로 유의한 반면 AA는 무의. **단 n=12~17·다중검정 미보정이라 확정 아님 → B-17 철회 근거가 흔들린다는 것까지만 기록, 차단 권장 안 함**

**★★오후 최대 발견 — B-18의 전제가 벤치마크 착시였다**(`89f04b1`)
- **경위**: 퐝가님 지시로 "SCAN 진입 중단"을 실제 적용해봤더니 **후보 232→230건(-2)**에 그쳤다. 현행 SCAN 10종목 중 **8종목이 ALPHA(5)·SWING(3) 라벨로 그대로 생환**. SCAN·ALPHA·SWING은 같은 `tomorrow_picks` 풀에서 나온 형제이고 라벨은 어느 섹션이 먼저 집었느냐일 뿐이었다 → **롤백**(효과 -2종목에 전략별 시계열만 오염)
- **라벨 무관 재측정**: 메인A 진입 전체(94건) 기저선 대비 D+5 -1.53 / D+10 -2.10 / D+20 -3.65%p **전부 무의**(p=.13~.21). **SCAN vs 비SCAN도 무의**(p=.34~.39) = **라벨은 아티팩트 확정**
  - ★여기서 **오전 결론도 정정**: 오전 "SCAN D+5 -4.53%p p=.0015"는 `signals_*.json` 존재일로 교집합을 건 결과였다. 그 파일이 5/15부터라 3/24~5/14 22건이 빠졌고, 전체 기간으론 SCAN도 **무의**(p=.078~.174)
- **모순 추적 → 원인 규명**: 진입이 무의인데 계좌 알파가 -34.1%p인 게 설명 안 됐다. 벤치마크를 의심해 분해:

  | 계좌 | 구간 | 계좌 | KOSPI | 동일가중 | αK | **αEW** | 구조격차 |
  |---|---|---|---|---|---|---|---|
  | 메인A | 3/24~7/22 | -11.7% | +22.4% | **-17.4%** | -34.1 | **+5.7** | **+39.8** |
  | 현금방어NAV | 6/25~7/22 | +1.1% | -23.9% | -6.5% | +25.0 | +7.7 | -17.4 |

  **메인A는 동일가중 대비 +5.7%p로 오히려 이기고 있다.** 현행 알파의 **117%가 구조적 격차** = 이 국면은 시총 최상위 주도장이라 개별주를 들고 있다는 사실만으로 지수에 39.8%p 뒤진다
- **★이 착시가 7/20 이후 진단 3개를 잘못 끌고 갔다**: ①B-18 "전 전략 알파 음수" ②B-17 철회 판정 ③오늘 오전 "SCAN이 범인". 5계좌 알파 1위가 현금방어NAV인 것도 "개별주를 안 들어서"였다
- **조치**(`89f04b1`): 스코어보드에 **αEW 병기** + 구조격차 1줄 + 알파 1위 양 기준. 유니버스 로드·계산 실패가 본체 발송을 막지 않도록 전부 폴백(7/20 미발송 사고 교훈). 검증 9.2초·JSONL 무변경
- **→ B-18 재정의**: 남은 과제는 진입 로직 개선이 아니라 **사이즈/자산배분(초대형주·지수 비중)**

**★B-20 조사 — 1차 판정을 내가 뒤집었다(오전 "발동 0건" → 오후 실측 "2경로 다 실재")**
- **1차(오전) 오판**: 7/17 로그의 SCAN-BLOCK 3건이 ALPHA로 재등재되지 않은 것만 보고 "우회 실측 발동 0건"으로 기각했다. **차단된 종목이 돌아오는 것만 찾느라, 애초에 차단 검사를 안 거친 종목을 통째로 놓쳤다.** 같은 로그에 있던 `[ALPHA] 삼성물산우B → AA 승격(score=100, PULLBACK15_DUAL)`이 바로 그 사례였다
- **2차(오후) 실측 규명** — `collect_candidates()` 직접 실행(오늘 picks 634건, 원장 md5 무변경 확인):
  - **★진짜 구멍 = 차단이 상위 15개에만 걸린다**. 섹션2는 `picks_sorted[:15]`만 검사하는데 섹션4는 `picks` 전체를 훑어 STRONG이면 AA 등재 → **16위 이하 STRONG은 차단 없이 AA·score=100(진입 우선순위 1등)으로 직행**. 실측 9건(SKC·HJ중공업·큐로셀·인벤티지랩·가온전선·켄코아·메카로·코오롱·코오롱생명과학)
  - **제2경로 = SWING 라벨교체**: 차단된 성호전자·LG이노텍이 `SWING/A/score100`으로 생환(실측 2건). ALPHA가 아니라 섹션3이었다
  - → **"6/27 SCAN/AA 전면 차단"은 반쪽 차단**이었고, 7/20 부검이 "AA는 이미 차단됨"을 전제로 삼은 것도 성립하지 않는다
- **⏸️ 봉쇄는 보류**: 구멍은 확정이나 **막는 게 이득이라는 근거가 없다** — 오늘 백테스트상 차단 대상 AA는 무의(p=.22~.49)인데 통과되는 A가 유의하게 더 나쁘다(D+10 -4.89 p=.020). 차단 정책의 방향 자체가 뒤집혔을 수 있어 근거 없이 코드를 고치지 않았다
- **★대신 휴장일 페이퍼 진입 발견**(`f161df1`): `run_daily`에 주말 가드만 있고 **공휴일 가드 부재** → 7/17 제헌절(금·weekday=4)이 그대로 통과. 진입일 50일 전수 판정 결과 피해 4건 — 5/1(근로자의날) 003410 -0.22% / **7/17 삼표시멘트 -5.70%·현대건설 -7.34%·HLB제약 -14.17% 전부 STOP_LOSS**. n=4라 인과는 주장 않으나 **손익 이전에 "성립할 수 없는 거래"**이고 원장 신뢰성을 깨며 B-16으로 실주문이 열리면 그대로 주문 오류. **B-13의 나머지 절반**(health_check 계열만 덮고 매매 본체 누락)

**★EWY 원인 재규명**(`7af774a`) — 7/22 BAT-D 실패 1건의 정체
- 감으로 재시도를 늘리지 않고 **12회 연속 프로브로 실측: 빈응답률 58%**(rt_cd=0·"정상처리"인데 output2만 0건). 기존 주석의 "제공 시간창 의존" 추정은 **틀렸고**, 장중 측정에서도 3초 간격에 EMPTY↔OK가 혼재 = **시각 무관 서버측 상시 랜덤**
- 구 설정 0.58³=**19.9%≈5일에 1일 실패**(7/20 3회차·7/21 2회차 성공·7/22 실패와 정확히 일치) → 신 설정 0.58⁸=**1.3%**. 간격 점증(3·3·5·5·10·10·15초)으로 **총 대기 51초 < 구 60초**

**배포 (15:35 예약 → 성공)**
- `c4aec2d..51a348b` fast-forward, 7파일. 배포분 4건 코드 존재 확인(EWY 재시도상수·휴장가드·αEW·종가 전수검사)
- ⚠️**다음 배포 시 주의 2건**: ①**예약 시각 15:35는 cron `BAT-L`(15:35)과 겹친다** — 이번엔 무사했으나(BAT-L은 src/ import, 배포는 scripts/ 변경) 경합 위험이 있으므로 **다음부터 15:45 등으로 옮길 것**. 7/16 선례를 검토 없이 답습한 결과다 ②배포 스크립트의 `echo "HEAD: $(git log ...)"`가 **예약 시점에 평가**돼 로그엔 옛 커밋이 찍힌다(실제 HEAD는 정상). 검증용으로 쓸 수 없으니 배포 후 직접 확인할 것

**마감 성찰 (0+3질문)**
0. **수익 직결?** — ✅ 있음. **A-0(1번 지표) 자체가 틀렸다는 걸 찾아낸 것**이 최대 성과다. 매일 보던 알파가 시스템을 34%p 나쁘게 보여줬고, 그 착시로 7/20 이후 진단 3개가 잘못된 방향으로 갔다. 이제 메인A는 **동일가중 대비 +5.7%p로 이기고 있음**이 드러났고, 진짜 과제가 진입 로직에서 **사이즈/자산배분**으로 옮겨졌다. 다만 정직하게 = 여전히 '버는 코드'는 아니고, 어디를 파야 하는지가 바뀐 것
1. 오탐/누락: 자동체크 오탐 0. **헬스체크 "종가 1,180/1,180 (100.0%)"가 50개 샘플 추정치를 실측처럼 표기**하는 것 발견(실측 1,174/1,180) — 오늘은 차이가 상폐/거래정지라 무해했으나 B-19에 병합
2. **판단 오류(★ 3건 — 오늘은 내 판정이 두 번 뒤집혔다)**: ①**KOSPI 차감 알파를 무비판적으로 적용한 것** — [[feedback_judge_by_alpha_not_pnl]]는 "절대손익 대신 시장 대비"인데, **잣대인 시장이 시총가중이면 개별종목 전략에 부적합**하다는 걸 놓쳤다. 기저선(동일가중) 없이는 알파도 오독된다 ②**B-20 "실측 발동 0건" 오전 판정이 틀렸다** — 차단된 종목이 돌아오는 것만 찾고 **애초에 차단 검사를 안 거친 종목(상위 15개 밖)을 통째로 놓쳤다**. 로그에 증거(`[ALPHA] 삼성물산우B → AA 승격`)가 있었는데도 못 봤다. ★교훈: **"우회했는가"를 볼 때 통과 경로만 보지 말고 검사 범위 자체를 먼저 확인해야** 한다. 오전 판정을 그대로 뒀으면 "차단은 잘 되고 있다"는 틀린 전제가 백로그에 박힐 뻔했다 ③**커밋을 장중이라고 미룬 것** — 배포는 장중 금지지만 커밋은 무관한데 오전 작업 전부를 로컬에만 두고 있었다(퐝가님 "문제 없나" 질문에 발견). 유실 위험을 자초함
2-1. **★★가장 큰 오류 = 벤치마크를 한 번도 의심하지 않은 것**. 7/17에 A-0를 만든 이래 6일간 "메인A 알파 -29~-34%p"를 매일 보면서 **잣대가 틀렸을 가능성을 검토하지 않았다**. 오늘도 퐝가님이 "SCAN 중단 진행하라"고 해서 실제로 적용해봤기 때문에 발견됐지, 분석만 계속했으면 못 찾았다. ★교훈: **지표가 6일 연속 같은 방향으로 극단이면 전략이 아니라 지표를 의심할 것**. 그리고 **가설은 코드로 적용해보면 예상 못 한 사실이 드러난다**(SCAN 끄기 → 8/10 라벨 생환 → 라벨 아티팩트 → 벤치마크 의심으로 연쇄)
3. 내일 1건: 오늘 배포 4건 A-6 검증(EWY 8회 재시도·휴장가드·종가 전수검사·**αEW 병기**) + B-18 재정의에 따른 사이즈/자산배분 축 설계

### 2026-07-21 (화) — 데이터 GREEN · B-18 기계적 원인 특정(손절 무죄 → 진입 범인)

**A-0 수익 스코어보드 (1번 지표) — 반등장에서 격차가 더 벌어짐**
- 메인A 누적 **-12.2% (α-33.7%p)** · B안 -9.4%(α+8.4) · 블루칩V3 -7.2%(α+10.6) · 파도VF -4.9%(α+15.4) · 현금방어NAV 0.0%(α+24.4)
- 동일구간 KOSPI **+21.5%**. ⚠️ **오늘 KOSPI +3.56% 급반등했는데 메인A -0.44%로 또 미참여** → 알파가 어제 -29.1 → **오늘 -33.7%p로 하루 만에 4.6%p 악화**. B-18이 데이터가 아니라 실시간으로 손실을 키우는 중

**기초데이터 (7/21 장날): 🟢 GREEN, 17/18 B등급 (93점)**
- 종가 **1,180/1,180 (100%)** · 유니버스 1,180종목 · 투자자수급 **832행 당일(0일전)** · 수급커버리지 2,643종목(직전 2,827, 정상) · 원자재 당일(wti 82.42) · KOSPI 7,287행 · DART 1,272건
- 세분수급(퀀트봇 담당 금투·연기금) 정상 — 예: 삼성전자 금투 -5,493억·연기금 +599억 실기록
- 유일 실패 = ❌ 국적별수급 STALE(6/9, 40일전) = **단타봇 분업 영역·알려진 외부대기**(KRX 잠금). 퀀트봇 데이터엔 이상 0
- BAT 전 단계 실패 0건, BAT-D 18:57 완주

**★B-18 기계적 원인 특정 백테스트 — 내 가설이 기각됨(손절 무죄)**
- **가설**: "메인A는 손절로 털린 뒤 반등을 못 잡는다(손절이 상승장 미참여의 기계적 범인)". 어제 부검의 STOP_LOSS 34건·승률 0%·-8.32%가 촉발
- **측정**: 메인A STOP_LOSS 청산 35건(어제 34+오늘 1)의 **손절일 종가**(=안 팔았으면 들고 갔을 지점) 대비 D+5·D+10 절대 반등률 + KOSPI 차감 알파. FDR·전수·VPS 실행
- **결과 = 손절은 정당했다**:
  - 절대 반등 D+5 **-3.28%**(양전 38%) / D+10 **-4.77%**(양전 17%) — 손절 후에도 계속 더 떨어짐. 손절이 **추가 -4.77% 하락을 막아줌**
  - KOSPI 차감 알파 D+10 **-6.48%**(양전 29%) — 시장보다도 더 못함 = **종목 선택 자체가 나빴던 것**이지 손절 타이밍 문제 아님
  - **월별 단조 악화**: 4월 +10.1%(1건) → 5월 +1.1% → 6월 -7.3% → **7월 -12.6%**(손절 후 폭락 지속) = 최근일수록 손절이 더 정당
  - 적대적 검증: 로보스타 +83.4% 이상치가 평균을 위로 끌어올렸는데도 **D+10 중앙값 -9.49%**로 평균보다 나쁨 → 이상치 제거 시 결론 강화. D+5·D+10·월별 3방향 일관
- **★범인 재특정 = 진입 쪽**: STOP_LOSS **35건 전부 SCAN**. **SCAN이 "떨어지는 칼날"(중소형주)을 반복 진입 → 손절 → 반복.** 손절 방어벽은 정상인데 **진입 종목군이 하락 지속 종목을 향해 있음**
- **오늘 A-0와 정합**: 시그널 엔진은 오늘 삼성전자·삼성생명(외국인 급증 `FOREIGN_SURGE_PB`)을 TOP5로 뽑았는데 메인A가 손절한 건 SCAN 중소형주 → **좋은 반등 신호는 감지하는데 메인A 진입이 그걸 안 씀**. 이게 "상승장 미참여"의 기계적 정체
- **다음 걸음(B-18 처방 방향)**: "메인A가 실제 진입한 SCAN 종목군 vs 같은 날 시그널 엔진 대형주(외국인 유입) TOP — 이후 수익 격차" 백테스트. "SCAN 진입 필터를 외국인 유입 대형주 쪽으로 좁히면 미참여가 줄어드는가" 검증. 여전히 과거 데이터·freeze 무관

**★전체 검수 (4팀 병렬 서브에이전트 + 직접 교차검증)** — 퐝가님 "코드·로직·버그·슬러지" 지시
- 팀1 매매로직 / 팀2 신설코드 / 팀3 헬스체크 / 팀4 슬러지·구조. **모든 발견을 직접 교차검증 후에만 조치**(7/14 `.env.SAFE_FOR_AI` 오탐 교훈)
- **안전군 조치 완료**(`24f8c92`+`61fc77c`, 페이퍼 관측 무관·내일 아침 첫 실동작):
  - HIGH `send_telegram` 응답 미검증(scoreboard·bull) → 응답 `ok` 확인 + bull은 Markdown 400 시 plain 재발송. **7/20 키오타 픽스의 나머지 절반**("저장/발송 로그 ≠ 실제 발송")을 완결
  - HIGH `health_check.py` 휴장가드 부재 → `kis_holiday_adapter` 적용. **B-13이 `data_health_check.py`만 고친 것을 자매파일까지 완결**(다음 평일 휴장일 가짜경보·무의미 재수집 차단)
  - MED scoreboard `latest.json` 원자적쓰기 / `telegram_adapter` 팬텀 import 4곳 → `src.telegram_sender` / 데드코드 `collect_investor_bulk.py` 삭제(B-8b 완결·CD007 위험) + `sector_etf_constituents.json` 추적해제
- **매매로직 조치 완료**(`c4aec2d`, 퐝가님 승인·freeze·페이퍼 규칙 앞으로만·과거 소급 없음):
  - #3 `weekly_rebalance`가 유지 승자의 `t1_sold·peak_price·trailing_active`를 리셋 → 이미 +10% 익절한 승자가 T1 재발동으로 반복 절단+트레일링 고점 소실(**B-18 상승장 미참여를 코드가 거들던 것**) → `entry_date`(보유일)만 리셋
  - #4 T1 부분익절이 `closed_trades·stats` 미기록 → 전략 승률/PF 저평가 → 기록 반영. **검증: py_compile + 인공 포지션 스모크 2건 PASS**
  - #5 docstring "5영업일" → "5일(캘린더 기준)" 오기 정정
- **에이전트 오탐 기각 1건**: `data_health` #2 샘플링 "34% 가짜FAIL" 주장 → 실측(수급종목 1180/1180 통과)이 반증. 에이전트의 모집단 가정(100/2800)이 낡음 = 7/14 유형 오탐. **직접 검증 없이 등재 안 함 원칙이 또 한 번 오탐을 걸러냄**
- **저긴급 백로그 등재**: B-19(헬스체크 조용한 실패 2건)·B-20(SCAN-BLOCK 라벨교체 조사)·B-21(코드위생 묶음)

**마감 성찰 (0+3질문)**
0. **수익 직결?** — ✅ 있음. ①B-18의 기계적 범인을 **손절→진입으로 정확히 좁힘**(감으로 손절 완화했으면 추가 -4.77%를 부르는 **정반대 처방**을 심을 뻔). ②검수 #3(리밸런스 승자 반복절단)은 **상승장 미참여를 코드가 직접 거들던 버그**라 B-18과 직결된 수익 개선. 아직 '버는 코드'는 아니나 진단·위생 양쪽 진전
1. 오탐/누락: 데이터 GREEN·자동체크 오탐 0. **검수 에이전트 오탐 1건(#2 샘플링)을 직접 검증으로 기각** — 에이전트 발견은 반드시 실측 대조 후 채택
2. **판단 오류(★)**: ①"손절이 범인" 가설 기각 — **청산 사유의 승률(STOP_LOSS 0%)은 그 종목이 원래 나빴는지를 말해주지 못한다**([[feedback_judge_by_alpha_not_pnl]] "절대손익은 베타에 오염"이 손절 판정에도 적용). ②**7/20 스코어보드 픽스가 키오타만 고치고 응답검증을 빠뜨린 절반짜리였음**을 오늘 검수가 잡음 — "픽스했다"도 재검증 대상. ③git rm(staged)만 커밋되고 Edit 수정분이 unstaged로 빠져 재커밋한 실수 — `git add` 확인 습관화
3. 내일 1건: SCAN 진입 종목군 vs 시그널 대형주(외국인 유입) TOP 격차 백테스트(B-18 처방·검수 B-20과 연계)

### 2026-07-20 (월) — 주말 배포 4건 첫 실동작 전부 성공 · B-13 완료

**A-0 수익 스코어보드 (1번 지표)** — 시장 급락일, 전 계좌 -3%대
- 메인A 누적 **-11.8% (α-29.1%p)** · B안 -9.7%(α+10.9) · 블루칩V3 -8.0%(α+12.6) · 파도VF -5.8%(α+17.3) · 현금방어NAV 0.0%(α+27.0)
- 동일구간 KOSPI +17.3%. 지수BH Top3가 전부 인버스(+75.8%/+35.9%/+32.2%) = 여전히 하락장 국면
- ⚠️ **메인A 알파가 유일하게 음수**이고 규모가 -29%p — 반등 공세 준비(로드맵 ②)가 늦어질수록 이 격차가 굳어진다

**기초데이터 (7/20 장날): 🟢 GREEN, 17/18 B등급**
- 종가 1,180/1,180 (100%) · 수급 1,180종목 · 투자자수급 831행(당일) · 수급커버리지 2,645 정상
- **원자재 ✅ 복귀** — wti=81.78(7/20 당일). 지난주 3/26 유물 → B-12로 신선도 회복
- 실패 1건 = 국적별수급(알려진 외부대기)뿐. BAT 전 단계 실패 0건

**A-6 주말 배포 4건 첫 실동작 — 전부 성공**
- **B-11 EWY 부활** ✅ 30종목 수집·Supabase 업로드 완료(삼성전자 33.64%·SK하이닉스 28.71%). **★재시도 로직이 실전에서 필수였음**: 빈 응답 2회(18:47:15, 18:47:45) → 3회차 성공. 재시도 없었으면 그대로 실패했을 것. prev source 가드도 정상(`TIGER_MSCI_KR_TR_310970`). **5/15 이후 두 달 만에 축 복구**
- **B-6 레버리지 ETF 수급** ✅ `'isin'` 실패 **21건/일 → 0건**
- **B-12 원자재** ✅ BAT-A 06:10 자동 실행, 5종 신선
- **BAT-D 실패 1건 → 0건** (EWY가 유일한 실패였음)

**신규 발견·처리 2건**
- 🔴 **수익 스코어보드 텔레그램 미발송** (`5193b5e`) — `.env` 정본 키는 `TELEGRAM_BOT_TOKEN`인데 스코어보드·EWY가 `TELEGRAM_TOKEN`을 읽어 조용히 SKIP. **A-0 지표를 만든 첫날 A-0 자신이 배달 버그를 드러냄**. BOT_TOKEN 우선+폴백으로 수정, 실발송 검증 완료. 동일 오타 전수조사 결과 나머지 스크립트는 정상
- ✅ **B-13 휴장일 가드 완료** (`2ba455b`) — 7/17 제헌절에 **🚨 D등급(13/18) 가짜 경보** 발송된 것 확인[^13]. `src/adapters/kis_holiday_adapter.py` 신설(KIS CTCA0903R `opnd_yn`+캐시) → 휴장일엔 시장의존 7항목 SKIP(등급 분모·분자 제외). **판정 소스를 KIS 공식 API로 둔 이유 = 우리 수집 파이프라인과 독립이어야 함** (자체 산출물로 판정하면 '수집 실패'가 '휴장'으로 오판되어 진짜 장애를 은폐). 판정 불가 시 장날 폴백(경보 억제보다 오경보가 안전)
  - 검증: 휴장일 판정 4일 정확(7/16 개장·7/17 휴장·7/18 휴장·7/20 개장) / **장날 7/20 회귀 없음(17/18 동일)** / 7/17 소급 재현 시 시장의존 7건 SKIP 확인. 단 소급 재현의 잔여 실패 3건(레짐·BRAIN·SHIELD "오래된")은 **과거 날짜 소급 검사 아티팩트**(당시 실로그는 ✅) — 실제 다음 휴장일엔 국적별수급 1건만 남아 🚨D → 🟡B 예상
- 주말(7/18~19) cron 미실행 확인 ✅ (로그 파일 없음 = 요일 제한 정상)

**마감 성찰 (0+3질문)**
0. **수익 직결 작업?** — ✅ **있음**: B-15 BULL 전환 감시 게이트 신설·배포. 단 정직하게 = 이건 '버는 코드'가 아니라 **'반등을 놓치지 않는 코드'**다. 실제 수익은 ①BULL 전환이 실제로 오고 ②B-16(실주문 경로)이 열려야 발생한다. 오늘의 진짜 소득은 **두 번의 백테스트로 무근거 로직 두 개를 심기 전에 걸러낸 것**.
1. 오탐/누락: 스코어보드가 4일간(7/17~7/20) 조용히 미발송 — **"저장 완료" 로그만 보고 발송까지 됐다고 넘겼던 것**이 원인. 신설 알림은 수신까지 실물 확인해야 한다.
2. 판단 오류: B-11 재시도 로직을 "혹시 몰라" 넣었는데 실전 첫날 2회 연속 빈 응답으로 **없었으면 실패**했을 상황이었다 — 간헐 실패에 대한 방어는 과잉이 아니었다.
3. 내일 1건: **B-16 실주문 경로(E 게이트) 점검** — B-15가 후보를 제시해도 집행이 freeze면 "번 돈"은 계속 0이다.

**B-15 상세 (저녁 작업)**
- **사전 백테스트 2건으로 내 가설이 기각됨** — 감으로 짰으면 무근거 로직을 심을 뻔했다:
  - ①`drv_pct 90+ → <60` 진정 진입: KOSPI 30년(7,287행) fwd20d **edge -1.88%p (역방향)**. V3b의 '하방변동<60'은 **레짐 분류/사이징** 조건이지 **진입 타이밍** 조건이 아니었는데 전용하려 한 오류.
  - ②지수 수급(외인·기관 5일·쌍끌이·매도→매수 전환): **전 조건 |t|<1.1 무효**. 검증된 수급 3단계는 **종목 레벨**이지 지수 레벨이 아니다.
  - → 기존 기각 목록(레짐게이트·인버스·로테이션·약세장 기관매집)과 완전 일관. **"지수를 보고 언제 들어갈지 맞히는 축"은 우리 시스템에서 구조적으로 기각된다**는 게 이제 6건째 증거.
- **발견한 진짜 구멍**: `run_scenario_v1.py`에 알림 코드가 **0줄** — BULL로 전환돼도 JSON에 조용히 기록만 되고 통보가 없다. 반등을 놓치는 기계적 원인.
- **채택 설계**: 타이밍 예측이 아닌 **반응 속도**. 전환일에만 텔레그램(평시 무알림) + 워치리스트를 저PER > 수급확인 > FV 순으로 제시. 근거는 생존한 종목축 하나(저PER D+20 +0.90%p·t=3.13·승률 76%, 비이벤트 대조 완료)뿐이며 수급 3단계는 표본 n=22라 확인 태그로만 사용.
- 검증: 평시 경로(조용한 1줄)·발동 경로(🚀 알림+후보 7종) 양쪽 VPS 실행 확인, 시뮬 인공기록 정리 후 정상 복원. 관측 전용·실주문 0.

### 2026-07-17 (금) — 제헌절 휴장 · 배포 6건 첫 실동작 전부 검증

**아침 루틴 결과 (7/16 데이터 판정: 🟢 GREEN, 16/18 B등급 — 실패 2건 모두 예상된 정직 표시)**
- A-1 ✅ 로컬·VPS 7/17(금) 10:25 일치. **오늘 제헌절 휴장**, 전일 7/16=장날, 다음 장날 7/20(월)
- A-2 ✅ cron_20260716 완주(06:10~18:54) — BAT-D 실패 1건(=EWY 소스차단, 정직 카운트) 외 전 단계 0
- A-3 ✅ 건강검진 **16/18 B** — ❌국적별수급(알려진 외부대기) + ❌원자재 STALE 112일전(**B-4 픽스의 의도된 정직 표시**, 어제까지 가짜 ✅)
- A-4 ✅ 종가 1,179/1,179 (100%) · 투자자수급 7/16 830행 · 수급커버리지 2,644 정상. ★발견: 커버리지 **D+1 보충 패턴** — 7/15가 어제 체크 땐 2,644, 오늘 2,827로 증가(KIS 30일 윈도우 재수집이 전일 누락 보충). D+0 ~2,644는 정상 범위
- A-5 ✅ FLOWX 14/14 신선 (어제까지 매일 가짜 STALE 4건 → 소멸)
- A-6 ✅ **7/16 예약배포(15:35, ef48bd1) 성공 + 픽스 6건 전부 첫 실동작 확인**: B-1 EWY "소스 차단(iShares 봇차단)" 정확 로그 / B-2 FLOWX 14/14·재실행 소멸 / B-3 CFO 원장 불일치 경고 소멸(HHI 등 정상 경고만 잔존) / B-4 원자재 ❌ 전환 / B-5 소요시간 139.5분 실측 출력("?분" 소멸)·오경보 0 / B-10 외국인=SKIP 표기 2회
- A-7 ✅ 실주문 0 (freeze 이중잠금 + 16:00 KILL_SWITCH 복원 cron 유지 확인)
- A-8 백로그: 잔여 항목 전부 ⏸️ 퐝가님 결정 대기(B-6·7·8·11·12)라 소화 대상 없음 — 대신 **B-13 신규 등재**(휴장일 가드)

**오늘 관찰 예약**: 휴장일인데 crontab 요일 기반이라 저녁 배치 그대로 실행됨 — 오늘 저녁 알림 일부(당일 데이터 없음류)는 예상 노이즈. 로그가 B-13 설계 실측 근거가 되므로 7/20(월) 아침에 분석.

**오후 — 결정 대기 5건 전부 소화 (퐝가님 "진행" 위임, 5커밋 전부 VPS 배포·실검증)**
- B-11 ✅ EWY 대체 (`f9821e7`+`9d5fc3b`): pykrx ETF PDF는 사망('isin')이라 기각 → **KIS ETF 구성종목시세(FHKST121600C0) 채택**. TIGER MSCI Korea TR(310970) 상위 30종목+비중(삼성전자 33.63% 실캡처)[^11]. 스키마·테이블 유지(웹 재배선 0), 구 EWY prev와의 가짜 비교 차단(source 가드), 빈 응답 간헐성(10:35 30종목→10:41 0종목) 재시도 3회. **최종 판정 = 7/20(월) 저녁 BAT-D**.
- B-12 ✅ 원자재 부활 (`f11b88a`): 신규 `collect_commodity_prices.py`(yfinance 5종) + BAT-A 배선. **유물의 실해악 실증: 대시보드가 WTI 103.06(3/26) 노출 중이었는데 실제 79.20 — 석 달 -23% 은폐**[^12]. cost_gap은 원가 기준값 출처 확보 전까지 미포함(창작 금지) → B-14. VPS 실행 검증 5종 OK.
- B-6 ✅ 레버리지 ETF 수급 KIS 재배선 (`a6e6d67`): 'isin' 21건/일의 근원 = pykrx ETF 모듈 전체 사망[^6]. KIS FHKST01010900이 ETF 지원 실측(KODEX레버리지 외인5일 +1,635.8억) → `get_etf_flow`·`get_etf_investor_flow` 2곳 교체, 0점 고정이던 수급축 부활. VPS 실모듈 검증.
- B-7 ✅ 훅 픽스 + 잔재 삭제 (`55b3494`): 훅 섹션6을 while-read(공백 보존)+식별자 검사+import 패턴 매칭으로 수정[^7] → 차단됐던 `quant_bot (1).py`(6/14 실수 커밋, 참조 0) 삭제 커밋이 수정된 훅을 실전 통과.
- B-8 ✅ **라이브 리스크 발견·제거** (`68a559e`): 수급 자동복구 경로(health_check)가 **폐기된 collect_investor_bulk(KRX 로그인=CD007 계정잠금 원인)를 여전히 호출** — 수급 0행인 날 헬스체크가 계정잠금 유발 가능했음 → collect_investor_kis --days 1 재배선[^8]. coo_orchestrator는 미스케줄(죽은 파일) 확인 → 본체 아카이브는 B-8b 저우선.

**마감 성찰 (3질문)**
1. 오탐/누락: pykrx ETF 모듈 사망을 B-6(수급)과 B-11(PDF)이 **각각 다른 증상으로 겪고 있었는데 7/14 검수는 연결 못 함** — 'isin' 에러의 근원을 그때 팠으면 하루에 잡았음. 증상 2개가 같은 라이브러리를 가리키면 근원부터.
2. 판단 오류: 어제 "B-11은 퐝가님 결정 대기"로 세웠지만, 오늘 파보니 pykrx 대안은 죽어있었고 KIS가 유일했음 — **선택지 검증 없이 결정만 요청한 것**. 다음부턴 선택지 실검증 후 올리기.
3. 내일(7/20) 1건: 아침 루틴 + 7/17 휴장일 저녁 로그 분석(B-13 설계) + **B-11 첫 장날 실동작 판정**(EWY exit 소멸 여부).

### 2026-07-16 (목) — 체계 가동 첫날

**아침 루틴 결과 (7/15 데이터 판정: 🟢 GREEN, B등급)**
- A-1 ✅ 로컬·VPS 7/16(목) 11:32 KST 일치, 전일 7/15 장날
- A-2 ✅ cron_20260715.log 완주(06:10~18:56) — BAT-F 실패 0 · BAT-HEALTH 실패 0 · **BAT-D 실패 1건 = EWY exit1** (신규 가시화 픽스가 정확히 드러낸 것[^1])
- A-3 ✅ 헬스체크 **17/18 B등급** — 유일 실패 = 국적별수급 STALE(알려진 외부대기)
- A-4 ✅ 종가 1,179/1,179 (100%) · 투자자수급 DB 29,084행(7/15) · 수급커버리지 2,644종목(직전 2,827 대비 -6.5%, 임계 15%p 통과)
- A-5 ✅ FLOWX·SIGNALS·RELAY Supabase 당일 정상 (FLOWX STALE 4건은 순서 오탐[^2])
- A-6 ✅ **7/14 배포 5커밋 첫 실동작 검증 완료**: 헬스체크 실패건수 정상 집계("실패 1건", None 아님) / 수급커버리지 #18 첫 가동·정상 판정 / EWY exit1 가시화 작동
- A-7 ✅ 실주문 0 유지 · 7/16 오전 에러 2건은 KIS 일시 연결끊김(자가회복)

**신규 발견**: B-1~B-4, B-10 등재 (각주 참조)

**오후 자동화 진행 — 백로그 6건 소화 (전부 로컬 시뮬 검증 후 배포)**
- B-1 ✅ EWY 원인 최종 규명: 헤더 문제 아님 — **iShares 미국 사이트 전역 봇차단**(UA·쿠키·TLS Chrome 위장 전부 무효, IVV도 동일, 마지막 성공 5/15)[^11]. `collect_ewy_holdings.py`에 HTML 응답 감지 추가 → 로그가 "헤더를 찾을 수 없음" 오진 대신 "소스 차단"을 정확히 보고 (exit 1 유지, 실행 검증). 대체소스는 B-11로 승격.
- B-2 ✅ `health_check.py`: 스캐너 4종(fib/alpha/ranking/bluechip)을 `LATE_UPLOAD_TABLES`로 분리, 3일 내 데이터면 신선 인정[^2] — 매일 가짜 STALE 4건 + 불필요 재실행 제거. 시뮬 4케이스 통과 (진짜 4일+ 스테일은 여전히 경보).
- B-3 ✅ `portfolio_brain.py` `_format_cfo`: 리포트 헤더에 "메인A 계좌 기준 — 종목 수 불일치는 정상" 라벨[^3] — AI의 매일 허위 정합성 경고 차단. 실호출 검증.
- B-4 ✅ `data_health_check.py`: 원자재 신선도 게이트 7일 추가[^4] — 3/26 유물이 ✅ 통과하던 구멍 봉쇄. 내일부터 원자재 ❌로 정직 표시(B-12 결정까지), 건강검진 17/18→16/18 예상. 시뮬 4케이스 통과.
- B-5 ✅ `bat_d_health_check.py`: 소요시간 "?분" 원인=완료 echo 레이스 → 당일 한정 현재시각 폴백으로 계산 복원 + **절대임계 140→175분**(6/23~7/15 실측 17일: 133.5~150.5분, 평균 140.4 — 기존 140은 절반의 날 오경보)[^5]. 시뮬 4케이스 통과.
- B-10 ✅ `upload_flowx.py`: 외국인 0건일 때 `FAIL` 대신 `SKIP (소스 stale)` 표기[^10].

**배포**: `dced605` 푸시 + VPS 일회성 예약(15:35 KST, nohup) — 장중 금지 준수, BAT-D(16:30) 전 반영 → 오늘 밤 6건 전부 첫 실동작. **⚠️ 퀀트봇 저장소는 자동배포 없음 확인** — crontab의 deploy_pull.sh(17:45+06:00)는 `REPO=/home/ubuntu/bodyhunter` 단타봇 전용. 배포는 항상 수동 git pull(또는 일회성 예약).

**마감 성찰 (3질문)**
1. 오탐/누락: 7/10 메모리 "VPS 자동배포 존재"를 믿고 자동 반영을 전제할 뻔 — deploy_pull.sh 내용을 직접 열어 단타봇 전용임을 확인. **메모리도 재검증 대상**. 또 EWY "헤더검출 완화"(7/14)는 진짜 원인(전역 봇차단)을 못 본 표면 처방이었음.
2. 내 판단 오류: 어제 백로그 B-1을 "파서 수정"으로 프레이밍 — 실제로는 소스 자체가 죽어 파서 수정으로 해결 불가. 증상(파싱 실패)이 아닌 원인(응답 내용물)부터 확인했어야 함.
3. 내일 개선 1건: 아침 루틴 A-6에서 오늘 배포 6건 실동작 검증 (특히 헬스체크 신규 판정: FLOWX 14/14 신선·원자재 ❌ 전환·소요시간 실측값·SKIP 표기).

---

## 각주 (발견 근거)

[^1]: 7/15 18:46:18 `[EWY] CSV 헤더를 찾을 수 없음 → 파싱 결과 0종목 — 중단 (exit 1)`. 7/14 픽스(`5202162`)는 헤더검출 완화 + 조용한 실패 가시화였는데, 완화로도 헤더를 못 찾음 = **소스 CSV 포맷 자체가 변경**된 것으로 추정. iShares 실물 CSV를 받아 포맷 대조 필요. BAT-D "실패 1건"의 정체가 바로 이것.
[^2]: 7/15 로그: 헬스체크가 18:45:08에 quant_fib_scanner·quant_alpha_scanner·quant_market_ranking·quant_bluechip_checkup 4종을 STALE(7/14) 판정 + 텔레그램 "수동확인" 발송 → 직후 18:45:30~18:46:16에 4종 모두 Supabase POST 201 정상 업로드. 체크 시점이 해당 업로드 단계보다 앞서는 **순서 문제**. 이 4종은 체크 대상에서 지연 검사하거나 헬스체크를 BAT-D 최종 단계로 이동해야 함.
[^3]: 7/15 18:02 v3_brain 경고 "CFO 리포트 3종목 vs 실제 포트폴리오 10종목". 검증 결과: CFO(`portfolio_cfo.py`)는 `paper_portfolio.json`(메인A, 3종목)을 읽고, v3_brain은 `paper_bluechip.json`(10종목) 계좌인데 `data/cfo_report.json`을 그대로 프롬프트에 주입. AI가 원장 차이를 "데이터 정합성 문제"로 오인해 **매일 경고 생성 + 토큰 낭비**. 최소 픽스 = CFO 리포트에 "메인A 기준" 라벨 명시, 근본 픽스 = 계좌별 CFO 리포트.
[^4]: 7/15 헬스체크 `원자재: wti=103.06 ... (2026-03-26)` — 약 4개월 전 데이터인데 ✅ 통과. 원자재 항목에 신선도(날짜) 검사가 없음. 시나리오 대시보드가 이 값을 쓴다면 stale 위험.
[^5]: 7/14 전체검수에서 확인 — 실소요 ~148분 > 기존 임계 140분이라 "복원"하면 매일 오경보. 실측 분포 기반 재보정(예 175분) 후 복원해야 함. 현재 BAT-D 메트릭에 "시간: ?분"으로 비활성 상태.
[^6]: 7/15 18:03 `수급 조회 실패 ('isin')` 21건 — KODEX 레버리지·인버스류. 점수 영향 max 85(저위험)로 7/14 검수 판정. pykrx ETF 미지원이 원인, etf_investor_flow.json 재배선이 대안.
[^7]: 7/14 검수: 파일명 공백 때문에 pre-commit 훅이 `quant_bot`+`(1).py`로 쪼개 `quant_bot_advisory` 테이블명과 오탐 매칭 → 커밋 차단. 실참조 0 확인 완료. 삭제하려면 훅 수정 또는 `--no-verify` 승인 필요.
[^8]: 7/14 검수: 3종 수집기 비활성인데 orchestrator에 참조 잔존. 데이터도 코드도 죽어있으나 삭제는 보존 판단 필요 (research/backtest 대량·data/ 추적물 포함).
[^9]: 7/14 검수: except-pass 70건 — 일괄 수정은 회귀 위험이 커서 기각, 접점 수정 시 per-site 로깅 추가로 합의.
[^10]: `flowx_uploader.py:759` 신선도 가드가 의도적으로 빈 목록을 반환하는데 업로드 결과가 `외국인=FAIL (0건)`으로 출력됨. `SKIP(소스 stale)` 표기로 바꾸면 매일 로그 판독 시 오해 제거. 근본해결은 단타봇 외인 소스 재연결(외부 대기, 3봇 분업).
[^11]: 7/16 검증: 기존 URL(`.ajax?fileType=csv`)이 CSV 대신 상품페이지 HTML(200, 1.3MB)을 반환. UA 위장·쿠키 선방문·`siteEntryPassthrough`·XHR 헤더·blackrock.com 미러·curl_cffi Chrome TLS 위장 전부 무효, 로컬(가정 IP)·VPS(AWS IP) 동일, IVV도 동일 = **사이트 전역 차단**(Source Defense JS 챌린지 필요). VPS `data/ewy/` 마지막 성공 파일 5/15 — 두 달간 조용히 죽어있었음(7/14 exit1 가시화 픽스가 드러냄).
[^12]: 7/16 검증: 살아있는 `liquidity_signal.json`(매일 06:10 갱신)은 FRED 유동성 지표만 있고 `commodities` 빈 dict. 원자재 실데이터는 `commodity_prices.json`(3/26)·`data/commodities/`(3/27)·시나리오 파일(4/8) 전부 유물이며 수집기는 `scripts/archive/orphan_20260421/fetch_commodity_prices.py`로 아카이브됨. 헬스체크는 B-4 픽스로 정직해졌으나 데이터 자체의 부활/제거 결정 필요.
[^13]: 7/17(제헌절) 확인: crontab은 `* * 1-5`(요일 기반)뿐, run_bat.sh·update_daily_data.py에 휴장(holiday) 가드 grep 0건. 평일 휴장일에도 전체 배치가 돌아 TODAY 기준 체크·수집이 헛돌 수 있음. **7/20 실측 확정**: 7/17 건강검진이 🚨 **D등급 13/18**로 종가 0%·수급 0종목·DART "0건 수집 — 비정상(장 운영일)"을 실패로 판정, 가짜 경보 발송. → `kis_holiday_adapter`(CTCA0903R) 신설로 해소 완료.
[^15]: 7/20 A-0 실측: 메인A 누적 -11.8%(α **-29.1%p**)로 5계좌 중 유일한 음수 알파이며 격차가 벌어지는 중. 방어 계좌들의 양수 알파는 폭락장 한정 창이라 반등 시 동시에 무너진다. → **7/20 저녁 B-15로 해소**(`3b73962`): 근본 원인은 부품 부재가 아니라 **연결·통보 부재**였다(`run_scenario_v1.py` 알림 코드 0줄). 사전 백테스트 2건으로 타이밍 축은 배제하고 반응 속도 문제로 재정의. 남은 관문은 B-16(집행 경로).
[^18]: 7/20 밤 알파 측정(각 거래의 **실제 보유기간 KOSPI 수익률**을 차감). 퐝가님 반론("시장에 따라 다른 거 아닌가, 지금 하락장이라 그런 거 아닌가")이 촉발한 재검증. 결과가 통념을 뒤집음 — **하락구간 알파 -0.23%p(시장과 동행)** vs **상승구간 알파 -3.08%p(시장을 크게 미달)**. 즉 손실의 원인은 하락장이 아니라 **상승장 미참여**이며, 이는 6/27 부검("KOSPI +51.4% vs 메인A -4.5%")·7/4 BRAIN 진단("강세 -40%p 미참여")과 동일 현상의 세 번째 확인. 전 전략 알파 음수(SCAN -1.77 / AI_BRAIN -3.37 / ALPHA -3.59%p)이므로 **개별 전략 차단으로는 해결되지 않는다**. ★방법론 교훈: 절대손익·승률은 베타에 오염된다 — 전략 판정은 반드시 동일구간 시장 대비로.
[^17]: 7/20 밤 메인A 부검(청산 90건, 실현 -569.7만원, 승률 38.9%). **전략별**: SCAN 74건 **-594.3만원**(승률 36.5%) / ALPHA 8건 -14.4만 / **AI_BRAIN 8건 +39.0만(승률 62.5%·평균 +2.17% — 유일한 순익 전략)**. **청산사유별**: STOP_LOSS 34건 **-922.5만원**(승률 0%·평균 -8.32%) vs TAKE_PROFIT 5건 +282.4만(+22.91%)·TRAILING_STOP 8건 +85.7만(승률 100%). 즉 손익비는 나쁘지 않고 **승률이 문제**. **월별 SCAN**: 4월 +170.1만 → 5월 -305.2만 → 6월 -267.7만 → 7월 -191.6만(3개월 연속 손실). **코드 확인**(`scripts/paper_trading_unified.py:388-403`): `has_strong`과 `grade_kr in ("강력 포착","적극매수")`만 `continue`(차단)하고, `("포착","매수")`는 `grade="A"`로 **진입 허용**된다. 현재도 DL이앤씨·HMM 2종목 SCAN 보유 중.
[^16]: B-15가 후보를 제시해도 freeze(실주문 0)라 실제 수익은 발생하지 않는다. unfreeze 잔여 관문은 E 게이트(페이퍼 20일 누적)뿐. 스코어보드가 페이퍼가 아닌 실계좌 숫자를 찍는 날이 "얼마 벌었나"에 0원 아닌 답을 하는 시점.
[^20]: 7/23 실측 — `collect_candidates()`를 오늘 `tomorrow_picks.json`(picks 634건)으로 직접 실행(원장 md5 무변경 확인). **차단 2건**(성호전자·LG이노텍, 둘 다 `PULLBACK15_DUAL`) → **둘 다 `strategy=SWING`·`grade=A`·score=100으로 최종 후보 생환**. 동시에 **ALPHA/AA 9건이 전부 STRONG 시그널 보유인데 차단 검사를 거치지 않았다** — 섹션2가 `picks_sorted[:15]`만 순회하기 때문. 즉 우회는 두 경로다: ①상위 15개 밖 STRONG → 섹션4에서 AA 직행(규모 큼) ②차단된 상위 15개 → 섹션3 SWING으로 생환(규모 작음). ★**봉쇄를 보류한 이유**: 같은 날 백테스트에서 차단 대상 AA는 전 지평 무의(p=.22~.49)인 반면 통과되는 A가 유의하게 나쁘다(D+10 -4.89%p p=.020·D+20 -11.66%p p=.018). 구멍을 막는 것이 곧 수익 개선이라는 근거가 없고, 오히려 차단 정책의 방향이 뒤집혔을 가능성이 있다 → 근거 확보 전까지 코드 무변경.
[^24]: 7/24 측정 3종(`analyze_exposure_adjusted_alpha`·`analyze_shield_recovery_gate`·`backtest_reaction_speed`, VPS 실행). **★최대 교훈 = 신설 지표는 극단 케이스로 검산할 것**: αEW는 "계좌가 100% 현금이면 +9.8%p로 시장을 이긴다"고 나오는 지표였다. 이 한 줄 검산이면 어제 배포 전에 잡혔고, 그랬으면 "종목선택 무죄 → 자산배분이 문제 → SHIELD를 풀자"는 **정반대 처방 3연쇄**로 가지 않았다. 노출도 보정 후 실제는 메인A -7.3%p·B안 -8.2%p. 부수: 7번째 타이밍 축(반응 속도)도 기각 — MA20 역방향·MA60 엣지 0, 20일 신고가는 지연 D+0~D+10에 엣지가 소멸하지 않아 **"빨리 붙는 것"이 관건이 아님**이 드러나며 가설 전제 자체가 무너짐. 구조격차는 평시 중앙값 +0.06%p(동전 던지기)이나 현재 97.4 백분위이고 **평균회귀가 아니라 양의 자기상관(r=+0.290)**이라 "버티면 돌아온다"도 기각.
[^23]: 7/23 백테스트(`scripts/backtest_scan_vs_signal_top.py` + `_robust.py`, VPS 실행). **★방법론 교훈이 결과보다 크다**: [[feedback_judge_by_alpha_not_pnl]]("절대손익 대신 시장 대비")를 무비판 적용해 KOSPI 차감 알파로 쟀는데, **잣대인 시장이 시총가중이면 개별종목 전략 판정에 부적합**하다 — 유니버스 1,180종목 동일가중 평균조차 KOSPI 대비 D+20 -9.9%p(상승구간 -25.5%p)라 "모든 개별주 전략이 음수"라는 무의미한 결론만 나온다. 실제로 7/20 밤 B-17 철회가 이 잣대로 내려진 판정이었고, 기저선 기준으로는 재현되지 않는다(A등급 D+10 -4.89%p p=.020·D+20 -11.66%p p=.018 유의 / AA는 무의. **단 n=12~17·다중검정 2등급×3지평 미보정이라 확정 아님 — 철회 근거가 흔들린다는 것까지만**). 기저선 없는 알파는 알파가 아니다. 부수: 7/21 "중소형주 반복 진입" 표현 정정(실제 Q5 중앙 6.79조 19건·Q4 15건으로 중대형주 다수). 같은 날 발견한 `_check_price_data` 샘플링 표기(B-19 ③)도 이 실행 중 실측 대조에서 드러났다.
[^21]: 7/21 백테스트(`paper_portfolio.json` STOP_LOSS 35건 전수, FDR, VPS 실행). **가설**: "손절이 상승장 미참여의 기계적 범인"(어제 부검의 STOP_LOSS 승률 0%·-8.32%가 촉발). **결과 = 기각**: 손절일 종가 대비 D+5 -3.28%(양전 38%)·D+10 -4.77%(양전 17%)·KOSPI 차감 알파 D+10 -6.48%(양전 29%)로 손절 후 계속 하락 = **손절이 추가 손실을 막았다(무죄)**. 월별 단조 악화(4월 +10.1→5월 +1.1→6월 -7.3→7월 -12.6%). 이상치 로보스타(+83.4%) 제거 시 중앙값 -9.49%로 결론 강화. **범인 재특정 = 진입**: 35건 전부 SCAN, 하락 지속 중소형주 반복 진입. 오늘 시그널 엔진은 삼성전자·삼성생명(FOREIGN_SURGE_PB) TOP5를 뽑았으나 메인A는 미사용. ★방법론 교훈: **청산 사유의 승률(STOP_LOSS 0%)은 그 종목이 원래 나빴는지를 말하지 못한다** — 절대손익 함정이 손절 판정에도 적용(→ [[feedback_judge_by_alpha_not_pnl]]).
[^27b]: 7/27 데이터계약 전환 — 웹봇 폴더에 7/24 저녁 도착한 지시서 2건(FLOWX 전면개편 마스터·데이터계약변경 매매판단필드 중단)을 3거래일 방치 후 오늘 인지. 개정 자본시장법(유사투자자문 규제) 대응으로 시그널/픽/추천/성과를 폐지하고 데이터·교육 플랫폼으로 전환, 퀀트봇=계산봇. 매매판단 16테이블 적재 차단 배포(`b7ae38b`, 내일 BAT-D부터). 유지 확정: scenario_dashboard·leader_cycle(공개페이지 직결 — 웹봇 d727478이 내 분류 오류 2건 지적). 웹봇도 나도 "출력 잘림·범위 미확인으로 0건/누락 판정" 실수를 함(웹봇 head-70 잘림 / 나 BAT미완 조기확인). ★재발방지 = 봇 수신함 아침 확인(A-★) + 목적함수 재설정(수익→계약준수·산출물 정확성). 상세 [[session_state_7_27_data_contract_pivot]].
[^27]: 7/27 발견 — 7/24 수급이 금요일 저녁엔 2,640종목이었으나 지금은 2,827로 채워짐(7/23과 동일·누락 0). 금요일 18:25 조기 확인분은 **BAT-D 미완 부분수집**이었고, "187종목 껍데기라 KIS가 뺐다·재조사 불필요"(`ccb8da2` 커밋)는 오진. 실체: **껍데기 종목(실거래 0)은 BAT-D 후반에 뒤늦게 채워진다**. 실거래값 있는 종목은 조기 시점에도 이미 다 들어와 있어 "실질 손실 없음"만 맞았다. 7/27 현재도 2,641종목(진행 중, 실거래 12,132행 확보)으로 동일 재현. ★교훈: **BAT-D 진행 중·장중 확인 시 수급 종목수는 과소평가되므로 종목수로 이상 판정 금지 — 실거래값 유무만 본다.** 지난주 금요일 187종목 조사는 이 함정 탓의 헛수고였다.
  **★7/28 정정 — "BAT-D 후반에 채워진다"는 틀렸다**: 7/28 BAT-D 완주(18:58) 후 19:30에 실측해도 2,643종목(껍데기 **2**)에 그쳤다. 반면 7/27=2,827(껍데기 193)·7/24=2,827(205). 즉 **껍데기 백필은 당일 BAT-D 안에서 일어나지 않으며**, 다음날 이후 어느 시점에 채워진다(시점 미특정 — 7/29 아침 7/28분이 2,827이 되는지로 확정 예정). 관측 2회로 메커니즘을 단정한 것이 성급했다. 다만 **가드의 결론(종목수 무시·실거래값으로 판정)은 그대로 유효**하다.

[^28]: 7/28 계약이행 검증(`scripts/verify_contract_suspension.py`, 읽기전용 SELECT). **★핵심 = 생산자 판별법 확보**: 퀀트봇 FLOWX 업로드는 KST **18:40**(BAT-D 종반)·**18:47**(BAT-F 재시도)에만 일어나므로, 당일 레코드의 `created_at` 시각대로 퀀트봇 적재분과 타 봇 적재분을 가를 수 있다. 실제로 `dashboard_sniper`·`dashboard_etf_signals`·`dashboard_relay`는 7/24·7/27에 **16:3x + 18:40** 두 시각이었다가 7/28엔 **18:40만 소멸** — 차단이 작동했다는 직접 증거다. 이 판별을 하기 전 1차 판정은 "위반 5건"이었고, **도구는 사실을 맞게 보고했는데 내가 귀속을 안 물어서 오판**할 뻔했다(웹봇도 동일 오귀속 "미이행 2건"). ★교훈: **"우리 것인가"를 먼저 묻지 않으면 남의 적재를 내 위반으로 읽는다.** 부수 확정: `etf_recommendations.json`이 4/8자 사장 파일이라 퀀트봇은 `quant_etf_recommendation` 7/28 레코드를 만들 수 없음 / 웹봇 테이블명 3건 접두 오류(`dashboard_relay`·`dashboard_crash_bounce`·`quant_etf_recommendation`) 정정 → 중단 확인 8건이 아니라 **9건** / `quant_ewy_holdings` 스케줄 `09:46`→**`18:45`** 실측 확정(7/24 18:48·7/27 18:46·7/28 18:47).

[^31]: 7/31 웹봇 조회(`4a12767`)에서 시작된 `dashboard_smart_money` 거짓 0 규명·조치(`22797ca`·`7b9b427`). **★교훈 두 가지.** ⑴**"적재됐는가"만 보는 감시는 값이 틀린 것을 영원히 못 잡는다.** 자가검사는 6주 내내 `✅ 정상 적재`를 찍었고 그동안 `price=0`이 매일 나갔다. 발견자는 우리가 아니라 **화면을 보는 웹봇**이었다 — 우리 파이프라인은 자기 산출물이 화면에서 어떻게 보이는지를 보지 않는다. ⑵**공동 적재 테이블에서 "전량 X" 규칙은 생산자를 가르지 않으면 미탐된다**: 60행 중 퀀트봇 50행만 0이고 타봇 10행은 실값이라 전체로 보면 조건이 성립하지 않는다. 7/28에 확보한 시각대 판별법을 값 검사에도 재사용해 해소. **부수 확정**: `/api/smart-money`가 `score DESC LIMIT 50`이라 퀀트봇 score 107~110이 상위 50칸을 정확히 다 채워 정보봇 실값 행을 51위 밖으로 밀어냈고, conflict=(date,ticker) upsert가 정보봇 실값 price까지 0으로 파괴하고 있었다(지문 = 한국가스공사 `created 08:05`인데 `signal_type=DUAL_FLOW`·`price=0`·`exec_strength=108.7`). **★선언↔실행 누락 3회째**: 6/14 웹봇 권고(옵션1 OHLCV-fill)에 "구현 가능"이라 답하고 6주간 미구현이었다(7/28·7/30에 이어 같은 패턴). 약속한 것은 백로그에 등재해야 살아남는다.

[^31b]: 7/31 오후 B-26② 확장 중 실측(`ad7e800`). **★교훈 = 감시를 확장하려다 그 감시가 얼마나 안 보고 있었는지를 알았다.** 오전에 "탐지 성공·오탐 0"으로 배포한 §2.5는 실제로 ⑴유지 5종 중 **2종만** 보고 있었고(JSON형 3종은 통째로 미검사) ⑵그 2종조차 **같은 날 다른 봇이 실값을 넣으면 미탐**이었다(7/29 정보봇 19:39 193행이 퀀트봇 38행의 전량 0을 덮음 — 7/30에 잡힌 건 그날 18시대에 우리만 있었던 우연). ⑶그리고 근본적으로 **`created_at`으로는 생산자를 확정할 수 없다**: `on_conflict` upsert가 UPDATE라 최초 생성 시각이 유지되기 때문이고, `quant_sector_flow`가 POST 200을 두 번 받고도 created_at 16:56 고정인 것이 증거다(→ B-32). 세 겹을 다 뚫고 나서야 **"우리가 보낸 페이로드를 우리가 직접 본다"**는 답에 도달했다 — DB를 조회해 추정하는 모든 방법은 남의 쓰기와 섞이지만, 우리 페이로드는 섞이지 않는다. ★부수 교훈: **오전에 지문을 손에 쥐고도 함의를 확장하지 못했다** — 한국가스공사 행(created=정보봇 시각, 내용=퀀트봇)을 "덮어쓰기 피해 증거"로만 읽고, 그것이 곧 "우리 생산자 판별법이 덮어쓰기를 못 본다"는 뜻임을 그 자리에서 잇지 못했다. 같은 사실이 두 결론을 가리킬 때 하나만 보고 넘어가는 버릇.

[^32]: 8/1(토·휴장) 오탐 억제(`ce8f0a2`)·웹봇 요청 3건(`81a39f6`)·B-32 해소(`2e9be51`). **★교훈 세 가지.** ⑴**감시를 만든 다음 날, 그 감시가 낸 첫 경보는 전부 거짓이었다.** "전량 0"은 **0이 정당한 값**(감산 전용 지표·범위 지표 바닥)과 **채우지 못한 0**을 구분하지 못한다. 어제 넣은 `price` 픽스는 옳았지만 같은 규칙으로 만든 판정기는 세 컬럼을 오인했다 — **탐지기를 만들 때는 "무엇을 잡는가"만큼 "무엇이 정상인가"를 정의해야 한다.** ⑵**★★내 억제 설계가 검증에서 뚫렸다**: 처음엔 "과거에 실값을 보낸 적 있으면 0도 가능한 값이니 억제"로 짰는데, 그러면 **정상 작동하던 컬럼이 0으로 고장나는 가장 흔한 결함이 영구 침묵**한다. 7/31 `price=0`이 잡힌 건 처음부터 0이었던 덕이지 규칙이 좋아서가 아니었다 — 실값을 쓰다 회귀했다면 못 잡았다. 억제 근거를 "이미 알려진 지속 상태인가"로 바꿔 급변은 언제나 경고하고 연속 0은 주기 재알림으로 돌렸다. **7/30 교훈("감시 도구는 자기 실패 모드로 검증하라")이 이틀 연속 필요했고, 이번엔 배포 전에 잡혔다.** 같은 방식으로 score 재배분 첫 안도 배포 전 기각했다(보너스를 절반으로 줄였더니 실제 연속일수 7~10이 상한 5에 포화돼 50행 **전부** 100 — 내가 비판한 클리핑보다 나빴다). ⑶**B-32는 남이 풀어줬다**: `created_at`이 UPSERT UPDATE에서 갱신되지 않는다는 것까지는 7/31에 도달했으나 대안이 없었는데, 정보봇이 **`xmin`**(마지막으로 쓴 트랜잭션 ID)으로 반증을 내고 웹봇이 전달했다. 실측 34% 미탐(50행 중 33행만 보임)이 xmin으로 50/50 정확해졌다. **웹봇도 같은 날 자기 판정을 철회했고 나도 같은 걸 놓쳤다** — 7/30 한국가스공사 지문이 이미 손에 있었는데 "덮어쓰기 피해 증거"로만 읽었다. ★부수: 웹봇의 처방(`min(score,100)`이면 편중이 저절로 풀린다)을 실측으로 반증했다 — 우리 최저 97 > 정보봇 최고 70이라 성립하지 않는다. **받은 처방도 검산 대상**이며, 근본 해소는 `source` 컬럼(B-33)이다.

[^34]: 8/1 코덱스 켈리 작업분 검수(미커밋). **★교훈: 안전한 방향이라고 해서 근거까지 맞는 건 아니다.** 코덱스 구현은 결과적으로 안전하다 — `kelly_cap 1.0`이라 증액이 불가능하고 항상 감산 방향이며, 사장님 요구("기존 게이트 수량을 넘지 않는 선에서 좋은 신호만 덜 줄인다")와 정확히 부합한다. 그런데 **그 안전함이 켈리 이론에서 나온 게 아니다**: 켈리 `f*`는 "자본 중 몇 %를 걸어라"인데 코드는 그 값을 기존 수량에 곱한다. 즉 "켈리 값을 감산 계수로 전용"한 것이고, `f*`가 0.6→0.8일 때 수량이 1.33배가 되는 이 비율에는 이론이 부여한 의미가 없다. **이름을 `kelly`로 두면 나중에 누가 "켈리니까 f\*만큼 자본을 태워야 한다"고 읽어 사고가 난다** — 지금 안전한 것과 계속 안전한 것은 다르다. ★부수 교훈 둘: ⑴**"실측으로 바꾸면 더 정확하다"가 항상 참이 아니다** — b를 하드코딩 1.5에서 `avg_win/avg_loss` 실측으로 바꾼 건 옳은 방향이지만, 무변동(0%)을 패배로 세며 손실을 0으로 더하는 편향이 b를 **4배까지 부풀린다**(실증: kelly 0.200→0.500). 편향을 고치기 전의 실측은 하드코딩보다 **더 위험**하다. ⑵**켈리 검증은 노출도를 맞추지 않으면 자동으로 이긴다** — 켈리는 정의상 노출을 줄이는 장치라 수익률·MDD만 비교하면 "MDD가 줄었다"가 공짜로 나온다. 7/24 αEW 노출도 편향(계좌 30% vs 벤치마크 100% → "덜 투자한 것"이 능력으로 둔갑, αEW +3.3 → αADJ -7.3%p)과 **완전히 같은 함정**이고, 그때 만든 αADJ가 여기 그대로 필요하다. ⑶단위 추측 코드(`hit_rate > 1`)는 7/31 웹봇 `formatBil` 사고와 동류 — 생산자 2곳 모두 백분율로 확정 저장하는데 소비자가 값 크기로 추측한다. `1.0`(1% 승률)을 100%로 읽고 인접값 `1.5`는 1.5%로 읽어 **판정이 66배 갈린다**.

[^33]: 8/1 웹봇 순서 리스크 지적 수용(`4ff71df`). **★★교훈: 계약을 지키는 조치가 감시 능력을 없앨 수 있다.** 나는 `signal_type` 한글 통일을 "공유 테이블 계약 준수"로만 보고, **그 영문 라벨이 사실상 `source` 컬럼 역할을 하고 있다**는 걸 못 봤다. 웹봇이 지적한 근거가 결정적이다 — **7/31 `price=0` 6주 사고를 웹봇이 잡아낸 유일한 근거가 "`DUAL_FLOW`인 행과 `price=0`인 행이 1:1"이었다.** 내가 하루 먼저 라벨을 통일했다면 그 사고는 더 오래 살아남았다. `created_at`은 B-32로 못 쓰고 `source`는 아직 없으니, 그 구간 동안 **양쪽 다 눈이 먼다**. → 계약은 코드에 남기고 적용만 플래그로 잠가 B-33과 같은 배포에서 켠다(끊기는 구간 0). ★부수 교훈 둘: ⑴**웹봇도 같은 날 자기 처방을 철회했다** — "척도를 통일하면 편중이 저절로 풀린다"를 재배분 규칙까지 직접 구현해 시뮬레이션한 뒤 **50/0으로 더 나빠짐**을 확인하고 철회했고, 현재 정보봇 1칸도 척도 덕이 아니라 우리 `price=null` 1행이 후순위 정렬에 밀린 자리였음을 짚었다. **내 반론(min/max 두 줄)보다 웹봇의 검증이 한 단계 깊었다** — 나는 "성립하지 않는다"까지만 갔고 원인과 더 나쁜 결론은 못 봤다. ⑵**대체 판별 수단으로 제시한 score 범위(97~100)도 약하다** — 정보봇이 90점대를 내면 깨질 뿐 아니라, **우리 쪽 사정으로도 깨진다**(STRONG이 50개 미만인 날엔 MODERATE 60~80이 올라와 정보봇 최고 70과 대역이 겹친다). 회신에 "판별 근거로 쓰지 말라"고 명시했다. 7/28에 시각대로 생산자를 단정했다가 그 판별법 자체가 틀렸던 것과 같은 함정이다.

[^35]: 8/1 켈리(B-34) 전제를 파다가 나온 발견. **★★교훈: 내가 "데이터가 없다"고 실증한 것부터 틀렸다 — 로컬 사본을 봤다.** 이 저장소는 `data/`가 gitignore이고 런타임 정본은 VPS인데(메모리에 명시된 원칙), 코덱스에게 그 오판을 근거로 넘겼고 코덱스 판정도 그 위에 섰다. **7/27·8/1 두 번 다 "0건/비었다"를 성급히 말했다**([^0건주장] 계열 3회째). VPS 실물엔 엔진 10개·표본 167건이 있었고, 코덱스가 요구한 `win+loss>=100`도 이미 넘겼다. ★그런데 파고드니 **켜면 안 되는 더 강한 이유**가 나왔다: ⑴같은 파일·같은 키를 두 생산자가 쓰고 **올바른 전방평가(16:50, D+3·다음날 시가)가 후방평가(18:53, 당일 수익률)에 매일 파괴된다** — 로그는 양쪽 다 성공이라 찍는다(7/31 `price=0`과 동형, 이번엔 로컬 JSON) ⑵살아남는 값은 **순환 논리** — `tomorrow_picks.json`("내일" 살 종목)이 18:43에 만들어지고 18:53에 **"오늘" 수익률**로 채점된다. 신호 생성 10분 전에 확정된 값으로 그 신호를 평가하니 전방 검증이 아니다 ⑶그 92.8%는 **시장 베타** — 7/31 KOSPI **+17.91%** 폭등일이라 전체 1,182종목의 **89.7%가 상승**(평균 +8.66%), 신호 167종목은 93.4%·+9.88%로 **초과분 +3.7%p뿐**인데 저장은 92.8% 절대값. **★★7/24 αEW 교훈("비교 대상의 기저선을 맞춰라")을 사이징 판정에는 적용해놓고 입력 승률에는 적용하지 않았다** — 켈리 판정지표로 αADJ를 요구하면서 정작 `p`는 절대 승률을 쓰는 앞뒤 안 맞는 상태였다. 같은 교훈이 두 자리에 필요한데 한 자리에만 적용돼 있었다.

[^36]: 8/6 미배포 발견·B-39 원인확정. **★교훈 세 가지.** ⑴**파이프라인에서 유일하게 사람 세션에 의존하는 단계가 배포인데, 그 실패를 보는 눈이 없었다** — 8/5 저녁 세션이 안 열리자 푸시된 픽스가 이틀치 BAT-D에 못 탔고, 로그·HEALTH·자가검사 어디에도 "코드가 낡았다"는 신호가 없다. 감시는 데이터만 보고 코드 신선도는 안 본다(→B-42). "저녁에 배포하겠다"는 선언은 세션이 열려야만 실행되는 약속이었다 — 선언↔실행 누락 계열의 4번째, 이번엔 사람이 아니라 **구조**로 막는다. ⑵**B-39는 "적재됐는가" 감시의 parquet판** — HEALTH 수급 항목이 존재·커버리지만 보고 값을 안 봐서 스마트머니 4요소 중 1개가 3.5개월 거짓 0이었다(7/31 price=0과 동형, 이번엔 내부 파일). 컬럼이 4/15에 추가되고 4/18 리팩토링으로 이틀 만에 죽은 것도 상징적 — **새 컬럼은 태어난 직후가 가장 위험하다**(첫 실동작 확인이 없으면 죽은 채 태어난 걸 모른다). ⑶**수정 전 단위·의미 검산이 이번엔 먼저 왔다** — DB net_val을 잇기 전에 겹침 구간(4/10~16) 대조로 소수점 일치를 확인했고, 그 과정에서 "3~4천억 이상값 의심"이 자사주 매입기 실값으로 판명됐다. 7/24("비교 대상의 잣대부터 검산")·8/5(mtime만 보고 의심) 교훈의 적용 성공 사례. ★부수: A-★(수신함 확인)를 세션 시작에 안 지켜 마스터 지시서를 마감에야 발견(1일 지연) — 루틴 항목은 순서까지가 루틴이다.

[^37]: 8/7 백필 완결·결함 3건 신규. **★★교훈 넷.** ⑴**성공 로그가 소비처 도달을 뜻하지 않는다** — 백필은 `1,209파일 265,456칸, 실패 0`을 찍었고 실제로 `data/raw`를 정확히 채웠다(DB와 소수점 일치). 그런데 계산이 읽는 건 `data/processed`였고 둘을 잇는 지표 재계산이 돌기 전이라 **소비처는 하나도 안 바뀌었다**. 게다가 "0/NaN만 채운다"는 안전 설계 때문에 이 작업은 **실패할 수가 없는 로그**를 낸다 — 성공률 100%가 무의미한 지표였다. **"완료"의 정의를 "쓰기 성공"이 아니라 "소비처에서 읽히는가"로 잡아야** 한다. 7/31 price=0(적재는 됐는데 값이 틀림)의 한 겹 위 버전 — 이번엔 **값도 맞고 쓰기도 성공했는데 위치가 소비 경로가 아니었다**. ⑵**단일 날짜 스냅샷으로 판정하면 부호가 뒤집힌다** — "8/7 기타법인 1,176개 전량 0"을 보고 수정 실패로 판단했는데, 시계열을 펴니 7/24~8/6이 채워져 있고 그 경계가 `8/7−14일`과 정확히 일치하는 **작동 증거**였다. 하루만 보면 고장과 정상이 같은 모양이다. ⑶**한 컬럼이 그랬다면 이웃도 의심하라** — B-39 하나를 검증하러 갔다가 같은 유형 3건이 옆에서 나왔다(연기금 401일·공매도 5.5개월·수집이력 2개월). 공통 구조는 **"소스가 없거나 끊겼는데 컬럼은 0으로 남아 계속 계산에 들어간다"** = FLOWX 지시서 §1 "결측을 0으로 채우지 않는다"의 반복 위반. 컬럼 단위 결함은 **단독으로 존재하지 않는다**. ⑷**소비처를 추적하기 전에 심각도를 말하지 말 것** — 연기금 401일 0을 보고 "퀀트봇 담당 축이 통째로 죽었다"로 갈 뻔했으나, 주 신호(`future_value_engine`·`signal_engine`)는 DB를 직접 읽어 **무사**했고 죽은 건 parquet 경유 v8 가점뿐이었다. 발견의 충격과 실제 영향은 별개다. ★부수: A-★ 수신함을 **어제 반성해놓고 오늘 경로 하나만 확인**해 웹봇 지시서를 놓칠 뻔했다(git `docs/` 커밋으로 도착, VPS pull에서 우연히 발견). 루틴을 "지켰다"고 말하려면 **경로 전부**를 봐야 한다.

[^38]: 8/9 전방 페이퍼 커밋 전체검수. **★★교훈 셋.** ⑴**검수 도구의 실패를 아무도 검수하지 않았다** — `self_review_checklist`가 6/10부터 8/9까지 **277건 전수 EXIT 1**, PASS는 0건이었다. 실패 문자열이 매 커밋마다 `codex_inbox` JSON에 그대로 적혔는데 두 달간 아무도 열어보지 않았고, `risk: critical` 변경조차 그 체크를 건너뛴 채 통과했다. 원인은 호출부 한 줄에 `-X utf8`이 빠진 것 — **CLAUDE.md에 명문화된 규칙이 정작 그 규칙을 강제하는 도구의 호출부에만 빠져 있었다.** 7/30에 "감시 도구는 대상이 아니라 자기 자신의 실패 모드로 검증해야 한다"고 적었는데, 이번엔 **도구 자체가 아니라 도구를 부르는 쪽**에서 같은 일이 났다. 감시의 사각은 늘 한 겹 바깥에 있다. ⑵**"실행 안 된 코드는 완료가 아니다"를 검수자가 대신 실행해서 확인할 수 있다** — `mirror_forward_paper_ledger.py`는 BAT-J(평일)에 배선됐고 주말이라 8/10이 첫 실행이었다. 리뷰만 했으면 "코드상 문제없음"에서 끝났을 텐데, `--ledger` 임시경로로 실제 데이터를 돌려보니 1,263이벤트·63초라는 **성능 실측**과 멱등성 확인이 동시에 나왔다. 인자 하나로 부작용 없이 돌릴 수 있는 설계였던 것이 검수를 가능하게 했다 — **검수 가능성도 설계 품질이다.** ⑶**"0으로 채워진 필드"를 이번엔 설계자가 먼저 표시해 뒀다** — B-39(기타법인)·B-47(공매도)은 소스가 죽었는데 컬럼이 0으로 남아 아무도 몰랐던 사례인데, 이번 원장은 비용·`net_pnl`이 비어 있는 것을 `cost_complete=false` + `"not certified net return"`으로 **명시**했다. 진전이다. 다만 그 표시가 `metadata` 안쪽에 있어 소비자가 무시하기 쉬우므로, **표시했다는 사실이 소비 계약을 대체하지는 않는다** — 플래그는 만든 쪽 알리바이가 아니라 읽는 쪽 계약이어야 한다.
