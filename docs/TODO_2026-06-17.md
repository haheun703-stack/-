# 퀀트봇 TODO — 2026-06-17 (수)

> 6/16(화) 이월. 상세 맥락은 메모리 `session_state_6_16_valuation_upload.md` 참조.

## 🔥 우선 (바로 진행)

1. **한국 밸류밴드 적재** — `python scripts/upload_valuation_band.py --market KR --write`.
   - 조건: yfinance.KS rate-limit 회복 + **한국 장마감 후**(finality). 6/16엔 장중 rate-limit으로 한국 30종목 대부분 데이터부족이었음. 미국은 6/16 적재 완료(`b772b78`).

2. **서버 배포 점검** — collect_investor_kis BAT 배선(`1762f78`) 서버 반영 여부 결정(git pull).
   - ★동시에 **서버 게이트 audit 로그 확인** = unfreeze E 카운트 유효성의 핵심(아래 참조).

## 📌 페이퍼/unfreeze 점검 후보 (6/16 점검 결실)

3. ~~서버 게이트 audit 로그 점검~~ ✅ 완료(6/16) — **발견: 게이트 로그 0·서버 6/10·E는 시간 아닌 배선이 채움**. 아래 E-0로 전환.
   - 서버 HEAD `12ca898`(6/10)라 6/12~ 리스크엔진 미배포 + 페이퍼 엔진(`paper_trading_unified.py`) 게이트 미경유(`gate_check` 호출 0). 정보봇 교차검증 확정.

## 🔒 unfreeze E-0 (다음 집중세션 — 페이퍼 게이트 드라이런 배선)

**E(페이퍼 20거래일)는 E-0 배선 선행 후에야 카운트 시작.** unfreeze-checklist.md E-0 참조.
- `paper_trading_unified.py`에 `gate_check`(paper 모드 GATE-DRYRUN) 경유 추가 → 게이트 로그 축적 시작.
- ⚠️난이도=반나절급: 페이퍼 엔진이 어댑터/잔고포트 미사용(자체 체결 시뮬)이라 gate_check가 요구하는 `balance_port.fetch_balance()`(equity)·`ohlcv_loader`(adv20)를 **페이퍼 잔고로 어댑팅 + enforce 모드 설계** 필요. 퀀트봇 도메인 작업(정보봇 대행 불가).
- ★**B(E 1회 검증 완화)는 배제** — §0 "협상 불가" 위반(정보봇 권고 수용).
- 배선 후: 서버 배포 → 20거래일 누적 → E 충족.

## 📌 기타 점검 (6/16 완료/선택)

4. ~~§5-3 회피 사유 기록 점검~~ ✅ 충족(6/16) — candidate_log에 진입/회피 사유·hard_gate_notes("수급 hard gate 미사용") 빠짐없이 기록.
5. **죽은 코드 정리(보류)** — `paper_trading_unified.py:278-305` AI_BRAIN 루프. 사장님 "하지 않음" 결정(무해·페이퍼 코어 건드림). E-0 배선 시 함께 정리 검토.

## ⏳ 대기/조건부

- **정보봇 회신 대기** — verdict 5종 교차검증 / checkup stale(4/12) 점검 (6/16 회신서 `b6ac38e` 전달함).
- **2층/알림 실데이터** — 현재 골격(level=normal·null). 누적/MDD/현재DD·알림 JSONB는 unfreeze(포트 실운용) 후.
- **대기**: KRX 잠금해제 → 국적별(키움 opt10059) / PCE backfill(6월말) / scoreboard 재소유.

## ✅ 완료 (6/16)

- 데이터계약 **3테이블 전부 구현**: 밸류밴드(§1·미국30 실적재)·2층(§2)·알림(§3) 골격 + 적재기. 커밋 `93c5b17`·`b772b78`·`0ca13f6`.
- KIS 수급 BAT-D 배선(`1762f78`) + 정보봇 회신서(`b6ac38e`) + 슬러지 489MB 삭제.
- **페이퍼/unfreeze 점검**: 페이퍼 정상작동(6/15까지·실주문0·메인 PF1.18·승률47.5%). 옛 전략=AI_BRAIN 폐기·BLUECHIP_A~D 조건미충족(버그 아님). unfreeze A·B·C·D ✅, **E=페이퍼20거래일 중 1/20**(게이트배선 6/13 후).

---
*freeze·실주문0·매매로직0·production 배선0 유지. unfreeze는 E(페이퍼 20거래일) 완료 후.*
