# TODO 2026-07-06 — 미국판 미래가치 엔진 (US Future Value Engine)

> 작성: 2026-07-05. 배경: 7/5 한국 미래가치 엔진 v1(1~4번) 완료 후 퐝가님 질문
> "미국주식은 뭘 사야할지·진입가·컨센서스·밸류 어떻게 하고 있나" → 조사 결과
> **미국은 관측 2종(사이클 shadow + 밸류 스냅샷)뿐, 한국 같은 통합 픽·컨센서스·
> 진입 판단이 아예 없음**. 재료 절반은 이미 있으니 한국이 간 길을 그대로 이식.

## 재개 첫 동작 (0번 규칙)
1. 로컬+VPS `date` 확인 → 장날/BAT 실행여부/다음 장날
2. **7/5 밤 BAT-D 첫 산출 검증**(놓치면 안 됨):
   - pykrx 노이즈 "KRX 로그인 실패" 로그 **0건** 확인(`202286e` 최종확정)
   - `data/shadow/future_value.json` 정상(우선주 제외 154종·kospi/kosdaq 레짐)
   - `data/contract_history.jsonl`·`market_map.json`·`kosdaq_index.csv` 생성 확인
3. `git pull origin main` (HEAD = 오늘 마지막 커밋)

## 🎯 미국판 미래가치 엔진 — 한국 대비 격차·이식 계획

### 이미 있는 것 (재사용)
- **US 사이클**: `run_leader_cycle.py` US 28~30종(매수적기 7종=TSM·ASML·AVGO·MU·AAPL·QCOM·LLY), `data/shadow/leader_cycle.json`, delta=연간영업이익 YoY. → L축 그대로.
- **US 밸류 스냅샷**: `src/use_cases/valuation_band.py` US_POOL 30종(PER/forwardPER/PBR/ROE/FCF/verdict), yfinance. → V축 부분재료.
- **US 가격데이터**: `data/us_market/us_daily.parquet` + `leader_cycle/{tk}.parquet`(주봉).

### 없는 것 (신규 — 이식 순서)
**① US 컨센서스 (한국 wisereport 대체)** ★1순위
- yfinance `Ticker.info`: `targetMeanPrice`·`targetHighPrice`·`recommendationKey`·
  `numberOfAnalystOpinions`·`forwardEps`·`forwardPE`·`trailingPE`·`dividendYield`.
- 신규 `scripts/scan_consensus_us.py`(scan_consensus 미러) → `data/consensus_screening_us.json`
  (한국 consensus_screening과 동일 스키마: ticker/target_price/upside_pct/forward_eps/…).
- ★주의: yfinance rate-limit → 배치 지연·캐시. VPS 고정IP만(로컬 yfinance 차단).

**② US 역사 PER 밴드 (한국 v1-2번 이식)**
- `valuation_band_history.py`의 밴드 로직(자기 5년 PER pct_rank·흑자게이트·연속TTM·
  recency가드) 그대로. 단 EPS 소스=yfinance `quarterly_financials`/`income_stmt`
  (한국 fundamentals_historical.csv 대체). 가격=us_daily.parquet.
- 신규 `src/use_cases/valuation_band_history_us.py` 또는 기존 모듈 일반화(지수무관 index_regime처럼).

**③ US 통합 엔진 (한국 future_value_engine 이식)**
- 신규 `src/use_cases/future_value_engine_us.py` — 축 재구성(US엔 금투/연기금 수급·
  DART공시·RSS테마 없음):
  - V 밸류: US 컨센서스 목표가 괴리 + forwardPER 분위 + US PER밴드
  - E 실적: 연간영업이익 YoY(leader_cycle delta 재사용)
  - L 사이클: US leader_cycle signal(매수적기 초입 가점·경계/청산 차단)
  - **S 수급 대체**: 미국은 한국식 세분수급 없음 → v2로 미룸(옵션흐름·13F·ETF flow).
    v1은 V+E+L 3축으로 시작(정직).
  - 국면: US는 index_regime로 S&P500/나스닥 레짐(^GSPC/^IXIC yfinance).
- 산출 `data/shadow/future_value_us.json` 관측 전용.

**④ 백테스트 (backtest_first 필수)**
- US 종목 자기시장(S&P500/나스닥) 벤치마크 D+20 초과수익. ★7/5 교훈: 지수 CSV
  커버리지=백테스트기간 확인(드롭 카운터), 실행가능 시점(익일 시가), 생존편향.
- 컨센서스 목표가 괴리·PER밴드·사이클 각 팩터 예측력 검증 후 가점 확정(계수튜닝 금지).

**⑤ 적대검수 (Workflow)** — 한국과 동일 3렌즈(정확성·백테스트타당성·배선)+2표결 반박.

### v2 (데이터 더 쌓인 뒤)
- US 수급 대체(옵션 put/call·13F 기관보유변동·ETF 자금흐름)
- US 진입가 로직(피보나치/ICT의 US 확장 — 현재 KR 전용)
- 한국 FV와 US FV 통합 대시보드(글로벌 픽)

## 안전선 (불변)
실주문0·이중잠금·페이퍼만·관측(shadow)→백테스트→페이퍼→20일게이트→실전 순서 고정.
계수 임의튜닝 금지(분위·온오프 게이트만). VPS 검증 후 판정(import OK≠동작 OK).
배포는 장마감후 git pull(장중금지). yfinance는 VPS만(로컬 차단).

## 한국 쪽 미결 (병행/후순위)
- **메인A 리밸런스 SCAN/AA 잔존 구멍**(`weekly_rebalance` 유지 시 entry_date 리셋→
  차단대상 기존 SCAN/AA가 MAX_HOLD 영구회피): 유지판정 SCAN/AA 제외+orig_entry_date
  보존. 퐝가님 결정 대기 → [[session_state_7_4_pykrx_quiet_full]].
- 한국 FV shadow 20거래일 forward 게이트(D+20 초과 +2%p·승률55%) → 페이퍼 7번째 승격.
- BRAIN 레짐 BULL 인정 비대칭 완화(백테스트 후).
