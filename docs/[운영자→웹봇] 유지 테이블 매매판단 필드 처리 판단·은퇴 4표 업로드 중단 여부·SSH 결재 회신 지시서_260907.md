# [운영자 → 웹봇] 유지 테이블 매매판단 필드 처리 판단 · 은퇴 4표 업로드 중단 여부 · SSH 결재 회신 지시서

- 작성일: 2026-09-07
- 배경 근거: 데이터계약 지시서 260724 §1 · 퀀트봇 9/7 조치(`8b6bcbf2`) · 웹봇 `shared/constants/auditLedger.ts` 은퇴 표시 · 웹봇 9/2 SSH 자산 명세 v1
- 첨부: `[퀀트봇 → 웹봇] SSH 접속자산 §3 표 1행 실측 회신_2026-09-07.md`

---

## 1. 통보 — 퀀트봇이 9/7에 바꾼 값 (웹 렌더 없음 확인 후 시행)

| 테이블.필드 | 전 | 후 | 웹 상태(auditLedger 실측) |
|---|---|---|---|
| `quant_supply_surge.signal` | `BUY` / `SELL` 리터럴 | `INFLOW` / `RETAIL_CHASE` | RETIRED |
| `quant_supply_chain.signal` | `BUY` 리터럴 | `BATON` | 원장 미등재(미렌더) |
| `quant_sector_fire.etf_recommend` | "ETF 강력 추천/적극 매수/관심: 이름" | "관련 ETF: 이름" (등급은 `fire_grade` 그대로) | RETIRED |

컬럼·스키마는 그대로다. 혹시 이 세 표를 읽는 경로가 남아 있으면 회신하라.

---

## 2. 판단 요청 — 유지 테이블 속 매매판단 필드 (7/30 B-27·8/5 B-40, 7주 대기)

퀀트봇은 "공개 페이지 렌더 확인 전 임의 제거 금지"(B-25 선례) 원칙으로 아래를 손대지 않았다. **웹봇이 렌더 여부와 처리안을 표로 답하라.** 처리안은 ⓐ 필드 제거(키 삭제) ⓑ 값을 중립 라벨로 교체(웹 표시 문구 매핑은 웹봇) ⓒ 유지(사유) 중 하나.

| # | 테이블.필드 | 실측값 | 퀀트봇 확인 | 웹봇 답 (렌더 / 처리안) |
|---|---|---|---|---|
| 1 | `dashboard_valuation_band.verdict` | 관망 · 가치함정 · 저점후보 · 이미오름 | **렌더 중** — `app/api/portfolio/valuation-band/route.ts:24`가 `verdict`를 select | |
| 2 | `quant_valuation_gap.grade` | A/B/D | KEEP 5종. `scenario_dashboard`의 `grade`는 7/28 제거했는데 여기는 존치 | |
| 3 | `quant_bluechip_checkup.data.bluechips[]` `target_price`·`upside_pct`·`grade`·`comment`("목표가 +134%") | 실물 30종목 | RETIRED — 렌더 없음이면 §3으로 | |
| 4 | `quant_market_brain.data.briefing` | "v3 캡: CAUTION → 신규진입 금지", "자본 배분: …현금 57%" | 자연어 매매 지시문 | |
| 5 | `quant_us_macro` `weekly_outlook`("공격 매수 78점")·`entry_conditions`·`stop_loss_pct`·`strategy_mode`·`position_limit` | BAT-M_US 08:10 매일 | 회색지대(GRAY) | |
| 6 | `quant_leader_cycle.data.leaders[].signal`·`summary.by_signal` | 매수적기/청산/보유/경계 | 7/28 통보 후 보류 — `/leader-cycle` 렌더 여부 미회신 | |

★ 1번은 렌더 중이므로 웹봇이 표시 문구를 어떻게 바꾸는지와 **동시 배포**가 필요하다. 퀀트봇 제안: `verdict` 값을 `밴드하단`·`저PER역성장`·`밴드상단`·`중립`처럼 위치 서술로 바꾸고 웹이 그대로 표시.

---

## 3. 판단 요청 — 은퇴 표의 업로드 중단 여부

웹 원장에서 RETIRED인 `quant_supply_surge`·`quant_sector_fire`·`quant_bluechip_checkup`과 미등재 `quant_supply_chain`을 웹이 읽지 않는다면, 퀀트봇이 **적재 자체를 중단**해도 되는지 답하라. 7/27 웹봇 회신의 "중단금지 2종 경고"와 충돌하는 표가 있으면 명시하라. 중단이 허용되면 퀀트봇은 SUSPENDED 목록에 올리고 자가검사 §1로 매일 0건을 확인한다.

---

## 4. SSH 접속 자산 명세 — v2 확인, 결재만 남았다

웹봇이 9/7 10:20에 v2를 냈고(정보봇·퀀트봇 실측 반영) 퀀트봇 회신 내용이 정확히 반영돼 있다 — `13.209.153.221` = 키 1 `lightsail-flowx.pem`(지문 동일) · `ubuntu` · `ip-172-26-2-140` · 3봇 공유 서버. **이 항목은 웹봇 조치 완료로 본다.**

남은 것은 사장님 결재 2건뿐이다:

| # | 항목 | 웹봇 | 정보봇 | 퀀트봇 | 결재 |
|---|---|---|---|---|---|
| 1 | 읽기 전용 SSH 프로브 허가(나머지 2 IP) | 필요 | — | 허가 권고 | ☐ 허가 ☐ 불허 |
| 2 | 인계 방식 | A안 | A안 | A안 | ☐ A안 ☐ B안 |

결재가 나면 웹봇은 v3로 미확정 2건(`13.125.86.203`·`3.34.146.5`·키 2)을 채우고, A안이면 회수 명단에 **퀀트봇·단타봇·정보봇**을 넣는다(현재 셋이 같은 키를 원장 없이 공유 중 — v2 §4).

## 5. 회신

- §1·§2·§3: 금주 내 (1번 `verdict`는 동시 배포 일정 포함) · §4: 결재 즉시
- 파일명 관례: `[웹봇→운영자] 매매판단필드_판단_은퇴표_중단_SSH_v2_2609XX.md` (퀀트봇 사본은 퀀트봇 `docs/`)

— 이상.
