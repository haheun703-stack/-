# TODO 2026-07-14 — 7/13 배포 2건 첫 실동작 검증 + 슬러지 정리

> 작성: 2026-07-13 밤. 배경: 7/13 저녁 기초데이터 전체점검(GREEN) 후 개선 2건 배포.
> 두 픽스 모두 **다음 BAT-D(16:30)·BAT-HEALTH(18:45)부터 첫 실동작** → 내일 검증 필수.
> - `3342646` A-1: KIS 수급 수집대상에 유니버스 union (신규상장 편입 사각지대 해소)
> - `c135e90` (c): 수급커버리지 급감 감지 체크(18번) — 대량 수집장애 방어
> 상세 맥락 → 메모리 [[session_state_7_13_data_check_supply_universe_fix]]

## 재개 첫 동작 (0번 규칙)
1. 로컬+VPS `date` 확인 → 장날/BAT 실행여부/다음 장날
2. `git pull origin main` 불필요 (7/13 밤 VPS pull·검증 완료, HEAD=c135e90). 단 06:00 자동배포 로그만 확인.

## 🎯 A-1 신규상장 편입 첫 실동작 검증 (BAT-D 16:30 후)
- VPS cron 로그(`logs/cron_20260714.log`)에 **"유니버스 신규 편입: N종목 — A-1"** 라인 출력 확인.
- 레메디(387690) 등 **7/13엔 DB에 없어 수집 안 되던 신규상장주**가 investor_daily.db에 7/14자로 적재됐는지:
  ```
  ./venv/bin/python3.11 -c "import sqlite3; c=sqlite3.connect('data/investor_flow/investor_daily.db'); print(c.execute(\"select count(*) from investor_daily where date='20260714' and ticker='387690'\").fetchone())"
  ```
- 대상 종목수가 유니버스 편입분만큼 늘었는지(2938→+N) — insert 카운트가 아니라 **DB distinct로 확인**(delete-재작성 구조라 insert수≠순증가).

## 🎯 수급커버리지 체크(18번) 첫 실동작 검증 (BAT-HEALTH 18:45 후)
- 헬스체크 로그에 **"✅ 수급커버리지: 20260714 N종목 (직전 20260713 2644) 정상"** 출력 확인.
- 오탐 안 나는지: 7/14 정상 수집이면 직전(2644) 대비 ±수% → 통과여야. 만약 ❌ 급감 뜨면 KIS 실제 장애 신호이므로 즉시 원인 확인.

## 🎯 효성화학·스트라드비젼 자동 수집 확인
- 7/13 저녁 KIS 500으로 빠진 걸 21:15 **수동 개별 재시도로 해소**(2642→2644). 저녁500=일시적, 21시 회복 실측.
- 내일 BAT-D(정규 시각)가 이 2종목을 **자동으로** 잡는지 확인. 매일 자동으로 잡히면 개별 재시도 로직 불필요, 반복 누락이면 4주체 폴백 or 재시도 로직 검토.
- ★교훈 반영: KIS 저녁500은 종목 고정이슈 아니라 일시적일 수 있음 → 재시도 가치 있음.

## 🧹 퐝가님판단 7건 슬러지 정리 ("정리해" 한마디면 처리)
지난 세션(7/10 전체검수)부터 대기 중:
1. 6/14 실수 커밋된 `quant_bot (1).py` + bundle 제거
2. `COO_DAILY_MASTER.md` 루트/docs 완전중복 정리
3. `docs/SYSTEM_MAP.md` 4개월 스테일 → 갱신(추천)
4. `.env` 백업 2종 제거
5. 2~3월 다운로드 zip 15종 정리
6. `insight_signals_v0.2.0/` 스테이징 폴더 + zip (현재 로컬 git untracked 잔존)
7. VPS 배포백업 폴더 정리

## 한국 쪽 미결 (병행/후순위, 변동 없음)
- 메인A 리밸런스 SCAN/AA 잔존 구멍(유지판정 SCAN/AA 제외+orig_entry_date 보존) → 퐝가님 결정 대기.
- D002 임원매수 = 관찰만(7/10 승격 철회 확정). forward 축적.
- V3b 교체(BULL 조짐 시)·STOP_LOSS 사이징 백테스트·fund_PER 백필·주도섹터 페이드 사전등록 검증.

## 안전선 (불변)
실주문0·이중잠금·페이퍼만. VPS 검증 후 판정(import OK≠동작 OK). 배포는 장마감후 git pull(장중금지).
