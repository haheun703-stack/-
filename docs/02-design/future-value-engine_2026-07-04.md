# 미래가치 통합 엔진 (Future Value Engine) 설계 — 2026-07-04

> 배경: 퐝가님 지시 "가치주·주도주·밸류에이션·수주잔고로 미래가치/가격을 계산해
> 단기·중기·장기 예상을 만들고, KOSPI/KOSDAQ 전체 시장과 엮어서 진행할 로직을 코드로."
> 페이퍼 부검(6/27)·BRAIN 레짐 검증(7/4)·팩터 백테스트(7/4)가 설계 근거.

## 0. 데이터로 확인된 사실 (7/4 백테스트, VPS 실측)

1. **BRAIN 레짐 = 방어 유능·공세 무능** (brain_history 77판정 × KOSPI):
   조정(6/22~) +8.6%p 방어 성공 / 강세(~6/19) -40%p 미참여. BEAR 판정 D+5 적중 81%.
2. **밸류갭 등급(S~D) 그대로는 역효과**: S등급이 D+20 지수대비 -21%p 최악.
   원인 = 스코어가 `실적 급증(oi_yoy)` 편중 → 이미 급등한 모멘텀 피크를 최고점 매수.
3. **팩터 분해 (D+20 cross-sectional, 5/1~7/3 46스냅샷)**:
   - 저PER(저평가): +3.98%p·승률 67%·중앙값 +5.44%p ← 유일하게 셋 다 양호
   - 실적YoY 고분위: -2.77%p·승률 28% ← 독(피크 추격)
   - 턴어라운드: 평균 +3.59%p이나 승률 25%(복권형)
4. 교훈: **같은 "좋은 종목"도 사이클 위치·시장 국면에 따라 약이 되고 독이 된다.**
   → 종목 스코어(무엇을)와 국면 스위치(언제)를 반드시 분리 설계.

## 1. 목표 (정직한 기대치)

- ~~일 5%~~ → 불가능(연 환산 수십만%). 이를 목표로 하면 레버리지·과최적화로 파괴.
- **실제 목표**: ①지수 대비 연 +10%p 초과(알파) ②조정장 MDD 지수의 1/2 이하
  ③페이퍼 20거래일 게이트 통과 후 단계적 실전. 일 단위가 아니라 **월/분기 단위**로 평가.

## 2. 구조 — 3층

### 층1: 종목 미래가치 스코어카드 (FV Scorecard, 무엇을)

| 축 | 내용 | 데이터 소스 (기존 자산) | 타임프레임 |
|---|---|---|---|
| V 밸류에이션 갭 | 현재 PER/PBR vs 자기 역사 밴드 → 적정가·괴리% | valuation_gap 일별(129종) + parquet | 장기 |
| E 실적 가속 | 영업이익 TTM-YoY의 **델타**(가속/둔화) — 수준(oi_yoy) 아님 | DartAdapter.get_op_growth_series (leader_cycle 재사용) | 중기 |
| L 사이클 위치 | 주도주 사이클 초입/중기/후기 — 초입 가점·후기(경계) 감점 | data/shadow/leader_cycle.json (KR+US 90종) | 중기 |
| O 수주 정보태그 | 공급계약 공시 본문 파싱(계약금액/매출대비%) — **가점 0, 태그만** | dart_contract_parser + contract_history.jsonl (★7/5 스터디 v2 576건: raw D+1 팝 +5.34%는 전부 체결불가 갭, 실행가능(D+1 시가) -2.98%·승률 19% → 가점 기각. 이력은 v2 '수주잔고 누적/시총' 가설용 축적) | 태그 |
| S 스마트머니 | 금투+연기금+기타법인 5D/20D (5/14 검증 우선순위) | investor_daily.db 11세분 | 단기 |
| T 테마 촉매 | RSS 발화 + 정책테마 | theme_alerts_today.json | 단기 |

### 층2: 타임프레임 합성 (언제 실현되나)

- **단기(5~20일)**: S(0.4)+T(0.3)+기술위치(0.3). 단 V 극단 고평가면 진입 차단.
- **중기(20~60일)**: E(0.4)+L(0.4)+O(0.2). ★L 후기(경계)면 E 고득점도 무효(7/4 백테스트 근거).
- **장기(60일+)**: V(0.5)+O(0.3)+E(0.2). 적정가 괴리 -30% 이상 저평가만.

### 층3: 시장 국면 스위치 (KOSPI/KOSDAQ)

- BRAIN 레짐 재사용하되 **비대칭 적용** (7/4 검증):
  - BEAR/PRE_BEAR(방어, 적중 81%) → 단기·중기 비중 축소, **장기(가치) 적립만** 허용
  - CAUTION → 중기+장기, 저PER 가중 ↑
  - PRE_BULL/BULL(향후 "추세+모멘텀" 완화 조건 별도 백테스트) → 단기+중기 공세, E/L 가중 ↑
- KOSDAQ 별도 국면은 v1 (kosdaq 지수 시계열 수집부터).

## 3. 산출물

- `data/shadow/future_value.json` — 종목별 `{fv_short, fv_mid, fv_long, fair_gap_pct, 근거태그}`
  **관측 전용(shadow)** 부터. 매매 미배선.
- 검증 통과 시: ①tomorrow_picks 가점(theme_bonus 패턴) ②페이퍼 7번째 포트.

## 4. 구현 단계 (backtest_first)

1. **v0 (7/4)**: FV 스코어러 + shadow 러너 — V·E·L·S 기존 데이터 결합. 수동 실행 검증.
2. **v1**: O 강화(공시 원문에서 계약금액/기간 파싱 → 잔고/시총), KOSDAQ 지수 수집·국면.
3. **백테스트 확장**: 스냅샷 히스토리가 쌓일수록 층2 가중 검증(계수 튜닝 금지, 분위만).
4. **게이트**: D+20 초과수익 +2%p·승률 55% 이상 확인 후 페이퍼 배선(퐝가님 승인).

## 5. 안전선 (불변)

실주문 0·이중잠금·페이퍼만. 관측(shadow) → 백테스트 → 페이퍼 → (20일 게이트) → 실전 순서 고정.
계수 임의 튜닝 금지(과최적화) — 분위(상/중/하)와 온/오프 게이트만 사용.
