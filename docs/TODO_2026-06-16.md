# 퀀트봇 TODO — 2026-06-16 (화)

> 6/15(월) 사무실 세션 이월. 내일 바로 이어서 진행. 상세 맥락은 메모리 `session_state_6_15_*` 참조.

## 🔥 우선 (바로 진행)

1. **밸류밴드 통합 구현** — `scan_bluechip_checkup.py`(살아있음·BAT-D G4.5 배선)에 **FCF 가치함정 축 추가**.
   - 정보봇 데이터계약(`D:\Global_Stock_Overview_Scripter_정보봇\docs\to-quant\[정보봇→퀀트봇] 밸류밴드·2층·알림 데이터계약 확정 참조_2026-06-15.md`)대로 **3테이블 UPSERT 적재기** 구현:
     - `dashboard_valuation_band` (종목별: per/fwd_per/pbr/roe/pos_52w/fcf_yield/verdict/source/earnings_up)
     - `dashboard_two_layer` (1행: core/satellite_pct/cum_return/mdd/current_dd)
     - `dashboard_drawdown_alert` (1행: current_dd/level/verdict/foreign_outflow/...)
   - 하이브리드 원칙: checkup 계산 **재활용**(중복 금지) + FCF·verdict만 신규.
   - `foreign_outflow` = 오늘 만든 KIS 수급(investor_daily.db 외인) 활용.
   - verdict 로직·KR소스 검증은 정보봇 분담.

2. **매일 자동수집 BAT 배선** — `collect_investor_kis.py`(견고화 완료, 커밋 9ecf8d0)를 `scripts/cron/run_bat.sh`(BAT-D)에 배선해 매일 장마감 후 KIS 수급 자동 수집. KRX 완전 독립.

3. **슬러지 삭제 확인** — `_tmp_processed_5_29.tar.gz` (489MB, 5/29 데이터 임시 백업) 삭제 여부 결정 (권한상 사장님 명시 승인 필요). 나머지(quant_bot(1).py·bundle·옛버전 tar)는 보존.

## ⏳ 대기/조건부

- **KRX 계정 잠금 해제** (서비스 복구 후 — 6/15 저녁 KRX "Service unavailable" 일시 장애) → 풀리면 국적별 수급 `nationality.db`(6/8 정지) backfill. ⚠️ 무리한 재시도 금지(IP 차단 우려).
- **국적별 수급 대안** — KIS 종목별 세분 미제공 → 키움 opt10059(별도 계좌) 검토 or KRX 잠금 해제 대기.
- **정보봇 의뢰서 정정+전달** — 밸류밴드 통합 방향 반영해 의뢰 내용 수정 후 정보봇 repo로 전달(파일시스템 직접).
- **PCE actual backfill** — 6월말 BEA 발표 후 FRED에서 backfill (현재 미발표=정상).
- **scoreboard 재소유 / ⓐ 재집계** — 별도 트랙(VPS 집계기 정체 추적 필요).

## ✅ 완료 (6/15 사무실)

- 6/15 기초데이터 전량 backfill — KRX 잠금 우회, KIS 자급 (종가·수급·지표·investor_daily.db 2717종목·csv)
- `collect_investor_kis.py` 정식화 + 코드검수 8건 견고화 (커밋 9ecf8d0, push 완료)
- scan_bluechip_checkup 오판 정정 → 밸류밴드 **통합 방향** 확정
- 정보봇 데이터계약 수신·검토 (통합 방향과 정합 확인)

---
*freeze·실주문0·매매로직0 유지. 실배선은 unfreeze(페이퍼 20일) 후.*
